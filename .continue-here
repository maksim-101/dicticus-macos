2026-05-16 — Phase 25 in LIVE-CAPTURE mode. Resume after dictating for a few days.

## TL;DR

V16-COMPOSITE prompt + Lever 1 dictionary expansion shipped directly to the running app, bypassing the planned harness-verify / tests / formal-UAT path. User is testing live. Resume = "analyze the JSONL".

## What's running RIGHT NOW

- App: `/Applications/Dicticus.app`, Debug-Recorder build, Developer ID signed (VTWHBCCP36)
- Code state: commit `0fc3198` (feat 25-03 ship) + `b17ed32` (install script fix). Both LOCAL ONLY — 13 commits ahead of origin (push deferred).
- Logging: every dictation cycle writes one JSONL record to `~/Library/Application Support/Dicticus/DebugRecordings/cleanup-2026-05-DD.jsonl`. Plain AND AI mode both logged. AI-mode records carry full LLM prompt + raw output + post-gate alongside the raw ASR.

## Wave 2 launch checklist for resume session

1. **Locate accumulated logs**:
   ```bash
   ls -la ~/Library/Application\ Support/Dicticus/DebugRecordings/cleanup-2026-05-*.jsonl
   ```
   Expect one file per day the user dictated. The 16th already has ~110+ records from this session's mixed test/dictation.

2. **Verify V16-COMPOSITE is in the recent records** (sanity check the live build wasn't replaced):
   ```bash
   tail -1 ~/Library/Application\ Support/Dicticus/DebugRecordings/cleanup-2026-05-*.jsonl | python3 -c "import json,sys; r=json.loads(sys.stdin.read()); p=(r.get('steps',{}).get('llm_prompt') or {}).get('text','') or ''; print('V16-COMPOSITE:', 'Domain topic words' in p and 'Spelled-out two-digit' in p)"
   ```

3. **Run the analysis** (the actual Wave 2 work):
   - Per-category lev deltas vs V15 baselines (categories: brand, anchor, num, acro, acro_enum, phase_face, ctrl, regress)
   - Dictionary-vs-LLM attribution: how often does `steps.post_dict.text != steps.raw.text` (= dict fired)? How often does `steps.post_gate.text != steps.post_rules.text` (= LLM contributed)?
   - Regression check against the 13 fixtures in `.planning/debug/harness/fixtures/phase25_brands.tsv`
   - Cross-check against memories: `project_v15_capture_findings`, `project_dictionary_dominates_brand_fixing`

4. **Conclude Phase 25**:
   - If clean: write `25-03-SUMMARY.md` + `25-04-SUMMARY.md` (live-capture replaced the planned 25-04 format), mark Phase 25 SHIPPED in STATE.md
   - If regressions: open `25-03b` with targeted fixes. Don't roll back the whole change — H3 (phase/face) and dictionary entries are independent of H4 (number integrity)

5. **Push the branch** (~13+ commits ahead — overdue per CLAUDE.md push cadence).

## What was skipped (deliberately) — re-add only if live-capture surfaces issues

- 25-03 Task 3 (V17 harness verification) — skipped per user direction
- 25-03 Task 4 (regression-net tests in CleanupPromptTests / SelfCorrectionResolverTests) — skipped; only add if a specific regression worth pinning emerges
- 25-03 Task 5 (formal UAT checkpoint) — superseded by live-capture analysis

## Rollback (if live-capture finds catastrophe)

```bash
git revert 0fc3198  # rolls back the V16-COMPOSITE + dict expansion
# Then rebuild + reinstall:
cd macOS && xcodebuild -project Dicticus.xcodeproj -scheme Dicticus-Debug-Recorder -configuration Debug-Recorder -derivedDataPath build CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO build
cd .. && ./scripts/install-local.sh
```

## Signing identity (in case it's missing again)

If `security find-identity -v -p codesigning` shows 0 identities, restore the p12 from 1Password — see memory `feedback_developer_id_signing_recovery` for the exact recovery commands. Vault TrueNAS, item `sqn2j6zeygtpewb2v66expqxva`, file `Certificates.p12`. Expected hash after import: `B9CA1FF8209D9B1BD4940F2D39C327EF836FD3C0`.

## What NOT to do on resume

- Don't re-run the matrix unless live-capture finds a specific regression to investigate
- Don't add tests speculatively (per memory `feedback_tests_as_regression_nets` — tests pin specific regressions, not coverage)
- Don't try to "finish" Tasks 3/4/5 unless they're actually needed — the live-capture path is the new Wave 2 and should be evaluated on its own merits
