---
phase: "12"
plan: "02"
type: "execute"
wave: 2
depends_on: ["12-01"]
files_modified:
  - "Shared/Utilities/ITNUtility.swift"
  - "Shared/Services/DictionaryService.swift"
  - "Shared/Services/HistoryService.swift"
  - "Shared/Services/TextProcessingService.swift"
  - "macOS/Dicticus/Services/CleanupService.swift"
  - "macOS/Dicticus/Dicticus.entitlements"
  - "macOS/project.yml"
  - "macOS/Dicticus/DicticusApp.swift"
autonomous: true
requirements: ["INFRA-01", "INFRA-03"]
must_haves:
  truths:
    - "Services use App Groups for shared storage"
    - "Text processing depends on protocol rather than concrete CleanupService"
    - "macOS app maintains all existing functionality with no test regressions"
  artifacts:
    - path: "Shared/Services/HistoryService.swift"
      provides: "Cross-platform transcription history"
    - path: "macOS/Dicticus/Dicticus.entitlements"
      provides: "App Groups capability"
  key_links:
    - from: "Shared/Services/TextProcessingService.swift"
      to: "Shared/Protocols/CleanupProvider.swift"
      via: "dependency injection"
---

<objective>
Extract the heavy lifting services into `Shared/`, wiring them to support iOS concurrency and App Groups data sharing. Update the macOS target to authorize this new App Group container, ensuring seamless cross-platform functionality.

Purpose: Shared services must operate safely across different target runtimes. `DatabasePool` allows concurrent iOS extension access, and App Groups allow data to be shared between the main app and Shortcut intents.
Output: Shared services, updated `CleanupService` conformance, and a verified macOS test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/PHASE-12-DECISIONS.md
@.planning/phases/12-shared-code/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Services Move & App Groups Refactor</name>
  <files>Shared/Services/DictionaryService.swift, Shared/Services/HistoryService.swift, macOS/Dicticus/Dicticus.entitlements, macOS/project.yml</files>
  <action>
    Create `Shared/Services` and move `macOS/Dicticus/Services/DictionaryService.swift` and `HistoryService.swift` there.
    In `HistoryService`: Update GRDB initialization to use `DatabasePool(path:)` instead of `DatabaseQueue` for cross-process concurrency. Change DB folder path from `.applicationSupportDirectory` to `FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: "group.com.dicticus")`.
    In `DictionaryService`: Change `UserDefaults.standard` to `UserDefaults(suiteName: "group.com.dicticus")`.
    Update `macOS/Dicticus/Dicticus.entitlements` to add `<key>com.apple.security.application-groups</key><array><string>group.com.dicticus</string></array>`. Add this property to `macOS/project.yml` as well under `entitlements.properties`.
  </action>
  <verify>
    <automated>grep "DatabasePool" Shared/Services/HistoryService.swift && grep "group.com.dicticus" Shared/Services/DictionaryService.swift</automated>
  </verify>
  <done>Services moved to Shared/ and updated for App Groups storage</done>
</task>

<task type="auto">
  <name>Task 2: ITN & Text Processing Protocol Wiring</name>
  <files>Shared/Utilities/ITNUtility.swift, Shared/Services/TextProcessingService.swift, macOS/Dicticus/Services/CleanupService.swift, macOS/Dicticus/DicticusApp.swift</files>
  <action>
    Move `macOS/Dicticus/Utilities/ITNUtility.swift` and `macOS/Dicticus/Services/TextProcessingService.swift` to `Shared/`.
    Refactor `TextProcessingService` to take `CleanupProvider?` instead of `CleanupService?` in its initializer and properties.
    Update `macOS/Dicticus/Services/CleanupService.swift` to declare conformance to `CleanupProvider`.
    Ensure `macOS/Dicticus/DicticusApp.swift` initializes `TextProcessingService` properly if needed.
  </action>
  <verify>
    <automated>grep "CleanupProvider" Shared/Services/TextProcessingService.swift</automated>
  </verify>
  <done>Text processing is decoupled from macOS-specific CleanupService</done>
</task>

<task type="auto">
  <name>Task 3: macOS Project Verification</name>
  <files>macOS/project.yml</files>
  <action>
    Run `cd macOS && xcodegen` to regenerate the Xcode project with the new entitlements and moved files.
    Run `xcodebuild test` for the macOS target. The tests MUST pass to ensure no regression in `HistoryService` and `DictionaryService`.
    Resolve any missing imports or path issues caused by the move.
  </action>
  <verify>
    <automated>cd macOS && xcodebuild test -scheme DicticusTests -destination "platform=macOS" | grep "TEST SUCCEEDED"</automated>
  </verify>
  <done>macOS application passes all 158 tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | App Groups container `group.com.dicticus` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | History/Dictionary DB | mitigate | Use DatabasePool WAL mode to handle concurrent writes without corruption |
| T-12-03 | Information Disclosure | App Groups Container | accept | Only Dicticus extensions can read the group container |
</threat_model>

<verification>
- Verify DB paths use App Groups.
- Verify text processing builds without `#if os(macOS)`.
- Verify macOS unit tests pass.
</verification>

<success_criteria>
All platform-agnostic services are extracted to `Shared/`. Dependencies inverted via `CleanupProvider`. macOS tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-shared-code/12-02-SUMMARY.md`
</output>