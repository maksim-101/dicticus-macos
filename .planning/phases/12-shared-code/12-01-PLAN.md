---
phase: "12"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "Shared/Protocols/CleanupProvider.swift"
  - "Shared/Models/TranscriptionResult.swift"
  - "Shared/Models/CleanupPrompt.swift"
  - "Shared/Models/DictationMode.swift"
  - "macOS/Dicticus/Services/HotkeyManager.swift"
  - "macOS/project.yml"
autonomous: true
requirements: ["INFRA-01"]
must_haves:
  truths:
    - "Base models and protocols compile successfully within the Shared/ directory"
    - "macOS target includes the new Shared/ source directory"
  artifacts:
    - path: "Shared/Protocols/CleanupProvider.swift"
      provides: "Cleanup dependency abstraction"
    - path: "Shared/Models/DictationMode.swift"
      provides: "Dictation state enum"
  key_links:
    - from: "macOS/project.yml"
      to: "Shared/"
      via: "source path inclusion"
---

<objective>
Extract the base data models and establish the `CleanupProvider` protocol in `Shared/` so that higher-level services can be migrated in the next plan without circular dependencies or unresolvable macOS-specific types.

Purpose: Break the hard dependency on `CleanupService` (which uses llama.cpp) by substituting it with a protocol `CleanupProvider`, allowing iOS to safely compile the shared code by injecting `nil`.
Output: Shared base models, new protocol, and updated macOS build definition.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/PHASE-12-DECISIONS.md
@.planning/phases/12-shared-code/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protocol & Base Models Extraction</name>
  <files>Shared/Protocols/CleanupProvider.swift, Shared/Models/TranscriptionResult.swift, Shared/Models/CleanupPrompt.swift</files>
  <action>
    Create directories `Shared/Protocols` and `Shared/Models`.
    Move `macOS/Dicticus/Models/TranscriptionResult.swift` and `macOS/Dicticus/Models/CleanupPrompt.swift` to `Shared/Models/` using `mv`.
    Create `Shared/Protocols/CleanupProvider.swift` defining `protocol CleanupProvider: Sendable`. It must include `var isLoaded: Bool { get }` and `func cleanup(text: String, language: String, dictionaryContext: [String: String]) async -> String`.
  </action>
  <verify>
    <automated>ls Shared/Models/TranscriptionResult.swift && ls Shared/Protocols/CleanupProvider.swift</automated>
  </verify>
  <done>Base models are moved and CleanupProvider protocol is defined</done>
</task>

<task type="auto">
  <name>Task 2: DictationMode Extraction</name>
  <files>Shared/Models/DictationMode.swift, macOS/Dicticus/Services/HotkeyManager.swift</files>
  <action>
    Extract the `DictationMode` enum from `macOS/Dicticus/Services/HotkeyManager.swift` into a new file `Shared/Models/DictationMode.swift`.
    Ensure `DictationMode` is public/internal as needed and remains `Sendable` and `CaseIterable`.
    Remove the nested enum from `HotkeyManager` so it uses the shared one.
  </action>
  <verify>
    <automated>grep "enum DictationMode" Shared/Models/DictationMode.swift</automated>
  </verify>
  <done>DictationMode is extracted and accessible globally</done>
</task>

<task type="auto">
  <name>Task 3: macOS Project Integration</name>
  <files>macOS/project.yml</files>
  <action>
    Update `macOS/project.yml` under `targets.Dicticus.sources` to include `- path: ../Shared`.
    Run `cd macOS && xcodegen` to regenerate the macOS `.xcodeproj`.
    Run macOS tests to ensure the project still compiles and functions identically.
  </action>
  <verify>
    <automated>cd macOS && xcodebuild test -scheme DicticusTests -destination "platform=macOS" | grep "TEST SUCCEEDED"</automated>
  </verify>
  <done>macOS project successfully incorporates the Shared directory and passes all tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process boundary | Cross-process communication between app targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Shared Models | mitigate | Ensure base models do not unintentionally expose PII without requiring app-level permissions |
</threat_model>

<verification>
- Verify `CleanupProvider` has correct signature.
- Verify `DictationMode` is visible to `HotkeyManager`.
- Verify `xcodegen` successfully runs.
- Verify macOS unit tests pass.
</verification>

<success_criteria>
Models and protocols extracted. macOS app builds with `Shared/` as a source and passes 158 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-shared-code/12-01-SUMMARY.md`
</output>