---
phase: "12"
plan: "03"
type: "execute"
wave: 3
depends_on: ["12-02"]
files_modified:
  - "iOS/Dicticus.xcodeproj"
  - "iOS/Dicticus/DicticusApp.swift"
  - "iOS/Dicticus/ContentView.swift"
autonomous: false
requirements: ["INFRA-02", "INFRA-03"]
must_haves:
  truths:
    - "iOS app compiles and launches successfully"
    - "Shared code is successfully referenced by the iOS target"
  artifacts:
    - path: "iOS/Dicticus/DicticusApp.swift"
      provides: "iOS App Entry Point"
  key_links:
    - from: "iOS/Dicticus/DicticusApp.swift"
      to: "Shared/Services/TextProcessingService.swift"
      via: "Shared dependencies initialization"
---

<objective>
Establish the bare iOS application scaffold in Xcode, import the `Shared/` codebase, and verify successful compilation on the iOS Simulator.

Purpose: Provide a clean iOS target baseline that natively hooks into the `Shared/` services without compiling macOS-specific code like llama.cpp.
Output: An Xcode iOS project building successfully.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/PHASE-12-DECISIONS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>iOS target requires manual Xcode scaffolding to avoid xcodegen issues with Intents later.</what-built>
  <how-to-verify>
    1. Open Xcode and select File > New > Project > iOS App.
    2. Name it "Dicticus", select SwiftUI, and set Bundle Identifier to "com.dicticus.ios".
    3. Save the project inside the `iOS/` folder (so it becomes `iOS/Dicticus.xcodeproj`).
    4. In Xcode Project Settings > Signing & Capabilities, add the "App Groups" capability and enable `group.com.dicticus`.
    5. Drag the `Shared/` folder into the Xcode project navigator under the Dicticus group, ensuring "Create groups" is selected and it is added to the Dicticus target.
  </how-to-verify>
  <resume-signal>Type "approved" or "done" after creating the project.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: iOS Base Wiring & Verification</name>
  <files>iOS/Dicticus/DicticusApp.swift, iOS/Dicticus/ContentView.swift</files>
  <action>
    Modify `iOS/Dicticus/DicticusApp.swift` to initialize the shared environment.
    Instantiate `DictionaryService.shared`, `HistoryService.shared`, and `TextProcessingService` (passing `cleanupService: nil`).
    Inject them into the SwiftUI environment.
    Modify `iOS/Dicticus/ContentView.swift` to display a basic "Dicticus iOS Scaffold" UI to confirm launch.
    Run `xcodebuild -project iOS/Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 15 Pro" build` to verify the iOS target compiles successfully with the shared code.
  </action>
  <verify>
    <automated>xcodebuild -project iOS/Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 15 Pro" build | grep "BUILD SUCCEEDED"</automated>
  </verify>
  <done>iOS app target compiles and can reference Shared/ code</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build System | Target boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Elevation of Privilege | App Groups | accept | Development environment only; real constraints apply at distribution |
</threat_model>

<verification>
- Verify `iOS/Dicticus.xcodeproj` exists.
- Verify `Shared/` services compile under iOS without errors.
- Verify `xcodebuild` succeeds for the iOS target.
</verification>

<success_criteria>
A bare iOS application can build from Xcode, fully importing `Shared/` services.
</success_criteria>

<output>
After completion, create `.planning/phases/12-shared-code/12-03-SUMMARY.md`
</output>