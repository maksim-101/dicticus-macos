---
phase: 19.5-ai-cleanup-ch-determinism
verified: 2026-04-26T08:00:00Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
---

# Phase 19.5: AI Cleanup CH-Determinism — Verification Report

**Phase Goal (from ROADMAP):** Make AI-cleanup output reliably Swiss-correct on iOS and macOS in a deterministic way: ASCII-apostrophe thousands separator, period decimal everywhere when Swiss toggle is ON, Helvetism prompt block, German-locale currency anti-flip pipeline (CHF↔EUR↔USD↔GBP), Swiss default ON migration, macOS toggle UI exposure, and the B2 Parakeet false-re-download hotfix.

**Verified:** 2026-04-26
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (per fix tag)

| #   | Fix Tag(s)        | Truth                                                                                                              | Status     | Evidence                                                                                                                                                          |
| --- | ----------------- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D-A1              | iOS Swiss German toggle defaults ON for fresh installs                                                             | ✓ VERIFIED | `iOS/Dicticus/Settings/AiCleanupSection.swift:95` — `appGroupBinding("useSwissGerman", default: true)`                                                           |
| 2   | D-A2              | macOS users have a visible Swiss toggle in menu-bar dropdown                                                       | ✓ VERIFIED | `macOS/Dicticus/Views/SwissGermanToggleRow.swift` (45 lines, AppGroup-bound, Toggle in HStack) + mount at `MenuBarView.swift:149`                                |
| 3   | D-A3              | Migration runs idempotently on both iOS+macOS launch paths                                                         | ✓ VERIFIED | `Shared/Utilities/SwissDefaultMigration.swift:29-36` (gated on `swissDefaultMigratedV2_1` key) + 4 call sites: iOS `DicticusApp.swift:39,55`, macOS `DicticusApp.swift:49`, macOS `SwissGermanToggleRow.swift:22` (B4 self-heal) |
| 4   | B2 / D-D1         | App relaunch with cached model does NOT show "needs download"                                                      | ✓ VERIFIED | `IOSModelWarmupService.swift:107` — `checkHasModels()` runs at top of `warmup()`; `iOS/Dicticus/DicticusApp.swift:60` — also called on scenePhase .active        |
| 5   | B3                | SwissNumberFormatter pre-classifies tokens BEFORE parsing (no speculative parseSwiss)                              | ✓ VERIFIED | `Shared/Utilities/SwissNumberFormatter.swift:95-101` — `containsApostrophe` gate routes Swiss-formatted tokens to `parseSwiss`, others to `parseGerman → parseSwiss` fallback. `parseGerman` further locked by `isStrictGermanThousands` (WR-02 fix, lines 148-170) |
| 6   | B5 / D-B1c        | CurrencyAntiFlip detects + reverts CHF/EUR/USD/GBP family flips                                                    | ✓ VERIFIED | `Shared/Utilities/CurrencyAntiFlip.swift:24-138`. `Family` enum has 4 cases; `revertCurrencyFlip` invoked at `CleanupService.swift:240` post-strip, gated `language == "de"` |
| 7   | B5 type-shape lock | `Family.allCases.count == 4` test enforces shape independent of source layout                                      | ✓ VERIFIED | `iOS/DicticusTests/CurrencyAntiFlipTests.swift:11` — `XCTAssertEqual(CurrencyAntiFlip.Family.allCases.count, 4)`                                                  |
| 8   | B6 / D-C2         | Swiss decimal separator is period (no comma decimals when Swiss toggle ON)                                         | ✓ VERIFIED | `SwissNumberFormatter.swift:180` — `formatter.decimalSeparator = "."`; invoked at `CleanupService.swift:254` inside `if useSwissGerman` block                    |
| 9   | W7                | Currency-glyph (`€/$/£`) prefix is stripped and reattached around number formatting                                | ✓ VERIFIED | `SwissNumberFormatter.swift:50` (`leadingGlyphs: Set<Character> = ["€", "$", "£"]`), `:64-72` strip, `:108` reattach                                          |
| 10  | W9                | License attribution is a runtime constant (works in CI and shipped .ipa, no `#filePath` walk)                       | ✓ VERIFIED | `SwissHelvetisms.swift:22-24` — public `licenseAttribution` constant containing "CC BY-SA 3.0" and "Liste von Helvetismen"; runtime asserted at `SwissHelvetismsTests.swift:33-36` |
| 11  | D-B1a/b/c         | Currency anti-flip wired both pre-LLM (prompt anchor) and post-LLM (revert)                                        | ✓ VERIFIED | Pre: `CleanupPrompt.swift:82-90` calls `CurrencyAntiFlip.detectCurrencies(in: text)` and emits STRICT line. Post: `CleanupService.swift:239-241` invokes `revertCurrencyFlip` after stripPreamble, gated `language == "de"` |
| 12  | D-C1/C2/C3        | SwissNumberFormatter runs inside Swiss-toggle-gated post-pass                                                       | ✓ VERIFIED | `CleanupService.swift:248-255` — both `applySwissITN` and `SwissNumberFormatter.format(cleaned)` run inside the `if useSwissGerman` block                       |
| 13  | D-D2              | HELVETISMS block included when Swiss toggle ON + language == "de"                                                  | ✓ VERIFIED | `CleanupPrompt.swift:66-73` — `swissEnabled && language == "de"` gate emits `HELVETISMS:` line backed by `SwissHelvetisms.words.joined(separator: ", ")`        |

**Score:** 13/13 truths verified. (Note: presented as 12/12 in frontmatter because B5 type-shape lock collapses into B5/D-B1c family verification; counted separately above for traceability.)

### Required Artifacts

| Artifact                                              | Expected                                                | Exists | Substantive | Status     |
| ----------------------------------------------------- | ------------------------------------------------------- | ------ | ----------- | ---------- |
| `Shared/Utilities/SwissDefaultMigration.swift`        | Idempotent migration helper (D-A3)                      | ✓      | ✓ 38 lines  | ✓ VERIFIED |
| `Shared/Models/SwissHelvetisms.swift`                 | ~30 Helvetisms + W9 license constant                    | ✓      | ✓ 58 lines, 30 entries | ✓ VERIFIED |
| `Shared/Utilities/CurrencyAntiFlip.swift`             | Pre-LLM detect + post-LLM revert (B5/D-B1)              | ✓      | ✓ 139 lines | ✓ VERIFIED |
| `Shared/Utilities/SwissNumberFormatter.swift`         | B3 pre-classification + W7 glyph + D-C1/C2/C3           | ✓      | ✓ 220 lines | ✓ VERIFIED |
| `Shared/Models/CleanupPrompt.swift`                   | STRICT anchor + HELVETISMS block (D-B1b, D-D2)          | ✓      | ✓ STRICT line :86, HELVETISMS :70 | ✓ VERIFIED |
| `Shared/Services/CleanupService.swift`                | Post-LLM revert + number-formatter wiring (D-B1c, D-C2/C3) | ✓ | ✓ revert :240, format :254 | ✓ VERIFIED |
| `iOS/Dicticus/Settings/AiCleanupSection.swift`        | `default: true` for useSwissGerman (D-A1)               | ✓      | ✓ line 95   | ✓ VERIFIED |
| `iOS/Dicticus/DicticusApp.swift`                      | Migration + checkHasModels wired                        | ✓      | ✓ migration :39,55; checkHasModels :60 | ✓ VERIFIED |
| `macOS/Dicticus/Views/SwissGermanToggleRow.swift`     | macOS toggle UI (D-A2)                                  | ✓      | ✓ 45 lines  | ✓ VERIFIED |
| `macOS/Dicticus/Views/MenuBarView.swift`              | Mount point for SwissGermanToggleRow                    | ✓      | ✓ line 149  | ✓ VERIFIED |
| `macOS/Dicticus/DicticusApp.swift`                    | Migration runs in `.task`                                | ✓      | ✓ line 49   | ✓ VERIFIED |
| `iOS/DicticusTests/CurrencyAntiFlipTests.swift`       | Type-shape (B5) + revert tests                          | ✓      | ✓ 4253 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/SwissNumberFormatterTests.swift`   | B3 + W7 unit + fixture tests                            | ✓      | ✓ 3353 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/SwissHelvetismsTests.swift`        | Word-count + W9 attribution test                        | ✓      | ✓ 1728 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/SettingsToggleTests.swift`         | Migration idempotence tests                             | ✓      | ✓ 4735 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/CleanupServiceTests.swift`         | B5/B6 integration tests                                 | ✓      | ✓ 11954 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/Fixtures/CurrencyAntiFlip.fixtures.json` | Currency revert corpus                            | ✓      | ✓ 978 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/Fixtures/SwissNumberFormatter.fixtures.json` | Number formatter corpus                       | ✓      | ✓ 478 bytes | ✓ VERIFIED |
| `iOS/DicticusTests/Fixtures/SwissGerman.fixtures.json`     | Swiss German cleanup corpus                         | ✓      | ✓ 650 bytes | ✓ VERIFIED |

### Key Link Verification

| #   | From                                  | To                                       | Via                                                                  | Status   |
| --- | ------------------------------------- | ---------------------------------------- | -------------------------------------------------------------------- | -------- |
| 1   | `iOS/Dicticus/DicticusApp.swift`      | `SwissDefaultMigration.runIfNeeded()`    | `.onAppear` (line 39) AND `scenePhase .active` (line 55)             | ✓ WIRED  |
| 2   | `macOS/Dicticus/DicticusApp.swift`    | `SwissDefaultMigration.runIfNeeded()`    | `MenuBarExtra` `.task` (line 49)                                     | ✓ WIRED  |
| 3   | `macOS/.../SwissGermanToggleRow.swift` | `SwissDefaultMigration.runIfNeeded()`   | `currentValue()` self-heal (line 22, B4 race fix)                    | ✓ WIRED  |
| 4   | `iOS/Dicticus/DicticusApp.swift`      | `IOSModelWarmupService.checkHasModels()` | `scenePhase .active onChange` (line 60)                              | ✓ WIRED  |
| 5   | `IOSModelWarmupService.warmup()`      | `self.checkHasModels()`                  | First statement in warmup body (line 107)                            | ✓ WIRED  |
| 6   | `MenuBarView.swift`                   | `SwissGermanToggleRow()`                 | Direct mount (line 149)                                              | ✓ WIRED  |
| 7   | `CleanupPrompt.build`                 | `CurrencyAntiFlip.detectCurrencies`      | Pre-LLM detection on raw `text` (line 83), gated `language == "de"`  | ✓ WIRED  |
| 8   | `CleanupPrompt.build`                 | `SwissHelvetisms.words`                  | HELVETISMS block, gated `swissEnabled && language == "de"` (line 71) | ✓ WIRED  |
| 9   | `CleanupService.cleanup`              | `CurrencyAntiFlip.revertCurrencyFlip`    | Post-strip, pre-Swiss block, gated `language == "de"` (line 240)     | ✓ WIRED  |
| 10  | `CleanupService.cleanup`              | `SwissNumberFormatter.format`            | After applySwissITN inside `if useSwissGerman` block (line 254)      | ✓ WIRED  |

### Wiring Sequence Verification (CleanupService.cleanup post-LLM pipeline)

The post-LLM pipeline order is critical to D-B1c / D-B2 contract. Verified order in `CleanupService.swift:230-255`:

1. `var cleaned = Self.stripPreamble(result)` — line 230
2. `if language == "de" { cleaned = CurrencyAntiFlip.revertCurrencyFlip(...) }` — line 239-241 (BEFORE Swiss block, German-wide)
3. `if useSwissGerman { ITNUtility.applySwissITN(to: cleaned); SwissNumberFormatter.format(cleaned) }` — line 248-255 (Swiss-toggle-gated)
4. Return `cleaned.isEmpty ? text : cleaned`

This matches the must_haves contract: currency revert is German-wide; number formatting is Swiss-toggle-gated.

### Anti-Patterns Found

None. Production wiring uses the new utilities; tests cover all fix tags. Three code-review warnings (WR-01/WR-02/WR-03) were closed in commits d0f517f / 649de89 / f82576e prior to verification.

### Orphan / Unwired Findings

**None.** Every utility added by 19.5-03 has at least one production consumer in 19.5-04 wiring:

- `SwissDefaultMigration` → 4 call sites (iOS app x2, macOS app x1, macOS toggle row x1) + 4 test sites
- `SwissHelvetisms` → consumed in `CleanupPrompt.build` line 71 + test asserts on `licenseAttribution` constant
- `CurrencyAntiFlip` → consumed in `CleanupPrompt.build` line 83 (detect) + `CleanupService.cleanup` line 240 (revert)
- `SwissNumberFormatter` → consumed in `CleanupService.cleanup` line 254

No `Shared/Utilities/*` or `Shared/Models/*` file added by this phase is dead code.

### Behavioral Spot-Checks

Skipped per scope note — iOS xcodebuild blocked by pre-existing `IOSTranscriptionService.swift:232` error (documented in 19.5-04 SUMMARY) and macOS test target's path config. Static + read-based verification deemed sufficient. The new tests are wired to the iOS test target and exist on disk; they will execute once the unrelated iOS build error is resolved.

### Requirements Coverage

Phase declares no direct requirements (parent CLEAN-01/CLEAN-02 already met by Phase 19; this phase tightens correctness from B5/B6/S7/S8 UAT findings + B2 regression). Cross-checked `.planning/REQUIREMENTS.md`: no requirement IDs map to Phase 19.5. No orphaned requirements.

### Human Verification Required

None for this static verification. Live UAT (real device, real Gemma inference) is recommended in a follow-up to confirm the deterministic Swiss output behaves as expected on:

- CHF/EUR/USD/GBP currency utterances (B5 anti-flip)
- "fünf Komma siebzig" → "5.70" (B6 decimal separator)
- "tausendzweihundertfünfzig Franken" → "1'250 Franken" (D-C1 ASCII apostrophe)
- "ich nehme das Velo" → preserved Helvetism (D-D2)

These are user-perception checks beyond the scope of this static verification.

### Gaps Summary

No gaps. All 13 observable truths verify in the codebase. All 4 utilities created in 19.5-03 are wired into production code paths in 19.5-04. All 4 launch-path migration call sites are present. All B2/B3/B5/B6/W7/W9/D-A1/D-A2/D-A3/D-B1a/D-B1b/D-B1c/D-C1/D-C2/D-C3/D-D2 fix tags are implemented and reachable. Three code-review warnings are closed (WR-01/WR-02/WR-03 commits).

The phase achieves its stated goal: Swiss-correct, deterministic AI-cleanup output on iOS and macOS, with the B2 Parakeet relaunch hotfix in place.

---

_Verified: 2026-04-26_
_Verifier: Claude (gsd-verifier)_
