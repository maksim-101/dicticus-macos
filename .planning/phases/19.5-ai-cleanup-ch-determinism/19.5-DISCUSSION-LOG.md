# Phase 19.5: AI Cleanup CH-Determinism — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents. Decisions are captured in `19.5-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 19.5-ai-cleanup-ch-determinism
**Areas discussed:** Swiss toggle default & macOS UI, Currency anti-flip strategy (B5), Number-formatting micro-rules, B2 hotfix + Helvetism list

---

## Area 1: Swiss toggle default & macOS UI

### Q1.1 — Swiss German toggle default

| Option | Description | Selected |
|--------|-------------|----------|
| Flip to default ON (with carve-out for fresh-only) | Default ON for new installs; existing untouched stay OFF | |
| Keep default OFF, flip mine manually | No behavior change for any user | |
| Default ON globally including existing untouched | Override unset value to ON across the board on first v2.1 launch | ✓ |

**User's choice:** Flip everyone to ON (resolved via the Q1.3 follow-up — recommended option). Reconciles `feedback_swiss_german_default.md` memory + S1 with Phase 19 D-15.

### Q1.2 — macOS Swiss toggle location

| Option | Description | Selected |
|--------|-------------|----------|
| Menu-bar dropdown row, next to AiCleanupInfoView | Lowest-friction; matches existing macOS UX | ✓ |
| Inside HotkeySettingsView | Mixes shortcuts + locale | |
| New dedicated SettingsView (mirror iOS) | Bigger lift; sets up future settings growth | |

**User's choice:** Menu-bar dropdown row.

### Q1.3 — Migration scope follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Flip everyone to ON | Set unset values to true on first launch via idempotent migration flag | ✓ |
| Only fresh installs get ON | Conservative carve-out via `swissDefaultMigrated` tracking | |

**User's choice:** Flip everyone — see D-A3 for the `swissDefaultMigratedV2_1` idempotent migration design.

---

## Area 2: Currency anti-flip strategy (B5)

### Q2.1 — Anti-flip mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-LLM detect + STRICT prompt + post-LLM diff revert | Belt-and-suspenders; covers different failure modes | ✓ |
| Pre-LLM prompt anchor only | Lighter; risks Gemma ignoring the anchor | |
| Post-LLM diff revert only | Cleanest separation; misses model-rephrasing cases | |

**User's choice:** Belt-and-suspenders (D-B1).

### Q2.2 — Anti-flip activation scope

| Option | Description | Selected |
|--------|-------------|----------|
| Only when Swiss toggle ON | Treat as Swiss-output guarantee | |
| Always when AI cleanup runs | Treat as universal correctness bug | |
| Always when language==de (regardless of toggle) | German-specific Gemma bias treatment | ✓ |

**User's choice:** language==de gating (D-B2). Captures the bug at its actual scope (Gemma's German-currency bias) without over-reaching to English.

---

## Area 3: Number-formatting micro-rules

### Q3.1 — Apostrophe glyph

| Option | Description | Selected |
|--------|-------------|----------|
| ASCII straight `'` (1'250) | Matches roadmap row notation, user-typed convention | ✓ |
| U+2019 typographic `’` (1’250) | Apple/CLDR default; visually authentic Swiss style | |
| User preference (default ASCII) | Defer the call; modest extra code | |

**User's choice:** ASCII (D-C1). Implementation: post-process `NumberFormatter` output replacing `\u{2019}` → `'`.

### Q3.2 — Decimal-separator scope

| Option | Description | Selected |
|--------|-------------|----------|
| All numbers → period when Swiss ON | Simpler; matches "always be a point" UAT statement | ✓ |
| Only currency → period; non-currency stays comma | Bundeskanzlei-faithful | |
| All numbers → period only when adjacent to currency | Hybrid; targeted bug fix | |

**User's choice:** All numbers (D-C2). Single pass, single mental model. Supersedes Phase 19 D-20.

---

## Area 4: B2 hotfix + Helvetism list

### Q4.1 — B2 scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into 19.5 plan as task 1 | Likely a known race; bounded scope | ✓ |
| Run /gsd-debug first, then fold conclusions | Safer if cause is more complex | |
| Split into 19.5.1 micro-phase | Cleanest separation; more overhead | |

**User's choice:** Fold (D-D1). Investigation is the planner's task 1; escalate only if root cause is outside our code.

### Q4.2 — Helvetism list governance

| Option | Description | Selected |
|--------|-------------|----------|
| Hardcoded constant in Shared/Models/SwissHelvetisms.swift | Static array, Wikipedia-attributed, zero infra | ✓ |
| User-editable JSON in App Group | Future flexibility; needs loader + UI work | |
| Reuse Custom Dictionary | Pollutes user dictionary with system entries | |

**User's choice:** Hardcoded constant (D-D2).

### Q4.3 — Continue or finalize

| Option | Description | Selected |
|--------|-------------|----------|
| Write CONTEXT.md | All four areas covered | ✓ |
| More questions about Area 4 | Word-list size, attribution format, etc. | |
| Explore additional gray areas | Eval fixtures, B5 metrics, STYLE prompt expansion | |

**User's choice:** Write CONTEXT.md.

---

## Claude's Discretion

Items where the user did not lock specifics — planner/executor decides:
- Exact regex patterns for currency detection (D-B1a)
- Positional best-match revert algorithm internals (D-B1c)
- Whether anti-flip code lives in new `CurrencyAntiFlip.swift`, extends `ITNUtility`, or attaches to `CleanupPrompt`/`CleanupService`
- Wording of the STRICT prompt anchor (D-B1b)
- Exact ~30 Helvetisms picked from the Wikipedia list (D-D2)
- Optional debug-only override for re-triggering the migration (D-A3)
- Copy for the macOS Swiss-toggle row (D-A2)

## Deferred Ideas

See `19.5-CONTEXT.md` `<deferred>` section. Notable: Schwyzerdütsch fine-tuning, user-editable Helvetism list, Bundeskanzlei STYLE expansion, hidden apostrophe-glyph preference, geolocation auto-detect (rejected), Shared/CleanupService consolidation, separate /gsd-debug for B2, full Swiss-output eval harness.
