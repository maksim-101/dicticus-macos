---
phase: 2.1
slug: asr-engine-swap-whisperkit-to-fluidaudio-parakeet-tdt-v3
status: active
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-16
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | XCTest (Xcode built-in) |
| **Config file** | `Dicticus/DicticusTests/` directory |
| **Quick run command** | `xcodebuild test -scheme Dicticus -destination 'platform=macOS' -only-testing DicticusTests` |
| **Full suite command** | `xcodebuild test -scheme Dicticus -destination 'platform=macOS'` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick test command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.1-01-T1 | 01 | 1 | INFRA-01, INFRA-03 | T-02.1-01, T-02.1-03 | Model download via HTTPS; 600s timeout watchdog | file-content | `cd Dicticus && grep -c "WhisperKit" project.yml && echo "FAIL" \|\| echo "PASS"; grep -q "import FluidAudio" Dicticus/Services/ModelWarmupService.swift && echo "PASS" \|\| echo "FAIL"; grep -q "AsrModels.downloadAndLoad" Dicticus/Services/ModelWarmupService.swift && echo "PASS" \|\| echo "FAIL"` | ✅ | ⬜ pending |
| 02.1-01-T2 | 01 | 1 | INFRA-01 | — | N/A | file-content | `cd Dicticus && grep -c "WhisperKit\|Whisper\|whisperKit\|whisper" DicticusTests/ModelWarmupServiceTests.swift && echo "FAIL" \|\| echo "PASS"; grep -c "func test" DicticusTests/ModelWarmupServiceTests.swift \| grep -q "12" && echo "PASS" \|\| echo "CHECK"` | ✅ | ⬜ pending |
| 02.1-02-T1 | 02 | 2 | TRNS-02, TRNS-03, TRNS-04 | T-02.1-04, T-02.1-05, T-02.1-06, T-02.1-07 | Audio samples cleared per recording; AVAudioConverter resampling; startRecording() throws on failure; NLLanguageRecognizer on-device | file-content + build | `cd Dicticus && grep -rn "WhisperKit\|whisperKit\|import WhisperKit" Dicticus/ && echo "FAIL" \|\| echo "PASS"; grep -q "AVAudioConverter" Dicticus/Services/TranscriptionService.swift && echo "PASS" \|\| echo "FAIL"; xcodebuild build -scheme Dicticus -destination 'platform=macOS' 2>&1 \| tail -5` | ✅ | ⬜ pending |
| 02.1-02-T2 | 02 | 2 | TRNS-03, TRNS-04 | — | N/A | unit + integration | `cd Dicticus && xcodebuild test -scheme Dicticus -destination 'platform=macOS' 2>&1 \| grep -E "Test Suite.*passed\|Test Suite.*failed\|Executed.*test" \| tail -5` | ✅ | ⬜ pending |

*Status: ⬜ pending -- ✅ green -- ❌ red -- ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Adapt `DicticusTests/ModelWarmupServiceTests.swift` — Plan 01 Task 2 updates for FluidAudio API (file-content checks; test execution deferred to Plan 02)
- [x] Adapt `DicticusTests/TranscriptionServiceTests.swift` — Plan 02 Task 2 updates for FluidAudio API + adds 4 new tests
- [x] Add `testDetectLanguage*` test cases for NLLanguageRecognizer (3 tests in Plan 02 Task 2)
- [x] Add `testVadManagerIntegration` test case for Silero VAD (1 test in Plan 02 Task 2, XCTSkipUnless)
- [x] Add `isFluidAudioAvailable()` helper to replace `isWhisperKitAvailable()` for test skipping

*Existing XCTest infrastructure covers all phase requirements. All Wave 0 items assigned to plan tasks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Push-to-talk transcription produces correct text | D-04 (API preservation) | Requires microphone + spoken audio | Hold hotkey, speak a test phrase in German and English, verify text output |
| CoreML compilation on first launch | D-08 (first-run experience) | Requires clean state (no cached compilation) | Delete model cache, launch app, verify warm-up indicator shows and completes |
| ANE performance (190-210x realtime) | Performance claim | Requires real hardware timing | Run transcription benchmark, measure wall-clock vs audio duration ratio |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready
