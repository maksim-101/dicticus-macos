---
phase: 19.7-macos-hygiene
verified: 2026-04-25T09:30:00Z
status: passed
score: 22/22 must-haves verified
overrides_applied: 0
---

# Phase 19.7: macOS Hygiene Verification Report

**Phase Goal:** Resolve macOS hotkey/permission/icon hygiene issues from v2.1 UAT — M1 (post-onboarding hotkey loss / re-auth UX), M2 (multi-install TCC pollution), M3 (Input Monitoring permission row), D1 (app icon missing in Finder).
**Verified:** 2026-04-25T09:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After running install-local.sh, /Applications/Dicticus.app is the only com.dicticus.macos bundle outside ~/.Trash and TM snapshots | VERIFIED | install-local.sh:172-193 contains verbatim mdfind predicate + 5 exclusion guards (canonical, .Trash, Backups.backupdb, .MobileBackups, /Volumes/com.apple.TimeMachine.localsnapshots); copies to canonical /Applications/Dicticus.app at Step 5 |
| 2 | install-local.sh never destroys data — stale copies are moved to ~/.Trash, never rm -rf | VERIFIED | `grep -E "rm -rf" scripts/install-local.sh` returns no match; mv-to-Trash with timestamped filename Dicticus-stale-$TIMESTAMP-... |
| 3 | uninstall.sh removes /Applications/Dicticus.app, com.dicticus.*.plist, LaunchAtLogin entry without prompting; prompts before deleting Application Support | VERIFIED | uninstall.sh has APP_BUNDLE rm -rf (Step 2), PREFS_GLOB removal (Step 3), LaunchAgents/com.dicticus.app.plist with launchctl unload (Step 3), interactive `read -r -p "Delete this directory? [y/N]"` gating Application Support (Step 4) |
| 4 | Both scripts echo a TCC reminder pointing user to System Settings > Privacy | VERIFIED | install-local.sh final summary echoes "TCC permission entries... cannot be cleaned programmatically... open System Settings > Privacy & Security"; uninstall.sh has same TCC info block listing all 3 panes |
| 5 | build-dmg.sh is unchanged | VERIFIED | `git diff --stat 3c2edf6..HEAD -- scripts/build-dmg.sh` returns empty |
| 6 | README.md has 'Manual uninstall' section listing four canonical paths | VERIFIED | README.md:42 has `## Manual uninstall`; lists /Applications/Dicticus.app, ~/Library/Preferences/com.dicticus.*.plist, ~/Library/Application Support/Dicticus/, ~/Library/LaunchAgents/com.dicticus.app.plist; line 53 calls out Microphone/Accessibility/Input Monitoring TCC panes |
| 7 | PermissionManager exposes @Published inputMonitoringStatus reflecting IOHIDCheckAccess | VERIFIED | PermissionManager.swift:55 declares `@Published var inputMonitoringStatus`; line 99 calls `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)` mapping kIOHIDAccessTypeGranted/Denied/Unknown to .granted/.denied/.pending |
| 8 | PermissionManager.allGranted requires all three permissions; misleading "Input Monitoring is NOT needed" comment is deleted | VERIFIED | line 77 `inputMonitoringStatus == .granted` in allGranted; `grep "Input Monitoring is NOT needed" macOS/` returns empty |
| 9 | MenuBarView shows three permission rows when any are not .granted; hides entire section when all .granted | VERIFIED | MenuBarView.swift:118 wraps VStack in `if !permissionManager.allGranted`; rows are Microphone, Accessibility, Input Monitoring (line 136) — Divider() inside conditional block |
| 10 | Each .denied row shows "Repair in System Settings" button opening per-pane deep-link | VERIFIED | PermissionRow.swift:40 — `Button(status == .denied ? "Repair in System Settings" : "Grant Access")`; `"Open Settings"` no longer appears |
| 11 | Each .pending row shows "Grant Access" button | VERIFIED | PermissionRow.swift:40 — same ternary, .pending branch is "Grant Access" |
| 12 | Tapping Repair on Input Monitoring row opens Privacy_ListenEvent | VERIFIED | SystemSettingsURLs.swift:12 declares `inputMonitoring = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent")`; MenuBarView passes it as the row's settingsURL |
| 13 | HotkeyManager exposes public reregisterAll() that tears down + re-spawns both AsyncStream tasks AND restarts ModifierHotkeyListener | VERIFIED | HotkeyManager.swift:154 `func reregisterAll()` — cancels plainDictationTask + cleanupTask, calls listener.stop()/start(), resets registrationFailed, re-spawns both Tasks |
| 14 | HotkeyManager exposes @Published var registrationFailed: Bool = false set true when AsyncStream ends | VERIFIED | HotkeyManager.swift:67 declares `@Published var registrationFailed: Bool = false`; lines 112, 126, 178, 189 set it true via MainActor.run when stream ends |
| 15 | When AX denied AND hasCompletedOnboarding, MenuBarView shows sticky banner with verbatim copy + Repair button + relaunch hint | VERIFIED | MenuBarView.swift:38-65 — gating expression `(accessibilityStatus == .denied && hasCompletedOnboarding) || hotkeyManager.registrationFailed`; line 44 verbatim "Hotkeys disabled — Accessibility permission missing."; Repair button on line ~49 calls `SystemSettingsURL.open(SystemSettingsURL.accessibility)`; line 54 hint "Relaunch Dicticus after granting." |
| 16 | HotkeySettingsView always shows 'Re-register hotkeys' button row at bottom calling hotkeyManager.reregisterAll() | VERIFIED | HotkeySettingsView.swift:11 declares `@EnvironmentObject var hotkeyManager: HotkeyManager`; line 40 row label "Re-register hotkeys"; line 44 button calls `hotkeyManager.reregisterAll()`; row is unconditional |
| 17 | On launch, app runs mdfind once (NOT in 2s polling); if results > 1, MenuBarView shows warning row with [Show] disclosure and install-local.sh hint | VERIFIED | PermissionManager.swift:166 `func checkMultipleInstalls()` runs `/usr/bin/mdfind` with the verbatim predicate; pollTimer block (line ~161) only calls `checkAll()` — no checkMultipleInstalls; MenuBarView.swift:210 calls it from .onAppear; lines 73, 92 show verbatim copy "Multiple Dicticus copies detected..." and "Run scripts/install-local.sh to consolidate." |
| 18 | Repo root contains assets/icon-master.png at exactly 1024×1024 — single source of truth | VERIFIED | `sips -g pixelWidth -g pixelHeight assets/icon-master.png` → pixelWidth: 1024, pixelHeight: 1024 |
| 19 | scripts/generate-icons.sh reads master, regenerates all 10 macOS appiconset PNGs at sizes Contents.json declares, overwrites iOS AppIcon.png | VERIFIED | generate-icons.sh contains 10 SLOT entries mirroring Contents.json verbatim (icon_16x16.png:16 through icon_512x512@2x.png:1024); Step 4 cp $MASTER → iOS AppIcon.png |
| 20 | All 10 macOS appiconset PNGs exist with exact filenames and pixel sizes Contents.json declares | VERIFIED | Per-PNG sips check: 16/32/32/64/128/256/256/512/512/1024 — all match Contents.json declarations |
| 21 | iOS and macOS share same glyph/shape — both built from assets/icon-master.png | VERIFIED | `cmp -s assets/icon-master.png iOS/Dicticus/Assets.xcassets/AppIcon.appiconset/AppIcon.png` exits 0 (byte-identical); macOS appiconset PNGs all generated from the same master |
| 22 | After running script + clean Release build, /Applications/Dicticus.app shows expected icon in Finder | VERIFIED | User UAT 2026-04-25 explicitly approved D1 resolution: "Finder shows Dicticus icon in /Applications/" after install-local.sh + killall Dock Finder. Documented in 19.7-04-SUMMARY.md frontmatter `d1_resolution: confirmed` |

**Score:** 22/22 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/install-local.sh` | Dev-only consolidator with mdfind + Trash semantics | VERIFIED | Exists, executable (0755), bash -n parses, contains verbatim mdfind predicate, 5 exclusion guards, no rm -rf |
| `scripts/uninstall.sh` | Universal uninstaller with read -r -p prompt | VERIFIED | Exists, executable (0755), bash -n parses, has interactive prompt, TCC info block, LaunchAgent removal |
| `scripts/generate-icons.sh` | Asset pipeline using sips -z fan-out | VERIFIED | Exists, executable (0755), bash -n parses, contains 10 SLOT entries, iOS overwrite step |
| `assets/icon-master.png` | 1024×1024 PNG | VERIFIED | Exists at repo root, exactly 1024×1024, byte-identical to iOS source at copy time |
| `macOS/.../AppIcon.appiconset/icon_*.png` (10 files) | Match Contents.json sizes | VERIFIED | All 10 present at correct pixel dimensions |
| `iOS/.../AppIcon.appiconset/AppIcon.png` | Single 1024×1024 iOS icon | VERIFIED | Exists, 1024×1024, byte-identical to master |
| `macOS/Dicticus/Services/PermissionManager.swift` | 3-permission owner + multi-install probe | VERIFIED | inputMonitoringStatus, IOKit.hid import, IOHIDCheckAccess switch, requestInputMonitoring(), checkMultipleInstalls(), multipleDicticusCopies all present |
| `macOS/Dicticus/Services/HotkeyManager.swift` | reregisterAll + registrationFailed + tracked Tasks + Logger | VERIFIED | All present at expected lines |
| `macOS/Dicticus/Views/PermissionRow.swift` | "Repair in System Settings" label | VERIFIED | Line 40 has verbatim string; "Open Settings" gone |
| `macOS/Dicticus/Views/MenuBarView.swift` | 3-row conditional + banner + multi-copy warning | VERIFIED | All gating expressions, verbatim copy strings, and onAppear mdfind probe present |
| `macOS/Dicticus/Views/HotkeySettingsView.swift` | Always-visible Re-register row | VERIFIED | EnvironmentObject injection + Re-register row + reregisterAll call all present |
| `README.md` | Manual uninstall + Updating the app icon sections | VERIFIED | Both sections present (lines 42 and 55) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| install-local.sh | /Applications/Dicticus.app | filtered mdfind enumeration → mv to ~/.Trash → cp build artefact | WIRED | mdfind predicate verbatim, 5 exclusion guards, mv-to-Trash with timestamps, cp -R from BUILD_APP |
| uninstall.sh | Application Support directory | interactive read -p before rm -rf | WIRED | `read -r -p "Delete this directory? [y/N] " reply` + case match, defaults to NO |
| PermissionManager.swift | IOKit IOHIDCheckAccess | checkAll() switch on kIOHIDAccessType | WIRED | `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)` + 4-branch switch (Granted/Denied/Unknown/default) |
| MenuBarView.swift | PermissionManager.inputMonitoringStatus | third PermissionRow in conditional VStack | WIRED | `permissionManager.inputMonitoringStatus` consumed at line 137 |
| PermissionRow.swift | SystemSettingsURL.inputMonitoring | SystemSettingsURL.open(settingsURL) | WIRED | Row API takes settingsURL; .denied branch in PermissionRow opens it; MenuBarView passes SystemSettingsURL.inputMonitoring |
| HotkeySettingsView.swift | HotkeyManager.reregisterAll() | @EnvironmentObject + Button action | WIRED | EnvironmentObject declared, button calls hotkeyManager.reregisterAll() |
| MenuBarView.swift | SystemSettingsURL.accessibility | Repair button → SystemSettingsURL.open | WIRED | `SystemSettingsURL.open(SystemSettingsURL.accessibility)` in banner Repair button |
| PermissionManager.swift | mdfind | Process launch in checkMultipleInstalls() called once from MenuBarView.onAppear | WIRED | Process spawn with verbatim predicate; called from .onAppear, NOT pollTimer |
| generate-icons.sh | macOS AppIcon.appiconset | sips -z resize loop driven by hardcoded SLOTS | WIRED | 10 SLOT entries × sips -z invocation; outputs to MAC_APPICONSET path |
| generate-icons.sh | iOS AppIcon.png | cp assets/icon-master.png to iOS appiconset | WIRED | `cp "$MASTER" "$IOS_APPICON"` at Step 4 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| MenuBarView (banner) | accessibilityStatus, registrationFailed | PermissionManager.checkAll() (AXIsProcessTrusted), HotkeyManager Tasks | Yes — both write real state from system APIs / AsyncStream end | FLOWING |
| MenuBarView (multi-copy row) | multipleDicticusCopies | mdfind Process at .onAppear | Yes — real Process exec with verbatim predicate, parses to URL list | FLOWING |
| MenuBarView (3-row section) | microphoneStatus, accessibilityStatus, inputMonitoringStatus | PermissionManager.checkAll() — AVAuthorizationStatus, AXIsProcessTrusted, IOHIDCheckAccess | Yes — three system-API reads on 2s polling | FLOWING |
| HotkeySettingsView (Re-register row) | (no state read; action only) | hotkeyManager.reregisterAll() invoked via Button | N/A — write side | FLOWING |
| PermissionRow (Repair button) | status (PermissionStatus) | parent passes status from PermissionManager | Yes — propagated from real system check | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| install-local.sh parses | `bash -n scripts/install-local.sh` | exit 0 | PASS |
| uninstall.sh parses | `bash -n scripts/uninstall.sh` | exit 0 | PASS |
| generate-icons.sh parses | `bash -n scripts/generate-icons.sh` | exit 0 | PASS |
| All scripts executable | `ls -la scripts/*.sh` | all 0755 | PASS |
| macOS Debug build succeeds | `xcodebuild -scheme Dicticus -configuration Debug ... build` (ad-hoc signed) | `** BUILD SUCCEEDED **` | PASS |
| Master icon dimensions | `sips -g pixelWidth -g pixelHeight assets/icon-master.png` | 1024×1024 | PASS |
| All 10 macOS PNG sizes match Contents.json | per-slot sips check loop | 10/10 match | PASS |
| iOS icon byte-identical to master | `cmp -s assets/icon-master.png iOS/.../AppIcon.png` | exit 0 | PASS |
| build-dmg.sh unchanged | `git diff --stat 3c2edf6..HEAD -- scripts/build-dmg.sh` | empty | PASS |
| Wrong "Input Monitoring is NOT needed" comment removed | `grep -r "Input Monitoring is NOT needed" macOS/` | no matches | PASS |
| pollTimer does not call checkMultipleInstalls (launch-only) | `grep -A 8 "pollTimer = Timer.scheduledTimer" PermissionManager.swift` | only checkAll() invoked | PASS |
| All 15 commits present | `git log --oneline` for each | all 15 found | PASS |

### Requirements Coverage

The phase uses decision/UAT-route IDs (M1, M2, M3, D1, D-01..D-17) declared in PLAN frontmatter. These IDs are NOT defined in `.planning/REQUIREMENTS.md` — that file tracks formal v2.0/v2.1 product requirements (INFRA-01, ASR-01, etc.). The ROADMAP.md (line 80) explicitly defines these IDs as "UAT row IDs from `19-UAT-FINDINGS.md`" and "CONTEXT.md decisions" — they are operational hygiene markers for this phase only.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| M1 | 19.7-03 | Hotkey re-authorization flow | SATISFIED | Sticky Repair banner + Re-register button + registrationFailed flag (truths 13-16) |
| M2 | 19.7-01 | Multi-install TCC pollution cleanup | SATISFIED | install-local.sh + uninstall.sh + multi-copy launch probe (truths 1-6, 17) |
| M3 | 19.7-02 | Input Monitoring permission row | SATISFIED | 3rd PermissionRow + IOHIDCheckAccess + corrected allGranted (truths 7-12) |
| D1 | 19.7-04 | App icon canonicalization + Finder regression fix | SATISFIED | icon-master.png + generate-icons.sh + user-confirmed UAT (truths 18-22) |
| D-01 | 19.7-01 | Two new scripts (install + uninstall), DMG unchanged | SATISFIED | Both scripts shipped; build-dmg.sh diff empty |
| D-02 | 19.7-01 | build-dmg.sh stays release-only artifact | SATISFIED | git diff --stat empty |
| D-03 | 19.7-01 | README "Manual uninstall" section | SATISFIED | README.md:42 with 4 canonical paths + TCC note |
| D-04 | 19.7-03 | 2-layer detection (AX trust + KeyboardShortcuts liveness) | SATISFIED | Existing AXIsProcessTrusted polling + new registrationFailed flag set on AsyncStream end |
| D-05 | 19.7-03 | Sticky Repair banner with verbatim copy + deep-link | SATISFIED | All 3 verbatim copy strings present + SystemSettingsURL.accessibility opener |
| D-06 | 19.7-03 | Always-visible "Re-register hotkeys" button | SATISFIED | HotkeySettingsView row unconditional, calls reregisterAll() |
| D-07 | 19.7-03 | Launch-only multi-copy mdfind probe | SATISFIED | checkMultipleInstalls() in PermissionManager, called from .onAppear; pollTimer does NOT call it (grep-confirmed) |
| D-08 | 19.7-02 | Input Monitoring as 3rd permission row, wrong comment corrected | SATISFIED | inputMonitoringStatus + IOHIDCheckAccess + grep "Input Monitoring is NOT needed" returns empty |
| D-09 | 19.7-02 | Per-row Repair label .denied/.pending/.granted | SATISFIED | "Repair in System Settings" / "Grant Access" / no button — confirmed in PermissionRow |
| D-10 | 19.7-02 | SystemSettingsURL.inputMonitoring with Privacy_ListenEvent | SATISFIED | URL constant declared with verbatim x-apple.systempreferences URL |
| D-11 | 19.7-02 | Hide entire permission section when all 3 .granted | SATISFIED | `if !permissionManager.allGranted` wraps the VStack + Divider |
| D-12 | 19.7-02 | Banner + permission rows coexist | SATISFIED | MenuBarView places banner above 3-row section; both can render together |
| D-13 | 19.7-04 | assets/icon-master.png at repo root, 1024² | SATISFIED | Exists at exactly 1024×1024 |
| D-14 | 19.7-04 | scripts/generate-icons.sh fan-out script + committed outputs | SATISFIED | Script exists with 10-slot SLOTS list + iOS overwrite; all generated PNGs committed |
| D-15 | 19.7-04 | Same glyph/shape across iOS and macOS | SATISFIED | Both built from assets/icon-master.png; iOS PNG byte-identical to master |
| D-16 | 19.7-04 | "No icon in Finder" regression fix-forward | SATISFIED | User UAT 2026-04-25 confirmed icon visible in Finder after script + killall Dock Finder |
| D-17 | 19.7-04 | README "Updating the app icon" section | SATISFIED | README.md:55 with workflow + cache-nuke fallback |

**Coverage:** 4/4 UAT row IDs (M1, M2, M3, D1) + 17/17 decision IDs (D-01..D-17) — 21/21 phase-scoped requirement IDs satisfied.

**Note on D-18:** ROADMAP mentions "D-01..D-18" but CONTEXT.md only defines D-01..D-17; D-18 is a sequencing note ("planner is free to override") and is informational only — does not produce a deliverable.

**No orphaned requirements.** All IDs declared in PLAN frontmatters map back to either UAT findings or CONTEXT decisions, and all are satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| MenuBarView.swift | 38-65 | Banner copy hardcoded as "Accessibility permission missing" but fires on `registrationFailed` even when AX is granted | Warning | Documented in 19.7-REVIEW.md as WR-02; misleads user post-sleep race. Acceptable per phase scope (gating expression matches plan verbatim); fix deferred to follow-up. |
| PermissionManager.swift | 166-193 | `Process.waitUntilExit()` on @MainActor from MenuBarView.onAppear | Warning | Documented in 19.7-REVIEW.md as WR-01; can stall popover open if Spotlight is busy. mdfind typically <100ms. Plan shipped per spec. |
| install-local.sh | 74-78 | Trash destination collision possible when stale copies share parent dir basename | Warning | Documented in 19.7-REVIEW.md as WR-03. Low real-world rate; mitigatable. |
| uninstall.sh | 41-45 | `for f in $PREFS_GLOB` relies on glob expanding without nullglob | Info | Documented in 19.7-REVIEW.md as WR-04; existing `[ -e "$f" ]` guard prevents bug today. |
| PermissionRow.swift | 73-84 | `relaunchApp` swallows Process.run() errors and quits anyway | Warning | Documented in 19.7-REVIEW.md as WR-05. Pre-existing helper, not added in this phase. |
| HotkeyManager.swift | 101-127, 170-190 | setup() and reregisterAll() duplicate AsyncStream consumer setup | Info | Documented in 19.7-REVIEW.md as IN-01. Drift risk; cosmetic. |
| HotkeyManager.swift | 112, 126, 178, 189 | Redundant `await MainActor.run` inside @MainActor class | Info | Documented in 19.7-REVIEW.md as IN-02. Visual noise only. |

All anti-patterns are categorized as Warning or Info — none are Blockers. They are tracked in the existing 19.7-REVIEW.md and do not invalidate goal achievement; the plans shipped exactly per spec. Recommended for follow-up cleanup phase.

### Human Verification Required

None. The Phase 19.7 human-verification gate (Plan 04 Task 5) was already approved by the user on 2026-04-25:

> "User UAT: APPROVED — Finder shows Dicticus icon in /Applications/. D1 regression resolved."

All other deliverables (M1, M2, M3) are scripts, code, and UI changes that have been verified programmatically against disk state. The hotkey re-auth banner, multi-copy warning, and Input Monitoring row will surface naturally in v2.1 user testing — no blocking human verification required for goal achievement.

### Gaps Summary

No gaps. All 22 must-have observable truths are verified against the codebase. All 21 phase-scoped requirement IDs (M1, M2, M3, D1, D-01 through D-17) are satisfied. All 12 expected artifacts exist with correct content. All 10 key links are wired. All behavioral spot-checks pass. Build succeeds.

The 4 SUMMARY.md "Self-Check" claims hold against disk reality:
- Plan 01: install-local.sh + uninstall.sh + README section all present + commits 26f3ff6/d690c89/054475c verified
- Plan 02: PermissionManager + PermissionRow + MenuBarView changes all present + commits bffc315/1f0045e/b6dae87 verified
- Plan 03: HotkeyManager + PermissionManager + HotkeySettingsView + MenuBarView changes all present + commits d894091/dd50d5e/0e55d70/f99a1ee verified
- Plan 04: assets/icon-master.png + generate-icons.sh + all 10 macOS PNGs + iOS PNG + README section all present + commits 56432de/6b7a1be/7edc6a1/8d9af55 verified, plus the post-checkpoint hotfix 11b373c (empty STALE_COPIES guard)

The hygiene phase achieves its goal: macOS daily dictation is unblocked.

---

_Verified: 2026-04-25T09:30:00Z_
_Verifier: Claude (gsd-verifier)_
