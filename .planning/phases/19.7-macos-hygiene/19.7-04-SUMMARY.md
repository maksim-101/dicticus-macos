---
phase: 19.7-macos-hygiene
plan: 04
subsystem: assets/icon-pipeline
tags: [icon, asset-pipeline, sips, macOS, iOS, README, D1, D-13, D-14, D-15, D-17]
requires: []
provides:
  - assets/icon-master.png (1024x1024 single source of truth, repo root)
  - scripts/generate-icons.sh (10 macOS slots + iOS AppIcon.png fan-out)
  - macOS appiconset regenerated against canonical master
  - README "Updating the app icon" developer doc
affects:
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset (10 PNGs regenerated)
  - iOS/Dicticus/Assets.xcassets/AppIcon.appiconset (no-op overwrite — master was copied from this artwork)
tech-stack-added: []
patterns-introduced:
  - "Asset pipeline driven by hardcoded SLOTS tuple list mirroring Contents.json"
  - "Generated artefacts committed to git for PR diff visibility (D-14)"
key-files-created:
  - assets/icon-master.png
  - scripts/generate-icons.sh
  - .planning/phases/19.7-macos-hygiene/19.7-04-SUMMARY.md
key-files-modified:
  - README.md
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_16x16.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_16x16@2x.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_32x32.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_32x32@2x.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_128x128.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_128x128@2x.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_256x256.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_256x256@2x.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_512x512.png
  - macOS/Dicticus/Assets.xcassets/AppIcon.appiconset/icon_512x512@2x.png
key-decisions:
  - "Use existing iOS AppIcon.png (1024x1024) as canonical master — no new artwork synthesized (per S3, unified visual identity, and user directive)"
  - "Hardcoded SLOTS tuple list mirroring Contents.json verbatim (no JSON parsing) — simpler, satisfies T-19.7-11 trust-input constraint"
  - "Build with CODE_SIGNING_ALLOWED=NO (ad-hoc) for the local Release build because the worktree environment has no DEVELOPER_TEAM_ID"
metrics:
  duration: ~25min (incl. checkpoint)
  completed: 2026-04-25
status: complete
d1_resolution: confirmed (Finder shows Dicticus icon in /Applications after install-local.sh + killall Dock Finder)
---

# Phase 19.7 Plan 04: macOS App Icon Pipeline Summary

One-liner: Establish `assets/icon-master.png` as the single source of truth and ship `scripts/generate-icons.sh` to fan it out to all 10 macOS appiconset slots + the iOS AppIcon.png; regenerate macOS icons; partial — clean Release build verified, awaiting human Finder UAT.

## What was built

- `assets/icon-master.png` (1024x1024, byte-identical copy of the existing iOS AppIcon.png) — repo-root single source of truth (D-13).
- `scripts/generate-icons.sh` (executable, mirrors `build-dmg.sh` idioms: `set -euo pipefail`, SCRIPT_DIR/REPO_ROOT derivation, `=== Step N: ===` echoes, existence guards, friendly summary). Uses a hardcoded 10-entry SLOTS tuple list `(filename, size_px)` mirroring Contents.json verbatim and feeds each through `sips -z`. Step 4 overwrites iOS `AppIcon.png` from the master.
- Regenerated all 10 macOS appiconset PNGs from the canonical master. iOS AppIcon.png unchanged (no-op overwrite — master was copied from it).
- README `## Updating the app icon` developer section (D-17) with the four-step workflow and the LaunchServices cache-nuke fallback (`killall Dock Finder`, `sudo mdutil -E /`).

## Pixel-size verification

All 10 macOS appiconset PNGs match the sizes declared in `Contents.json`:

| Filename                 | Expected | Actual | Status |
|--------------------------|----------|--------|--------|
| icon_16x16.png           | 16       | 16     | OK     |
| icon_16x16@2x.png        | 32       | 32     | OK     |
| icon_32x32.png           | 32       | 32     | OK     |
| icon_32x32@2x.png        | 64       | 64     | OK     |
| icon_128x128.png         | 128      | 128    | OK     |
| icon_128x128@2x.png      | 256      | 256    | OK     |
| icon_256x256.png         | 256      | 256    | OK     |
| icon_256x256@2x.png      | 512      | 512    | OK     |
| icon_512x512.png         | 512      | 512    | OK     |
| icon_512x512@2x.png      | 1024     | 1024   | OK     |

`assets/icon-master.png`: 1024x1024 (verified) — byte-identical to iOS AppIcon.png at copy time (`cmp -s` returned 0).

## Clean Release build

- `cd macOS && xcodegen generate` → project.xcodeproj regenerated.
- `xcodebuild -scheme Dicticus -configuration Release -derivedDataPath build clean build CODE_SIGN_IDENTITY=- CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO` → `** BUILD SUCCEEDED **`.
- Build artifact: `macOS/build/Build/Products/Release/Dicticus.app` (relative to worktree root: `/Users/mowehr/code/dicticus/.claude/worktrees/agent-a37a117b/macOS/build/Build/Products/Release/Dicticus.app`).
- Compiled `AppIcon.icns` (15.8 KB) is present at `Contents/Resources/AppIcon.icns`.
- `Info.plist` has `CFBundleIconFile = AppIcon` and `CFBundleIconName = AppIcon`.
- Note (deviation Rule 3): Plan command did not include code-signing flags. The worktree has no `DEVELOPER_TEAM_ID` env var set (project.yml uses `CODE_SIGN_STYLE: Manual` + `CODE_SIGN_IDENTITY: "Developer ID Application"`), so the literal command failed with "requires a provisioning profile". Re-ran with ad-hoc signing flags consistent with the README's "ad-hoc signed" claim. This does not affect icon rendering — `AppIcon.icns` is still compiled into the bundle.

## Status: COMPLETE — D1 verified

Task 5 (`checkpoint:human-verify`) resolved 2026-04-25:
1. Orchestrator built fresh Release artifact with ad-hoc signing flags (`CODE_SIGN_IDENTITY=- CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO`) → `** BUILD SUCCEEDED **`.
2. `./scripts/install-local.sh` consolidated copies (0 stale found, prior canonical install moved to `~/.Trash/Dicticus-prev-1777100926.app`) and copied fresh build to `/Applications/Dicticus.app`. Confirmed `Contents/Resources/AppIcon.icns` (15.8 KB) is in the bundle.
3. `killall Dock Finder` forced icon-cache refresh.
4. **User UAT: APPROVED** — Finder shows Dicticus icon in `/Applications/`. D1 regression resolved.

### Hotfix landed during UAT

Running `install-local.sh` against an empty `STALE_COPIES[]` aborted at Step 4 because `for p in "${STALE_COPIES[@]}"` trips `set -u` on empty bash arrays. Fixed in commit `11b373c` (guard with `[ ${#STALE_COPIES[@]} -gt 0 ]`). Touches Plan 19.7-01's script — recorded as a follow-up fix.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Build command needed ad-hoc signing flags**
- **Found during:** Task 5 (clean Release build)
- **Issue:** Literal command `xcodebuild ... clean build` failed with `"Dicticus" requires a provisioning profile` because the worktree has no `DEVELOPER_TEAM_ID` env var; `project.yml` declares `CODE_SIGN_STYLE: Manual` + `CODE_SIGN_IDENTITY: "Developer ID Application"`.
- **Fix:** Re-ran with `CODE_SIGN_IDENTITY=- CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO` (ad-hoc). Matches README's documented "ad-hoc signed" constraint for local Release builds without a Developer ID.
- **Files modified:** none — build flags only.
- **Commit:** none — runtime invocation, not source change.

## Commits (per task)

| Task | Commit  | Description                                                       |
|------|---------|-------------------------------------------------------------------|
| 1    | 56432de | feat(19.7-04): establish assets/icon-master.png as source of truth |
| 2    | 6b7a1be | feat(19.7-04): add scripts/generate-icons.sh asset pipeline        |
| 3    | 7edc6a1 | feat(19.7-04): regenerate macOS appiconset from icon-master.png    |
| 4    | 8d9af55 | docs(19.7-04): document icon update workflow in README             |
| 5    | (none)  | human-verify gate — APPROVED by user 2026-04-25                    |

## Self-Check: PASSED

- assets/icon-master.png: FOUND (1024x1024)
- scripts/generate-icons.sh: FOUND (executable, parses cleanly)
- 10 macOS appiconset PNGs: ALL FOUND with correct pixel sizes
- iOS AppIcon.png: FOUND
- README.md "## Updating the app icon" section: FOUND
- Build artifact macOS/build/Build/Products/Release/Dicticus.app: FOUND with AppIcon.icns compiled in
- Commits 56432de, 6b7a1be, 7edc6a1, 8d9af55: ALL PRESENT in git log
