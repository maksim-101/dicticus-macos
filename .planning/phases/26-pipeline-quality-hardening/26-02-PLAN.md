---
phase: 26-pipeline-quality-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/Utilities/SelfCorrectionResolver.swift
  - macOS/DicticusTests/SelfCorrectionResolverTests.swift
  - iOS/DicticusTests/SelfCorrectionResolverTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "'eigentlich ganz gut, doch' clause is NOT dropped by resolver"
    - "'das Ganze ankurbelt, doch' clause is NOT dropped by resolver"
    - "'dieser Stadt Zürich, oder' clause is NOT dropped by resolver"
    - "Existing 27/27 resolver tests remain green"
    - "Genuine self-corrections using 'doch' or 'oder' as standalone connectors no longer fire (acceptable regression — these connectors cause more harm than good)"
  artifacts:
    - path: "Shared/Utilities/SelfCorrectionResolver.swift"
      provides: "Resolver without doch/oder as standalone connectors"
    - path: "macOS/DicticusTests/SelfCorrectionResolverTests.swift"
      provides: "3 UAT regression fixtures + updated test count"
      contains: "testGermanDochQualificationPreserved"
    - path: "iOS/DicticusTests/SelfCorrectionResolverTests.swift"
      provides: "iOS parity — byte-identical to macOS test file"
  key_links:
    - from: "SelfCorrectionResolver.germanConnectors"
      to: "SelfCorrectionResolver.resolve"
      via: "connector alternation pattern"
      pattern: "germanConnectors"
    - from: "SelfCorrectionResolver.pureCorrectionConnectors"
      to: "SelfCorrectionResolver.resolve"
      via: "pure-correction comma advance"
      pattern: "pureCorrectionConnectors"
---

<objective>
Fix P1 (HIGH) SelfCorrectionResolver dropping German "doch"/"oder" qualification clauses.

Purpose: In 3 UAT records, the resolver misidentified German discourse particles "doch" and "oder" (used as tag questions/confirmations at clause boundaries) as self-correction connectors, dropping the preceding clause and producing grammatically broken output. The fix removes "doch" and "oder" from the connector lists entirely — these are too ambiguous in German to serve as reliable self-correction markers.

Output: SelfCorrectionResolver.swift without "doch"/"oder" connectors, plus 3 UAT regression fixtures locked in cross-platform tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md

<interfaces>
From Shared/Utilities/SelfCorrectionResolver.swift:
```swift
public enum SelfCorrectionResolver {
    public static func resolve(_ text: String, language: String) -> String
}
```

Current germanConnectors (line 248-269):
- Line 257: "doch"          <-- REMOVE
- Line 268: "oder"          <-- REMOVE
- Lines 252-253: "oder vielmehr", "oder besser"  <-- KEEP (these are multi-word connectors where "oder" is part of a legitimate correction phrase)

Current pureCorrectionConnectors (line 295-303):
- Line 299: "doch"          <-- REMOVE (follows from germanConnectors removal)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove doch/oder connectors + add 3 UAT regression fixtures</name>
  <files>Shared/Utilities/SelfCorrectionResolver.swift, macOS/DicticusTests/SelfCorrectionResolverTests.swift, iOS/DicticusTests/SelfCorrectionResolverTests.swift</files>
  <read_first>
    - Shared/Utilities/SelfCorrectionResolver.swift (full file — 388 lines; focus on germanConnectors lines 248-269, pureCorrectionConnectors lines 295-303)
    - macOS/DicticusTests/SelfCorrectionResolverTests.swift (full file — 362 lines; 27 existing tests, understand MARK sections for placement)
    - .planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md (lines 53-66 for P1 affected records 102, 117, 132)
  </read_first>
  <behavior>
    New regression tests (from UAT records):
    - Record 102: "wir sind in dieser Stadt Zürich, oder wäre das auch schon einschränkend" -> input preserved unchanged (resolver must NOT fire on ", oder")
    - Record 117: "gefällt mir eigentlich ganz gut, doch dieser eine Teilsatz" -> input preserved unchanged (resolver must NOT fire on ", doch")
    - Record 132: "das Ganze ankurbelt, doch wenn noch tun" -> input preserved unchanged

    Existing tests that must still pass:
    - All 27 current tests remain green
    - "oder vielmehr" and "oder besser" multi-word connectors still work (they are kept; only standalone "oder" is removed)
    - testGermanOderVielmehr: "Es war Montag, oder vielmehr Dienstag." -> "Es war Dienstag." (still works)
    - testGermanOderBesser: "Drei Stück, oder besser fünf Stück." -> "fünf Stück." (still works)
  </behavior>
  <action>
    STEP 1 — Write 3 regression-net test methods in macOS/DicticusTests/SelfCorrectionResolverTests.swift under a new MARK section "Phase 26 UAT regressions — doch/oder false positives". Each test calls SelfCorrectionResolver.resolve with the exact text from UAT records 102, 117, 132 and asserts XCTAssertEqual to the full input string (unchanged).

    STEP 2 — Run macOS tests. The 3 new tests MUST FAIL (RED) because "doch" and "oder" are still in germanConnectors.

    STEP 3 — Edit SelfCorrectionResolver.swift:
    a) Remove "doch" from germanConnectors array (line 257).
    b) Remove "oder" from germanConnectors array (line 268). Keep "oder vielmehr" (line 252) and "oder besser" (line 253) — these are multi-word connectors where "oder" serves a legitimate correction role.
    c) Remove "doch" from pureCorrectionConnectors set (line 299).

    STEP 4 — Run macOS tests again. All 27 existing + 3 new = 30 tests MUST pass (GREEN).

    STEP 5 — Copy macOS test file byte-for-byte to iOS/DicticusTests/SelfCorrectionResolverTests.swift. Verify with diff -q.

    IMPORTANT: "oder vielmehr" and "oder besser" MUST remain in germanConnectors. The regex pattern sorts connectors longest-first, so "oder vielmehr" and "oder besser" will match before any standalone "oder" would have. Removing standalone "oder" does not affect multi-word connectors containing "oder".
  </action>
  <verify>
    <automated>xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/SelfCorrectionResolverTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SelfCorrectionResolver.swift germanConnectors array does NOT contain standalone "doch" or standalone "oder"
    - SelfCorrectionResolver.swift germanConnectors array DOES contain "oder vielmehr" and "oder besser"
    - SelfCorrectionResolver.swift pureCorrectionConnectors set does NOT contain "doch"
    - grep -c '"doch"' Shared/Utilities/SelfCorrectionResolver.swift returns 0
    - grep -c '"oder",' Shared/Utilities/SelfCorrectionResolver.swift returns 0 (the standalone entry with trailing comma)
    - macOS SelfCorrectionResolverTests: 30 tests pass (27 existing + 3 new)
    - iOS SelfCorrectionResolverTests.swift is byte-identical to macOS version
    - SelfCorrectionResolver.resolve("wir sind in dieser Stadt Zürich, oder wäre das auch schon einschränkend", language: "de") returns the input unchanged
    - SelfCorrectionResolver.resolve("Es war Montag, oder vielmehr Dienstag.", language: "de") returns "Es war Dienstag." (multi-word connector still works)
  </acceptance_criteria>
  <done>P1 doch/oder false positives eliminated, 3 UAT regression fixtures locked, all 30 tests green on macOS, iOS test file byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASR -> Resolver | Untrusted ASR text enters SelfCorrectionResolver; must not drop legitimate content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | SelfCorrectionResolver content drops | mitigate | Remove ambiguous connectors ("doch", "oder") that cause false-positive content deletion; regression tests lock each UAT record |
</threat_model>

<verification>
xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/SelfCorrectionResolverTests CODE_SIGNING_ALLOWED=NO
diff -q macOS/DicticusTests/SelfCorrectionResolverTests.swift iOS/DicticusTests/SelfCorrectionResolverTests.swift
</verification>

<success_criteria>
- All 30 resolver tests green (27 existing + 3 new UAT regression fixtures)
- "doch" and "oder" no longer in connector lists (standalone forms)
- "oder vielmehr" and "oder besser" still functional
- 3 UAT records (102, 117, 132) pass through resolver unchanged
- iOS parity verified
</success_criteria>

<output>
Create `.planning/phases/26-pipeline-quality-hardening/26-02-SUMMARY.md` when done
</output>
