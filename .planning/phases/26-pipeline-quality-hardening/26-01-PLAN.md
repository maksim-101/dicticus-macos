---
phase: 26-pipeline-quality-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/Utilities/ITNUtility.swift
  - macOS/DicticusTests/ITNUtilityTests.swift
  - iOS/DicticusTests/ITNUtilityTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "'twenty five' converts to 25, not 2005"
    - "'forty one' converts to 41, not 4001"
    - "'twenty five point one dash zero six' converts to '25.1-06'"
    - "'Punkt' between digit groups converts to decimal separator"
    - "'Komma' between digit groups converts to comma separator"
    - "Existing ITN tests (6/6) remain green"
  artifacts:
    - path: "Shared/Utilities/ITNUtility.swift"
      provides: "Fixed English ITN candidate order + numeric structural word post-pass"
      contains: "applyNumericStructuralWords"
    - path: "macOS/DicticusTests/ITNUtilityTests.swift"
      provides: "Regression tests from UAT records + structural word tests"
      contains: "testEnglishTwentyFiveNotConcatenated"
    - path: "iOS/DicticusTests/ITNUtilityTests.swift"
      provides: "iOS parity — byte-identical to macOS test file"
  key_links:
    - from: "ITNUtility.applyEnglishITN"
      to: "NumberFormatter.number(from:)"
      via: "candidates array order"
      pattern: "subTextAllHyphens.*subText"
    - from: "ITNUtility.applyITN"
      to: "applyNumericStructuralWords"
      via: "post-pass after language-specific ITN"
      pattern: "applyNumericStructuralWords"
---

<objective>
Fix P0 (CRITICAL) ITN number concatenation and P3 (MEDIUM) numeric structural word conversion.

Purpose: P0 fixes NSNumberFormatter parsing "twenty five" as 2005 by swapping candidate order so hyphenated form is tried first. P3 adds a post-pass that converts "point"/"dash"/"zero"/"Punkt"/"Komma" to symbols when adjacent to digits, completing the Phase 23 scope absorption.

Output: Fixed ITNUtility.swift with correct candidate ordering and new structural-word post-pass, plus cross-platform regression tests from UAT record 134.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md

<interfaces>
From Shared/Utilities/ITNUtility.swift:
```swift
struct ITNUtility {
    static func applyITN(to text: String, language: String) -> String
    static func applySwissITN(to text: String) -> String
    // Private: applyGermanITN, applyEnglishITN, applyRangeHomophoneFix
}
```

Current candidate order at line 327:
```swift
let candidates = [subText.lowercased(), subTextAllHyphens.lowercased()] + (subTextMixed.map { [$0.lowercased()] } ?? [])
```
This tries space-separated FIRST. NSNumberFormatter parses "twenty five" (space) as 2005 (concatenating 20+05). Hyphenated "twenty-five" parses correctly as 25 but is never reached because the space form "succeeds" first.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ITN candidate order + add structural word post-pass</name>
  <files>Shared/Utilities/ITNUtility.swift, macOS/DicticusTests/ITNUtilityTests.swift, iOS/DicticusTests/ITNUtilityTests.swift</files>
  <read_first>
    - Shared/Utilities/ITNUtility.swift (full file — 357 lines; focus on applyEnglishITN lines 274-356 for P0, and applyITN lines 7-15 for P3 post-pass insertion point)
    - macOS/DicticusTests/ITNUtilityTests.swift (full file — 41 lines; existing 6 tests)
    - .planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md (lines 36-51 for P0 record 134 details, lines 126-131 for P3 scope)
  </read_first>
  <behavior>
    P0 regression tests (from UAT record 134 and systemic pattern):
    - "twenty five" in English ITN produces "25", not "2005"
    - "forty one" in English ITN produces "41", not "4001"
    - "thirty seven" in English ITN produces "37", not "3007"
    - "one hundred twenty three" still produces "123" (existing test preserved)
    - "forty-two" hyphenated still produces "42" (existing test preserved)

    P3 structural word tests:
    - "25 point 1 dash 06" produces "25.1-06" (English)
    - "Version 25 point 1" produces "Version 25.1" (English)
    - "25 Punkt 1" produces "25.1" (German)
    - "25 Komma 5" produces "25,5" (German)
    - "zero 6" after a digit-dash produces "06" (English)
    - "the point is clear" does NOT convert "point" to "." (no adjacent digits)
    - "on the other hand" does NOT match anything (no structural words between digits)
    - "Punkt eins" without preceding digit does NOT convert (guard: left side must be digit)
  </behavior>
  <action>
    P0 FIX — In applyEnglishITN, at the candidates array construction (line 327), swap the order so subTextAllHyphens comes BEFORE subText. The new order: [subTextAllHyphens.lowercased(), subText.lowercased()] + mixed. This ensures NSNumberFormatter tries "twenty-five" (correct: 25) before "twenty five" (wrong: 2005). The mixed format candidate stays last.

    P3 ADDITION — Add a new static method applyNumericStructuralWords(to:language:) -> String. Wire it as the LAST transform in applyITN, after applyRangeHomophoneFix. The method uses regex replacements:

    English structural words (case-insensitive):
    - Pattern: (\d+)\s+point\s+(\d+) -> $1.$2 (decimal point)
    - Pattern: (\d+)\s+dash\s+(\d+) -> $1-$2 (hyphen/version separator)
    - Pattern: (\d+)\s+zero\s+(\d+) -> $1 followed by "0" then $2 (zero-prefix, e.g. "25 zero 6" -> "2506" — actually this should be: the word "zero" between digits becomes "0", so "1 dash zero 6" -> "1-06" after dash pass then zero pass)
    - Standalone zero adjacent to digit: (\d+[\.\-])\s*zero\s+(\d+) -> $10$2

    German structural words:
    - Pattern: (\d+)\s+Punkt\s+(\d+) -> $1.$2 (decimal)
    - Pattern: (\d+)\s+Komma\s+(\d+) -> $1,$2 (German decimal comma)

    Guard against false positives: structural words only match when BOTH sides are digit tokens (\d+). The word "point" in "the point is clear" has no preceding digit, so it never fires.

    Process order matters: run dash conversion first, then point/Punkt/Komma, then zero. This way "25 point 1 dash zero 6" becomes "25 point 1 dash 06" (zero pass) -> "25.1 dash 06" (point pass) -> hmm, need to think about ordering. Actually: run all three in a single pass or run dash first, then point, then zero-collapse.

    Better approach: run in this order:
    1. "zero" between/after digit-separator -> "0" (so "dash zero 6" -> "dash 06")
    2. "point"/"Punkt" between digits -> "." (so "25 point 1" -> "25.1")
    3. "Komma" between digits -> "," (so "25 Komma 5" -> "25,5")
    4. "dash" between digits -> "-" (so "25.1 dash 06" -> "25.1-06")

    Write all regression tests in macOS/DicticusTests/ITNUtilityTests.swift. After macOS tests pass, copy the file byte-for-byte to iOS/DicticusTests/ITNUtilityTests.swift.

    Existing 6 tests MUST remain green. The test for "one hundred twenty three" -> 123 already passes because the mixed-format candidate handles it. The test for "forty-two" already has a hyphen, so it is unaffected by candidate reordering.
  </action>
  <verify>
    <automated>xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/ITNUtilityTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ITNUtility.swift line ~327: candidates array starts with subTextAllHyphens.lowercased(), then subText.lowercased(), then mixed
    - ITNUtility.swift contains a new static func applyNumericStructuralWords(to:language:) -> String
    - applyITN calls applyNumericStructuralWords after applyRangeHomophoneFix
    - macOS ITNUtilityTests: all existing 6 tests pass + at least 8 new tests (3 P0 + 5+ P3)
    - iOS ITNUtilityTests.swift is byte-identical to macOS version (verified by diff -q)
    - ITNUtility.applyITN(to: "I have twenty five apples", language: "en") returns "I have 25 apples"
    - ITNUtility.applyITN(to: "twenty five point one dash zero six", language: "en") contains "25.1-06"
    - ITNUtility.applyITN(to: "the point is clear", language: "en") returns "the point is clear" (no false positive)
  </acceptance_criteria>
  <done>P0 number concatenation bug fixed (25 not 2005), P3 structural words converted in numeric contexts, all existing + new tests green on macOS, iOS test file is byte-identical copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASR -> ITN | Untrusted ASR text enters ITNUtility; malformed input must not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | ITNUtility structural word regex | mitigate | Guard patterns require \d+ on both sides of structural words; no match without adjacent digits |
| T-26-02 | Denial of Service | ITNUtility regex backtracking | accept | Input is short ASR text (<500 chars typically); regex patterns are simple alternations with no nested quantifiers |
</threat_model>

<verification>
xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/ITNUtilityTests CODE_SIGNING_ALLOWED=NO
diff -q macOS/DicticusTests/ITNUtilityTests.swift iOS/DicticusTests/ITNUtilityTests.swift
</verification>

<success_criteria>
- "twenty five" -> "25" (not 2005) in English ITN
- "forty one" -> "41" (not 4001) in English ITN
- "25 point 1 dash zero six" -> "25.1-06" after full ITN pipeline
- "25 Punkt 1" -> "25.1" in German ITN
- "25 Komma 5" -> "25,5" in German ITN
- "the point is clear" unchanged (false positive guard)
- All existing 6 ITN tests green
- iOS test file byte-identical to macOS
</success_criteria>

<output>
Create `.planning/phases/26-pipeline-quality-hardening/26-01-SUMMARY.md` when done
</output>
