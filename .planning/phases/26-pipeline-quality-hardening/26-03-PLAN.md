---
phase: 26-pipeline-quality-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/Services/DictionaryService.swift
  - macOS/DicticusTests/DictionaryServiceTests.swift
  - iOS/DicticusTests/DictionaryServiceTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "'versus' passes through dictionary unchanged (no Vercel replacement)"
    - "Existing dictionary tests remain green"
    - "'Versal' is retired and purged on launch"
    - "Legitimate 'Vercel' dictation still works via remaining entries (e.g. 'Vercel' exact match)"
  artifacts:
    - path: "Shared/Services/DictionaryService.swift"
      provides: "Retired Versal entry + Vercel exact-match-only entry"
      contains: "Versal"
    - path: "macOS/DicticusTests/DictionaryServiceTests.swift"
      provides: "Regression test for versus false positive"
      contains: "testPhase26_VersusNotReplacedWithVercel"
    - path: "iOS/DicticusTests/DictionaryServiceTests.swift"
      provides: "iOS parity — byte-identical to macOS test file"
  key_links:
    - from: "DictionaryService.purgeRetiredDefaults"
      to: "DictionaryService.dictionary"
      via: "retired key removal on launch"
      pattern: "Versal"
    - from: "DictionaryService.applyFuzzyPass"
      to: "DictionaryService.fuzzyReplaceToken"
      via: "Levenshtein distance check"
      pattern: "LevenshteinDistance.distance"
---

<objective>
Fix P2 (HIGH) dictionary "versus" -> "Vercel" false positive.

Purpose: The dictionary entry "Versal" -> "Vercel" (line 95 of DictionaryService.swift) is being fuzzy-matched against the common English word "versus" at Levenshtein distance 2 (both 6 chars). This replaces every spoken "versus" with "Vercel". The fix retires the "Versal" key (moving it to purgeRetiredDefaults) and replaces it with a "Vercel" -> "Vercel" identity entry that only fires on the exact brand name. The "Zyria" -> "ZueriA" and "Acara" -> "Aqara" entries on the same line are unaffected.

Output: DictionaryService.swift with retired "Versal" entry, new "Vercel" exact-match-only entry, and cross-platform regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md

<interfaces>
From Shared/Services/DictionaryService.swift:
```swift
@MainActor
class DictionaryService: ObservableObject {
    func apply(to text: String) -> String       // exact-match pass + fuzzy pass
    private func applyFuzzyPass(_ text: String) -> String
    private func fuzzyReplaceToken(_ token: String, candidates: [String]) -> String
    private func purgeRetiredDefaults()          // removes keys listed in `retired` array
    private func prepopulateWithDefaults()       // seeds default dictionary entries
}
```

Current problematic entry (line 95):
```swift
"Versal": "Vercel"
```

Fuzzy match mechanism (applyFuzzyPass): for tokens >= 6 chars, checks dictionary keys >= 6 chars where Levenshtein distance <= 2. "versus" (6 chars) vs "Versal" (6 chars) = distance 2 -> matches -> replaced with "Vercel".

purgeRetiredDefaults (line 58-71): removes listed keys on every launch. Currently retires the "1m"/"I m" cluster.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire Versal entry + add Vercel exact-match + regression test</name>
  <files>Shared/Services/DictionaryService.swift, macOS/DicticusTests/DictionaryServiceTests.swift, iOS/DicticusTests/DictionaryServiceTests.swift</files>
  <read_first>
    - Shared/Services/DictionaryService.swift (full file — 268 lines; focus on prepopulateWithDefaults line 86-141 for the "Versal" entry, purgeRetiredDefaults lines 58-71 for the retirement pattern)
    - macOS/DicticusTests/DictionaryServiceTests.swift (full file — 211 lines; understand setUp pattern and test structure)
    - .planning/phases/25.1-ai-cleanup-quality-v3b-paper-driven-remediation-telemetry-pa/v19c-corrected-analysis-may19-22.md (lines 72-74 for P2 record 37 details)
  </read_first>
  <behavior>
    - "the approach of A versus B" passes through dictionary apply() unchanged — "versus" is NOT replaced with "Vercel"
    - "deploy to Vercel" passes through dictionary apply() unchanged — "Vercel" is already correct
    - "versal" as spoken input no longer gets dictionary replacement (retired)
    - Existing DictionaryServiceTests, DictionaryServiceClassBTests, DictionaryServiceFuzzyMatchTests all pass
  </behavior>
  <action>
    STEP 1 — Write a regression test in macOS/DicticusTests/DictionaryServiceTests.swift. Add a new MARK section "Phase 26 UAT regressions" in the DictionaryServiceClassBTests class (or a new test class). The test seeds the dictionary with the same defaults pattern used by other Class B tests, including "Versal" -> "Vercel", then asserts that apply(to: "the approach of A versus B is clear") returns the input UNCHANGED. This test MUST FAIL initially because the fuzzy pass matches "versus" to "Versal".

    STEP 2 — Edit DictionaryService.swift:
    a) In purgeRetiredDefaults(), add "Versal" to the retired array. This removes the key from any existing user install on next launch.
    b) In prepopulateWithDefaults(), remove the "Versal": "Vercel" entry from the defaults dictionary.
    c) In prepopulateWithDefaults(), add "vercel": "Vercel" as a new entry (lowercase key for case-insensitive exact match). This handles the case where ASR outputs "vercel" in lowercase. The exact-match regex pass (case-insensitive by default) will match "vercel"/"Vercel"/"VERCEL" and normalize to "Vercel". The key "vercel" at 6 chars will NOT fuzzy-match "versus" because the exact-match pass handles it first, and in the fuzzy pass, identity matches (lowered == keyLowered) are explicitly skipped at line 261.

    STEP 3 — Run macOS tests. The new regression test MUST now pass (GREEN) because "Versal" is retired so it is no longer in the dictionary, and "versus" does not fuzzy-match against "vercel" (distance 3).

    STEP 4 — Copy macOS test file byte-for-byte to iOS/DicticusTests/DictionaryServiceTests.swift. Verify with diff -q.
  </action>
  <verify>
    <automated>xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/DictionaryServiceTests -only-testing:DicticusTests/DictionaryServiceClassBTests -only-testing:DicticusTests/DictionaryServiceFuzzyMatchTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DictionaryService.swift purgeRetiredDefaults() retired array contains "Versal"
    - DictionaryService.swift prepopulateWithDefaults() does NOT contain "Versal" key
    - DictionaryService.swift prepopulateWithDefaults() contains "vercel": "Vercel" (lowercase key)
    - grep -c '"Versal"' Shared/Services/DictionaryService.swift returns 2 (one in retired array, one in the 2026-05-06 comment or similar context — NOT in the defaults dict)
    - macOS DictionaryService tests: all existing tests pass + 1 new regression test passes
    - DictionaryService.shared.apply(to: "the approach of A versus B") does NOT contain "Vercel"
    - iOS DictionaryServiceTests.swift is byte-identical to macOS version
  </acceptance_criteria>
  <done>P2 "versus" -> "Vercel" false positive eliminated, "Versal" retired from dictionary, "vercel" exact-match entry added, regression test locked, iOS parity verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASR -> Dictionary | Untrusted ASR text enters DictionaryService; false-positive matches corrupt user output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | DictionaryService fuzzy match | mitigate | Retire "Versal" key that fuzzy-matches "versus"; replace with exact "vercel" key that has Levenshtein distance 3 from "versus" (outside fuzzy threshold) |
</threat_model>

<verification>
xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' -only-testing:DicticusTests/DictionaryServiceTests -only-testing:DicticusTests/DictionaryServiceClassBTests -only-testing:DicticusTests/DictionaryServiceFuzzyMatchTests CODE_SIGNING_ALLOWED=NO
diff -q macOS/DicticusTests/DictionaryServiceTests.swift iOS/DicticusTests/DictionaryServiceTests.swift
</verification>

<success_criteria>
- "versus" passes through dictionary unchanged
- "Versal" purged from dictionary on launch
- "vercel" -> "Vercel" exact match works
- All existing dictionary tests green + 1 new regression test
- iOS parity verified
</success_criteria>

<output>
Create `.planning/phases/26-pipeline-quality-hardening/26-03-SUMMARY.md` when done
</output>
