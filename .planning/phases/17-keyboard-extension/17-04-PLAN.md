---
phase: 17-keyboard-extension
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified: [iOS/DicticusWidget/DictationLiveActivity.swift, iOS/Dicticus/Intents/StopDictationIntent.swift, iOS/Dicticus/DictationViewModel.swift, iOS/DicticusKeyboard/KeyboardExtensionView.swift]
autonomous: true
requirements: [ACT-02, KEYB-01]

must_haves:
  truths:
    - "Live Activity (Dynamic Island) shows a Stop button while recording"
    - "Tapping Stop button in Live Activity ends recording and transcribes"
    - "Keyboard extension UI matches system appearance (Light/Dark mode)"
  artifacts:
    - path: "iOS/DicticusWidget/DictationLiveActivity.swift"
      provides: "Interactive Live Activity UI"
    - path: "iOS/Dicticus/Intents/StopDictationIntent.swift"
      provides: "Action handler for the Stop button"
---

<objective>
Enhance the dictation experience with an interactive Stop button in the Live Activity and refine the keyboard extension's visual appearance and cleanup logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-keyboard-extension/17-03-SUMMARY.md
@.planning/phases/17-keyboard-extension/17-CONTEXT.md
@iOS/DicticusWidget/DictationLiveActivity.swift
@iOS/Dicticus/DictationViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Add Stop Button to Live Activity</name>
  <files>iOS/DicticusWidget/DictationLiveActivity.swift, iOS/Dicticus/Intents/StopDictationIntent.swift</files>
  <action>
    1. Update `DictationLiveActivity.swift` (Dynamic Island expanded region) to include a "Stop" button:
       - Use `Button(intent: StopDictationIntent()) { ... }`.
       - Style it with a red circular background and "stop.fill" icon.
    2. In `StopDictationIntent.swift`, implement the `perform()` method:
       - Post the `.stopDictation` notification (already defined in Plan 01).
  </action>
  <verify>
    <automated>grep -q "StopDictationIntent" iOS/DicticusWidget/DictationLiveActivity.swift</automated>
  </verify>
  <done>Live Activity provides a manual override to stop recording.</done>
</task>

<task type="auto">
  <name>Refine Keyboard UI and Cleanup</name>
  <files>iOS/DicticusKeyboard/KeyboardExtensionView.swift, iOS/Dicticus/DictationViewModel.swift</files>
  <action>
    1. Refine `KeyboardExtensionView.swift` styling:
       - Support Light/Dark mode using `@Environment(\.colorScheme)`.
       - Add haptic feedback (UIImpactFeedbackGenerator) to key taps.
       - Ensure keys have proper "Liquid Glass" styling if applicable for iOS 26 context (using standard materials/blur).
    2. In `DictationViewModel.swift`, ensure that if the app is closed/crashed during recording, `kbSource` and `pendingDictation` flags are eventually cleared or handled gracefully.
  </action>
  <verify>
    <automated>grep -q "colorScheme" iOS/DicticusKeyboard/KeyboardExtensionView.swift</automated>
  </verify>
  <done>Keyboard UI is polished and accessible in all system themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Live Activity → App | App Intent triggers logic in the main app process |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service | Rapid Stop tapping | mitigate | Ensure `stopDictation()` in `DictationViewModel` is idempotent and handles multiple calls gracefully. |
</threat_model>

<verification>
Manual verification: Start recording, verify Live Activity appears in Dynamic Island, tap the Stop button, and verify transcription starts.
</verification>

<success_criteria>
- Stop button works in Live Activity.
- Keyboard extension looks native in both light and dark modes.
- Haptic feedback is present on key taps.
</success_criteria>

<output>
After completion, create `.planning/phases/17-keyboard-extension/17-04-SUMMARY.md`
</output>
