---
phase: 17-keyboard-extension
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified: [iOS/Dicticus/DictationViewModel.swift, iOS/DicticusKeyboard/KeyboardExtensionView.swift, iOS/DicticusKeyboard/KeyboardViewController.swift]
autonomous: true
requirements: [KEYB-01, KEYB-02]

must_haves:
  truths:
    - "Tapping dictate button on keyboard opens Dicticus app"
    - "Dicticus app writes transcription result to shared App Group defaults"
    - "Keyboard extension detects the result and inserts it at the cursor"
  artifacts:
    - path: "iOS/Dicticus/DictationViewModel.swift"
      provides: "Keyboard-aware result delivery"
    - path: "iOS/DicticusKeyboard/KeyboardViewController.swift"
      provides: "Polling logic for transcription results"
---

<objective>
Integrate the dictation bounce flow between the keyboard extension and the main app, enabling auto-insertion of transcribed text at the cursor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-keyboard-extension/17-01-SUMMARY.md
@.planning/phases/17-keyboard-extension/17-02-SUMMARY.md
@.planning/phases/17-keyboard-extension/17-CONTEXT.md
@iOS/Dicticus/DictationViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Update DictationViewModel for Keyboard Results</name>
  <files>iOS/Dicticus/DictationViewModel.swift</files>
  <action>
    Modify `stopDictation()` in `DictationViewModel.swift`:
    1. Check if `kbSource` flag is set in `UserDefaults(suiteName: "group.com.dicticus")`.
    2. If true:
       - Write the final transcribed text to `kbResult` key in shared defaults.
       - Set `kbResultReady` to `true`.
       - Clear the `kbSource` flag.
    3. Ensure existing clipboard copy logic still runs as fallback/standard behavior.
  </action>
  <verify>
    <automated>grep -q "kbResult" iOS/Dicticus/DictationViewModel.swift</automated>
  </verify>
  <done>Main app delivers transcription results to the keyboard via App Group.</done>
</task>

<task type="auto">
  <name>Implement Polling and Insertion in Keyboard Extension</name>
  <files>iOS/DicticusKeyboard/KeyboardViewController.swift, iOS/DicticusKeyboard/KeyboardExtensionView.swift</files>
  <action>
    1. In `KeyboardExtensionView.swift`, implement the Dictate (🎙️) button action:
       - Use `NSExtensionContext.open(_:completionHandler:)` to open `dicticus://dictate?source=keyboard`.
    2. In `KeyboardViewController.swift`, implement a polling mechanism (e.g., `Timer` or `RunLoop` every 0.5s):
       - Check `kbResultReady` in shared `UserDefaults`.
       - If `true`:
         - Read `kbResult`.
         - Use `textDocumentProxy.insertText(result)`.
         - Clear `kbResult` and `kbResultReady` in shared defaults.
         - Stop polling.
    3. Start polling immediately after the "Dictate" button is tapped.
  </action>
  <verify>
    <automated>grep -q "insertText" iOS/DicticusKeyboard/KeyboardViewController.swift</automated>
  </verify>
  <done>Keyboard extension auto-inserts text after bounce-back dictation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Extension → App | URL scheme bounce triggers app foregrounding |
| App → Extension | Shared UserDefaults used to pass sensitive transcription text |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information Disclosure | Result persistence | mitigate | Clear `kbResult` from App Group immediately after successful insertion in the keyboard extension. |
</threat_model>

<verification>
Manual verification: Open Notes app, switch to Dicticus keyboard, tap 🎙️, record something in the app, swipe back to Notes, and verify text appears at cursor.
</verification>

<success_criteria>
- Bounce flow works: Keyboard -> App -> Record -> Swipe back -> Auto-insert.
- Result is correctly passed via App Group.
- Polling stops and cleans up after insertion.
</success_criteria>

<output>
After completion, create `.planning/phases/17-keyboard-extension/17-03-SUMMARY.md`
</output>
