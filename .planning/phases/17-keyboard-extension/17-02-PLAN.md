---
phase: 17-keyboard-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [iOS/DicticusKeyboard/KeyboardViewController.swift, iOS/DicticusKeyboard/KeyboardExtensionView.swift]
autonomous: true
requirements: [KEYB-01]

must_haves:
  truths:
    - "Keyboard extension renders a QWERTZ layout"
    - "Tapping keys inserts text into the active text field"
    - "Shift key toggles capitalization"
    - "Backspace key deletes characters"
  artifacts:
    - path: "iOS/DicticusKeyboard/KeyboardViewController.swift"
      provides: "Keyboard entry point and SwiftUI hosting"
    - path: "iOS/DicticusKeyboard/KeyboardExtensionView.swift"
      provides: "SwiftUI-based QWERTZ layout"
---

<objective>
Implement the visual and interactive components of the QWERTZ keyboard layout using SwiftUI within the Keyboard Extension.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-keyboard-extension/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Implement KeyboardViewController with SwiftUI Hosting</name>
  <files>iOS/DicticusKeyboard/KeyboardViewController.swift</files>
  <action>
    Implement `KeyboardViewController` (subclass of `UIInputViewController`).
    1. Set up a `UIHostingController` to embed `KeyboardExtensionView`.
    2. Add the hosting controller's view as a subview and manage its constraints to match the input view's height.
    3. Expose `textDocumentProxy` and `advanceToNextInputMode()` to the SwiftUI view via a shared environment object or closure.
  </action>
  <verify>
    <automated>grep -q "UIHostingController" iOS/DicticusKeyboard/KeyboardViewController.swift</automated>
  </verify>
  <done>KeyboardViewController successfully hosts the SwiftUI view.</done>
</task>

<task type="auto">
  <name>Implement KeyboardExtensionView (QWERTZ Layout)</name>
  <files>iOS/DicticusKeyboard/KeyboardExtensionView.swift</files>
  <action>
    Create `KeyboardExtensionView` in SwiftUI.
    1. Design a QWERTZ layout from scratch (per D-05).
       - Row 1: Q, W, E, R, T, Z, U, I, O, P
       - Row 2: A, S, D, F, G, H, J, K, L
       - Row 3: Shift, Y, X, C, V, B, N, M, Backspace
       - Row 4: 123 (layer toggle), Globe (advance mode), Space, Dictate (🎙️), Return
    2. Handle Shift logic (uppercase/lowercase toggle).
    3. Implement key actions using `textDocumentProxy`:
       - `insertText(_:)` for letters, numbers, and space.
       - `deleteBackward()` for backspace.
       - `advanceToNextInputMode()` for the globe key.
    4. Ensure layout fits common iOS keyboard heights (approx 216pt-260pt).
  </action>
  <verify>
    <automated>grep -q "VStack" iOS/DicticusKeyboard/KeyboardExtensionView.swift</automated>
  </verify>
  <done>QWERTZ keyboard UI is functional for standard typing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Keyboard | Input events captured by the extension |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | Keylogging | mitigate | Dicticus is private and local. No network access in extension (unless RequestsOpenAccess is granted, and even then, we don't log keys). |
</threat_model>

<verification>
Manual verification: Install the keyboard in Settings and verify the QWERTZ layout appears and can type into a text field.
</verification>

<success_criteria>
- QWERTZ layout renders correctly.
- Typing, Shift, Backspace, and Space work as expected.
- Globe key switches keyboards.
</success_criteria>

<output>
After completion, create `.planning/phases/17-keyboard-extension/17-02-SUMMARY.md`
</output>
