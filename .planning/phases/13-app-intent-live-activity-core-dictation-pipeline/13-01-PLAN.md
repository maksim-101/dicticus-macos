---
phase: 13-app-intent-live-activity-core-dictation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iOS/project.yml
  - iOS/Dicticus/Info.plist
  - iOS/Dicticus/Dicticus.entitlements
  - iOS/Dicticus/LiveActivity/DictationActivity.swift
  - iOS/DicticusWidget/DicticusWidgetBundle.swift
  - iOS/DicticusWidget/DictationLiveActivity.swift
  - iOS/DicticusWidget/Info.plist
autonomous: true
requirements: [ACT-02]

must_haves:
  truths:
    - "Widget Extension target builds successfully alongside the main app"
    - "DictationAttributes is accessible from both the Dicticus app target and the DicticusWidget target"
    - "FluidAudio SPM package resolves and compiles for the iOS target"
    - "Info.plist declares NSMicrophoneUsageDescription and NSSupportsLiveActivities"
  artifacts:
    - path: "iOS/DicticusWidget/DicticusWidgetBundle.swift"
      provides: "Widget extension entry point with DictationLiveActivity"
      contains: "@main"
    - path: "iOS/DicticusWidget/DictationLiveActivity.swift"
      provides: "Live Activity UI for Dynamic Island and Lock Screen"
      contains: "ActivityConfiguration"
    - path: "iOS/Dicticus/LiveActivity/DictationActivity.swift"
      provides: "DictationAttributes shared between app and widget"
      contains: "struct DictationAttributes: ActivityAttributes"
  key_links:
    - from: "iOS/DicticusWidget/DictationLiveActivity.swift"
      to: "iOS/Dicticus/LiveActivity/DictationActivity.swift"
      via: "DictationAttributes type usage"
      pattern: "DictationAttributes\\.self"
    - from: "iOS/project.yml"
      to: "iOS/DicticusWidget/"
      via: "XcodeGen target definition"
      pattern: "DicticusWidget"
---

<objective>
Set up the Widget Extension target, DictationAttributes model, Live Activity UI, FluidAudio SPM dependency, and required Info.plist/entitlement entries for the iOS dictation pipeline.

Purpose: The Live Activity must exist before any dictation can start (Apple requirement: Live Activity must start before AVAudioSession). This plan creates the entire ActivityKit infrastructure and project configuration so Plans 02 and 03 can build on a compilable foundation.

Output: Widget Extension target that renders a Live Activity in Dynamic Island and Lock Screen, DictationAttributes shared model, FluidAudio SPM integrated into iOS target, and all required Info.plist keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-RESEARCH.md
@.planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-PATTERNS.md

<interfaces>
<!-- Current iOS project.yml — will be modified -->
From iOS/project.yml:
```yaml
name: Dicticus
options:
  bundleIdPrefix: com.dicticus
  deploymentTarget:
    iOS: "17.0"
settings:
  SWIFT_VERSION: "6.0"
  IPHONEOS_DEPLOYMENT_TARGET: "17.0"
packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift.git
    from: 7.0.0
targets:
  Dicticus:
    type: application
    platform: iOS
    sources: 
      - path: Dicticus
      - path: ../Shared
    entitlements:
      path: Dicticus/Dicticus.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.dicticus
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.dicticus.ios
      INFOPLIST_FILE: Dicticus/Info.plist
      GENERATE_INFOPLIST_FILE: YES
      CODE_SIGN_ENTITLEMENTS: Dicticus/Dicticus.entitlements
      DEVELOPMENT_TEAM: ${DEVELOPER_TEAM_ID}
    dependencies:
      - package: GRDB
        product: GRDB
  DicticusTests:
    type: bundle.unit-test
    platform: iOS
    sources: 
      - path: DicticusTests
    dependencies:
      - target: Dicticus
```

From Shared/Models/TranscriptionResult.swift:
```swift
struct DicticusTranscriptionResult: Sendable {
    let text: String
    let language: String
    let confidence: Float
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DictationAttributes, Widget Extension files, and update project.yml</name>
  <files>
    iOS/Dicticus/LiveActivity/DictationActivity.swift,
    iOS/DicticusWidget/DicticusWidgetBundle.swift,
    iOS/DicticusWidget/DictationLiveActivity.swift,
    iOS/DicticusWidget/Info.plist,
    iOS/project.yml
  </files>
  <read_first>
    iOS/project.yml,
    iOS/Dicticus/Info.plist,
    iOS/Dicticus/Dicticus.entitlements,
    Shared/Models/TranscriptionResult.swift,
    .planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-RESEARCH.md (Pattern 2, Pattern 3, Pattern 7),
    .planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-PATTERNS.md (DictationActivity.swift, DicticusWidgetBundle.swift, DictationLiveActivity.swift, project.yml sections)
  </read_first>
  <action>
1. Create `iOS/Dicticus/LiveActivity/DictationActivity.swift`:
   ```swift
   import ActivityKit
   import Foundation

   struct DictationAttributes: ActivityAttributes {
       public struct ContentState: Codable, Hashable {
           var isRecording: Bool
           var elapsedSeconds: Int
       }
   }
   ```
   This file MUST be added to BOTH the Dicticus and DicticusWidget targets in project.yml (Pitfall 4 in RESEARCH.md — widget extension cannot see main app types).

2. Create directory `iOS/DicticusWidget/` and add three files:

   `iOS/DicticusWidget/DicticusWidgetBundle.swift`:
   ```swift
   import WidgetKit
   import SwiftUI

   @main
   struct DicticusWidgetBundle: WidgetBundle {
       var body: some Widget {
           DictationLiveActivity()
       }
   }
   ```

   `iOS/DicticusWidget/DictationLiveActivity.swift`:
   ```swift
   import WidgetKit
   import SwiftUI

   struct DictationLiveActivity: Widget {
       var body: some WidgetConfiguration {
           ActivityConfiguration(for: DictationAttributes.self) { context in
               HStack {
                   Image(systemName: "mic.fill").foregroundColor(.red)
                   Text(context.state.isRecording ? "Recording\u{2026}" : "Processing\u{2026}")
                   Spacer()
                   Text("\(context.state.elapsedSeconds)s")
                       .monospacedDigit()
               }
               .padding()
           } dynamicIsland: { context in
               DynamicIsland {
                   DynamicIslandExpandedRegion(.leading) {
                       Image(systemName: "mic.fill").foregroundColor(.red)
                   }
                   DynamicIslandExpandedRegion(.trailing) {
                       Text(context.state.isRecording ? "Recording" : "Processing")
                   }
                   DynamicIslandExpandedRegion(.bottom) {
                       Text("Tap to open Dicticus")
                           .font(.caption)
                   }
               } compactLeading: {
                   Image(systemName: "mic.fill").foregroundColor(.red)
               } compactTrailing: {
                   Text("\(context.state.elapsedSeconds)s").monospacedDigit()
               } minimal: {
                   Image(systemName: "mic.fill").foregroundColor(.red)
               }
           }
       }
   }
   ```

   `iOS/DicticusWidget/Info.plist`:
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
   <plist version="1.0">
   <dict>
       <key>NSExtension</key>
       <dict>
           <key>NSExtensionPointIdentifier</key>
           <string>com.apple.widgetkit-extension</string>
       </dict>
   </dict>
   </plist>
   ```

3. Update `iOS/project.yml` with these changes:
   - Add FluidAudio SPM package under `packages:`:
     ```yaml
     FluidAudio:
       url: https://github.com/FluidInference/FluidAudio.git
       from: 0.13.6
     ```
   - Add `FluidAudio` dependency to the `Dicticus` target:
     ```yaml
     dependencies:
       - package: GRDB
         product: GRDB
       - package: FluidAudio
         product: FluidAudio
       - target: DicticusWidget
     ```
   - Add `increased-memory-limit` entitlement to the `Dicticus` target entitlements:
     ```yaml
     entitlements:
       path: Dicticus/Dicticus.entitlements
       properties:
         com.apple.security.application-groups:
           - group.com.dicticus
         com.apple.developer.kernel.increased-memory-limit: true
     ```
   - Add new `DicticusWidget` target:
     ```yaml
     DicticusWidget:
       type: app-extension
       platform: iOS
       sources:
         - path: DicticusWidget
         - path: Dicticus/LiveActivity/DictationActivity.swift
       settings:
         PRODUCT_BUNDLE_IDENTIFIER: com.dicticus.ios.widget
         INFOPLIST_FILE: DicticusWidget/Info.plist
         SKIP_INSTALL: YES
         PRODUCT_NAME: DicticusWidget
         SWIFT_VERSION: "6.0"
         DEVELOPMENT_TEAM: ${DEVELOPER_TEAM_ID}
       info:
         path: DicticusWidget/Info.plist
         properties:
           NSExtension:
             NSExtensionPointIdentifier: com.apple.widgetkit-extension
     ```
   - Ensure `DictationActivity.swift` source path is included in BOTH targets. For the main `Dicticus` target this is automatic (it's under `Dicticus/` which is already a source path). For the `DicticusWidget` target, the explicit `- path: Dicticus/LiveActivity/DictationActivity.swift` entry handles this.

4. Run `cd iOS && xcodegen generate` to regenerate the Xcode project.

5. Run `xcodebuild build -project iOS/Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 17 Pro" | tail -5` to verify the project builds.
  </action>
  <verify>
    <automated>cd /Users/mowehr/code/dicticus/iOS && xcodegen generate --spec project.yml && xcodebuild build -project Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 17 Pro" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - iOS/Dicticus/LiveActivity/DictationActivity.swift contains `struct DictationAttributes: ActivityAttributes`
    - iOS/Dicticus/LiveActivity/DictationActivity.swift contains `var isRecording: Bool`
    - iOS/Dicticus/LiveActivity/DictationActivity.swift contains `var elapsedSeconds: Int`
    - iOS/DicticusWidget/DicticusWidgetBundle.swift contains `@main`
    - iOS/DicticusWidget/DicticusWidgetBundle.swift contains `DictationLiveActivity()`
    - iOS/DicticusWidget/DictationLiveActivity.swift contains `ActivityConfiguration(for: DictationAttributes.self)`
    - iOS/DicticusWidget/DictationLiveActivity.swift contains `DynamicIsland`
    - iOS/project.yml contains `FluidAudio:` under packages
    - iOS/project.yml contains `DicticusWidget:` under targets
    - iOS/project.yml contains `com.apple.developer.kernel.increased-memory-limit: true`
    - iOS/project.yml contains `target: DicticusWidget` in Dicticus dependencies
    - xcodegen generate succeeds
    - xcodebuild build succeeds (exit code 0)
  </acceptance_criteria>
  <done>Widget Extension target builds alongside main app. DictationAttributes is shared between both targets. FluidAudio SPM resolves for iOS. Project generates and compiles cleanly on iOS Simulator.</done>
</task>

<task type="auto">
  <name>Task 2: Update Info.plist and entitlements for microphone, Live Activities, and memory</name>
  <files>
    iOS/Dicticus/Info.plist,
    iOS/Dicticus/Dicticus.entitlements
  </files>
  <read_first>
    iOS/Dicticus/Info.plist,
    iOS/Dicticus/Dicticus.entitlements,
    .planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-RESEARCH.md (Pattern 7 — Info.plist additions, Pitfall 7 — mic permission, Pitfall 8 — memory entitlement)
  </read_first>
  <action>
1. Update `iOS/Dicticus/Info.plist` — add the following keys inside the existing `<dict>` block (before the closing `</dict>`):
   ```xml
   <key>NSMicrophoneUsageDescription</key>
   <string>Dicticus needs microphone access to transcribe your speech. Your audio never leaves this device.</string>
   <key>NSSupportsLiveActivities</key>
   <true/>
   <key>NSSupportsLiveActivitiesFrequentUpdates</key>
   <false/>
   ```

2. Update `iOS/Dicticus/Dicticus.entitlements` — add the `increased-memory-limit` key inside the existing `<dict>`:
   ```xml
   <key>com.apple.developer.kernel.increased-memory-limit</key>
   <true/>
   ```
   The final entitlements plist should contain both `com.apple.security.application-groups` (existing) and `com.apple.developer.kernel.increased-memory-limit` (new).

3. Re-run build to verify Info.plist and entitlements are correctly picked up:
   `cd iOS && xcodebuild build -project Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 17 Pro" 2>&1 | tail -5`
  </action>
  <verify>
    <automated>grep -c "NSMicrophoneUsageDescription" /Users/mowehr/code/dicticus/iOS/Dicticus/Info.plist && grep -c "NSSupportsLiveActivities" /Users/mowehr/code/dicticus/iOS/Dicticus/Info.plist && grep -c "increased-memory-limit" /Users/mowehr/code/dicticus/iOS/Dicticus/Dicticus.entitlements</automated>
  </verify>
  <acceptance_criteria>
    - iOS/Dicticus/Info.plist contains `<key>NSMicrophoneUsageDescription</key>` followed by a `<string>` value mentioning microphone and speech
    - iOS/Dicticus/Info.plist contains `<key>NSSupportsLiveActivities</key>` followed by `<true/>`
    - iOS/Dicticus/Info.plist contains `<key>NSSupportsLiveActivitiesFrequentUpdates</key>` followed by `<false/>`
    - iOS/Dicticus/Dicticus.entitlements contains `<key>com.apple.developer.kernel.increased-memory-limit</key>` followed by `<true/>`
    - iOS/Dicticus/Dicticus.entitlements still contains `<key>com.apple.security.application-groups</key>` (not removed)
    - Build continues to succeed after Info.plist and entitlements changes
  </acceptance_criteria>
  <done>Info.plist declares microphone usage reason, Live Activities support, and no frequent updates. Entitlements include increased-memory-limit for Parakeet CoreML model loading on 4 GB RAM devices. Build still compiles successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget Extension process boundary | DicticusWidget runs in a separate process; only reads ContentState via ActivityKit IPC — no direct access to app memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | DictationAttributes.ContentState | accept | ContentState contains only `isRecording: Bool` and `elapsedSeconds: Int` — no sensitive data (no transcript text, no audio) exposed to the widget extension process |
| T-13-02 | Elevation of Privilege | NSMicrophoneUsageDescription | mitigate | Privacy-preserving description added to Info.plist; iOS permission system gates microphone access; no silent recording possible |
</threat_model>

<verification>
- `xcodegen generate --spec iOS/project.yml` succeeds
- `xcodebuild build -project iOS/Dicticus.xcodeproj -scheme Dicticus -destination "platform=iOS Simulator,name=iPhone 17 Pro"` succeeds
- DicticusWidget target exists in generated .xcodeproj
- `grep "DictationAttributes" iOS/Dicticus/LiveActivity/DictationActivity.swift` returns a match
- `grep "ActivityConfiguration" iOS/DicticusWidget/DictationLiveActivity.swift` returns a match
</verification>

<success_criteria>
- iOS project builds with FluidAudio SPM package resolved
- Widget Extension target compiles and is embedded in the app
- DictationAttributes is shared between app and widget via dual target membership
- Info.plist has mic permission string and Live Activities support
- Entitlements include increased-memory-limit
</success_criteria>

<output>
After completion, create `.planning/phases/13-app-intent-live-activity-core-dictation-pipeline/13-01-SUMMARY.md`
</output>
