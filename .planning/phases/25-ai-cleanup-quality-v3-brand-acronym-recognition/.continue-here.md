---
context: phase
phase: 25-ai-cleanup-quality-v3-brand-acronym-recognition
task: 1
total_tasks: 5
status: paused
last_updated: 2026-05-16T07:03:42.066Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure in this session.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: V16-COMPOSITE must skip H1 (always-on canonical brand injection).** Harness matrix proved H1 causes Dokploy → Docker brand-specificity regression (V16A row, P25-brand-07-doghand: lev 0 → 5). The structural mitigation is that plan 25-03 Task 2 explicitly forbids H1; the harness-side `build_always_on_context` function must NOT be wired into V17 — use `build_targeted_context` (same as V16C/V16D) per `V16_CONTEXT_FN` in `.planning/debug/harness/run.py:1170-1178`.

- [ ] **CONSTRAINT: V17 verification (Task 3) is a hard gate on Task 2 Swift commit.** V16F bundle catastrophe (lev 44 on P25-regress-06-feeding-fitting in matrix §3) proved composite prompts can regress on previously-stable fixtures. Structural mitigation: Task 3's `<behavior>` block explicitly blocks Task 2 commit on any of 4 gate failures; the V17 prompt must be iterated and re-verified until all 4 gates pass (or escalate to Task 5 human checkpoint). Do NOT commit Swift code on a V17 FAIL.

- [ ] **CONSTRAINT: Dictionary expansion + V16 prompt changes are cross-platform.** Per `feedback_cleanup_cross_platform_parity` memory. Every entry added to `Shared/Services/DictionaryService.swift:85-107` automatically ships on iOS (single shared file). New test fixtures in `CleanupPromptTests` and `DictionaryServiceTests` must exist on BOTH `macOS/DicticusTests/` and `iOS/DicticusTests/` — byte-for-byte identical where possible.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Subagent uses Write instead of Edit on plan files | The gsd-planner truncated `25-04-PLAN.md` earlier this session by Write-overwriting it with partial content (recovered via reconstruction). The 25-03 re-plan also fell back to Write because Edit was unavailable in the subagent's tool set — grew the file, no truncation that time, but the risk persists. | blocking | When dispatching the next gsd-planner agent, explicitly verify Edit is available in its tool set BEFORE the spawn, or accept the file will be Write-rewritten and add an automated post-spawn diff-check (wc -l before/after, plus a content-cosine-similarity if available) to catch any silent loss. |
| Misattributing brand fixes to the LLM when the dictionary did them | My initial 218-record analysis claimed "AI cleanup caught 9/10 Claude fixes" — but cross-pipeline-layer audit (commit 4b93acc) showed those were all dictionary substitutions; the LLM passed them through unchanged. I shipped Phase 25's framing on this misattribution. | advisory | Any future "the LLM did X" claim must be checked against the post_dict and post_rules JSONL fields, not just the post_gate final. |
| Audit-only commits with .planning/ gitignored | The original 25-01 commits (1d39d34, 09542b5, 0a45e4f, 2c97738) were audit-only — the harness files live under .planning/ which is gitignored. The 4b93acc commit force-added the matrix.md/tsv/run_h8_h9.py to make them recoverable. | advisory | Phase 25 artifacts under .planning/debug/harness/results/ should be force-added when committing — see how 4b93acc did it. |

<current_state>
Phase 25 Wave 1 (plans 25-01, 25-01-H8/H9 followup, 25-02) is COMPLETE. Wave 2 (plan 25-03) is RE-PLANNED with the new hybrid structure but not yet executed. Wave 3 (plan 25-04) is a 3-day human capture window, separate session.

Working tree: clean except 2 known modified files (STATE.md, ROADMAP.md — orchestrator-owned, expected) and untracked .continue-here + iOS/build/ artifacts. 10 commits ahead of origin.

Branch: feature/debug-recording-and-cleanup.
</current_state>

<completed_work>

Completed plans:
- **25-01** (commit 2c97738): V16 hypothesis matrix — 259 inferences, V15 + V16A-F prompt variants vs 37 phase25_brands.tsv fixtures at seed=42. Initial winner V16-COMPOSITE = H3+H4+H5.
- **25-01 H8/H9 follow-up** (commit 4b93acc, user-mandated rerun): added rules-only (H8) + expanded-dictionary (H9) baselines to matrix. Revealed dictionary does ~90% of brand-fixing work. H9 aggregate 72 (second-best). Matrix.md addendum §1b/§2.8/§2.9/§4/§5 documents the revised recommendation.
- **25-02** (commits 542d0b1 + 19f6994 + f7a6c8f): plain-mode DebugRecorder logging — pivoted to "document existing contract + parity tests" after pre-flight verification showed the write path was already live in production code at outer scope.

Plan revisions:
- **25-03 RE-PLANNED** to 5-task hybrid structure: Task 1 dictionary expansion (highest ROI), Task 2 V16-COMPOSITE prompt (H3+H4 only, skip H1, conditional H5), Task 3 V17 harness verification (hard gate), Task 4 regression-net tests, Task 5 human UAT. File grew from 393 → 631 lines via Write (Edit was unavailable to the planner subagent).
</completed_work>

<remaining_work>

- **25-03 Task 1**: Add ~16 dictionary entries to `Shared/Services/DictionaryService.swift:85-107` + new `DictionaryServiceTests.swift` on macOS + iOS, each test citing a V15 capture-window timestamp.
- **25-03 Task 2**: Adopt V16C domain-topic hint + phase/face few-shot AND V16D number-integrity rule + few-shot in `Shared/Models/CleanupPrompt.swift`. Bump V15 → V16. Skip H1. H5 deferred to Task 3 verdict.
- **25-03 Task 3**: Compose V17 = V16C + V16D in the harness, register in `.planning/debug/harness/run.py`, run vs `fixtures/phase25_brands.tsv` at seed=42, append §6 verification block to `v16_matrix.md`. Four gates (aggregate < 50, brand ≤ 2, regress-06 == 0, brand-01 == 0). Iterate or escalate on FAIL.
- **25-03 Task 4**: Regression-net fixtures locking new V16 contract + V15 wins, parity on both platforms.
- **25-03 Task 5**: Human UAT checkpoint on Debug-Recorder build with V16 + dict + plain-mode logging.
- **25-04**: 3-day capture window v2 + diff + UAT (next-next session, not this one).
</remaining_work>

<decisions_made>

- Plan 25-03 task order flipped: dictionary first (highest ROI, deterministic), prompt second (where the LLM genuinely contributes).
- Skip H1 always-on canonical injection (Dokploy → Docker regression risk).
- H5 is conditional, decided by V17 verification result, not pre-committed.
- TextProcessingService.swift matcher-broadening fix from the original 25-03 was DROPPED — dictionary expansion supersedes it.
- Plain-mode logging: no schema change to `Shared/Diagnostics/DebugRecorder.swift` (Optional fields already at lines 74-76).
</decisions_made>

<blockers>
- **xcodebuild verification of 25-02 deferred**: parallel-build llama-cpp xcframework cache contention. Workaround: clean xcodebuild test as first action in Wave 2.
- **25-03 Task 3 Step C CLI flags are wrong**: `--variant` should be `--variants`, `--fixture-file` should be `--fixtures`, `--append-tsv` doesn't exist. Workaround: either add flags or wrap (see `run_h8_h9.py` for append pattern).
- **10 commits ahead of origin**: not blocking, but the CLAUDE.md push cadence rule says push at GSD-step boundaries. Push at Wave 2 start.
</blockers>

## Required Reading (in order)

1. `.planning/debug/harness/results/v16_matrix.md` (especially the addendum §1b/§2.8/§2.9/§4/§5) — authoritative evidence for the revised 25-03 plan
2. `.planning/phases/25-ai-cleanup-quality-v3-brand-acronym-recognition/25-CONTEXT.md` — locked phase decisions
3. `.planning/phases/25-ai-cleanup-quality-v3-brand-acronym-recognition/25-03-v16-prompt-and-dictionary-feeder-PLAN.md` — the revised plan; verify Task 1's dictionary entry list matches v16_matrix.md §5 Lever 1 verbatim
4. `Shared/Services/DictionaryService.swift:85-107` — insertion point for Task 1
5. `Shared/Models/CleanupPrompt.swift` — V15 source for Task 2's edits (read the existing V5 historical comment block — V16 should match its style)
6. `.planning/debug/harness/run_h8_h9.py` — pattern for an append-aware harness script, useful for Task 3
7. Memories: `project_dictionary_dominates_brand_fixing` (the H8/H9 framing), `project_v15_capture_findings` (original brand framing — partially superseded), `feedback_cleanup_cross_platform_parity`, `feedback_tests_as_regression_nets`

## Critical Anti-Patterns (do NOT repeat these)

- **Always-on canonical brand injection (H1) regressing brand specificity** → Use targeted-substring matching (the `build_targeted_context` builder), expand the DICTIONARY not the prompt context.
- **Misattributing brand fixes to the LLM** → Always check post_dict and post_rules JSONL fields before claiming an LLM contribution.
- **Subagent Write instead of Edit truncating plan files** → Verify Edit availability before spawning planner subagents; check file size before/after.

## Infrastructure State

- **Branch**: `feature/debug-recording-and-cleanup`, 10 commits ahead of `origin/main`.
- **Worktrees**: none (all cleaned up after Wave 1).
- **llama-server**: not running (was used by 25-01 matrix and 25-01-followup; cleaned up after run).
- **Debug-Recorder build**: last installed 2026-05-12 (V15 era). Wave 2 will install V16 build after 25-03 ships.
- **Capture logs**: `~/Library/Application Support/Dicticus/DebugRecordings/cleanup-2026-05-{08..16}.jsonl` (533 records: 527 aiCleanup, 6 plain — all pre-V15 trivial).
- **Harness state**: `.planning/debug/harness/results/v16_matrix.tsv` has 333 rows (9 variants × 37 fixtures). `v16_matrix.md` has §1–§7 (original) + §1b/§2.8/§2.9/§4/§5 addendum (H8/H9).

## Pre-Execution Critique Required

Not applicable — Task 1 is straightforward dictionary expansion with no design ambiguity. Task 2 has Task 3 V17 verification as its critique gate.

<context>
The two big findings of this session — plain-mode logging was already live, and the dictionary does ~90% of brand-fixing — both undercut the original CONTEXT.md framing. The user mandated the H8/H9 rerun specifically to surface dictionary-attribution evidence before any Wave 2 Swift code ships. That evidence reshaped 25-03 from "prompt rewrite + matcher-broaden" into "dictionary expansion + smaller prompt change + hard verification gate." The hybrid is empirically grounded but the V17 verification has not yet been run — Task 3 must pass before Task 2's Swift commits land.

The remaining intellectual risk is the V16F-style composite catastrophe: stacking H3 + H4 worked in isolation (V16C alone, V16D alone) but the bundle was never tested. V17 tests the bundle. If V17 fails, we either iterate (V17b adds H5) or escalate to human UAT for a smaller scope. The plan is designed to fail safely: no Swift code ships on a V17 FAIL.
</context>

<next_action>
1. `/gsd-resume-work` to reload the resume workflow.
2. Confirm and run `git push origin feature/debug-recording-and-cleanup` (10 commits ahead).
3. Clean xcodebuild test of 25-02 plain-mode tests on macOS + iOS (deferred from Wave 1).
4. Read `.planning/debug/harness/results/v16_matrix.md` addendum (§1b/§2.8/§2.9/§4/§5).
5. Spot-read `.planning/phases/25-ai-cleanup-quality-v3-brand-acronym-recognition/25-03-v16-prompt-and-dictionary-feeder-PLAN.md` (Write-reconstructed; 631 lines, no truncation but eyes recommended).
6. `/gsd-execute-phase 25 --wave 2` — launches the 25-03 executor.
</next_action>
