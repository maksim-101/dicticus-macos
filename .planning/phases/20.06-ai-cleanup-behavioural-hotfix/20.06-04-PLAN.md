---
phase: 20.06-ai-cleanup-behavioural-hotfix
plan: 04
type: execute
wave: 3
status: planned
branch: feature/phase-19-ai-cleanup-ios
depends_on: ["20.06-01", "20.06-02", "20.06-03"]
files_modified:
  - .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md   # CREATED — captures the re-run outputs verbatim, per-Configuration
  - .planning/STATE.md                                                              # MODIFIED — only at the end, only if UAT GREEN — flips Phase 20 from "Code Complete (UAT-failed: behavioural)" to "Shipped"
  - .planning/ROADMAP.md                                                            # MODIFIED — only at the end, only if UAT GREEN — flips Phase 20.06 row to ✅ Shipped
autonomous: false
requirements_addressed:
  - F-20-UAT-01   # dialect preservation — re-test
  - F-20-UAT-02   # currency idempotency + speaker-explicit anchor — re-test
  - F-20-UAT-03   # iOS long-press → contextMenu Copy (not link preview) — re-test
  - F-20-UAT-04   # iOS chevron discoverability + NavigationLink push to detail — re-test
  - F-20-UAT-05   # number drift "110.57" → "100.57" — re-test gate (no separate fix; expected to be mitigated by F-20-UAT-01)
must_haves:
  truths:
    - "macOS Configuration C (AI cleanup ON, Swiss toggle ON) produces output where 'auf' stays 'auf', 'ausgeflogen' stays 'ausgeflogen', 'gekostet' stays 'gekostet' — i.e., dialect register preserved."
    - "iOS Configuration B (AI cleanup ON, Swiss toggle ON) produces the same dialect-preserving output."
    - "macOS output of '110.57 €' contains AT MOST one currency token at that position — no 'Euro Euro' duplication."
    - "iOS output of '4.50 Franken' preserves 'Franken' (or canonical 'CHF') — never substitutes 'Euro'."
    - "iOS output of '110.57' preserves '110.57' — no number drift to '100.57' (F-20-UAT-05 re-test gate)."
    - "iOS Configuration A (AI cleanup OFF, Swiss toggle ON) baseline still produces the good rules-only output documented in 20-UAT-FINDINGS.md (no regression to the deterministic path)."
    - "iOS HistoryRow long-press surfaces the .contextMenu Copy action — NOT the system path/link preview."
    - "iOS HistoryRow shows a visible trailing chevron and tapping the row pushes HistoryDetailView."
    - "All UAT outputs captured verbatim in 20.06-04-UAT-RESULTS.md per-Configuration."
    - "If ALL above are GREEN: STATE.md and ROADMAP.md updated to mark Phase 20.06 SHIPPED and Phase 20 closed (parent-phase closure unblocked)."
  artifacts:
    - path: ".planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md"
      provides: "Verbatim UAT outputs for the original Phase 20 test sentence on both platforms × all relevant configurations"
      contains: "Configuration"
    - path: ".planning/STATE.md"
      provides: "Phase 20.06 shipped marker + Phase 20 closure (only on GREEN UAT)"
      contains: "20.06"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 20.06 row marked ✅ Shipped (only on GREEN UAT)"
      contains: "20.06"
  key_links:
    - from: "Plan 20.06-01"
      to: "F-20-UAT-01 + F-20-UAT-05"
      via: "HELVETISMS prompt rework — re-tested by Configurations B + C dialect preservation"
      pattern: "Configuration (B|C)"
    - from: "Plan 20.06-02"
      to: "F-20-UAT-02"
      via: "Idempotent foldCurrencyUnits + speaker-explicit anchor — re-tested by 'Euro Euro' and 'Franken→Euro' checks"
      pattern: "Euro Euro|Franken"
    - from: "Plan 20.06-03"
      to: "F-20-UAT-03 + F-20-UAT-04"
      via: ".contextMenu + chevron — re-tested by manual long-press and tap on iOS"
      pattern: "contextMenu|chevron"
---

<objective>
Final UAT gate for Phase 20.06. The previous three plans deliver the behavioural fixes:
- 20.06-01: HELVETISMS prompt rework (dialect preservation)
- 20.06-02: currency formatter idempotency + speaker-explicit anchor + STRICT prompt
- 20.06-03: iOS HistoryRow .contextMenu + trailing chevron

This plan re-runs the same 2026-04-27 UAT test sentence on both platforms and locks the
result in writing. If all configurations pass, Phase 20 closes (its artifact-level
verification was already 12/12 GREEN — only the behavioural goal was outstanding).

This is the gate for F-20-UAT-05 too: per the UAT findings doc, the number drift
"110.57"→"100.57" was hypothesised to vanish once Gemma stops rewriting words. This plan
verifies that hypothesis empirically — no separate code fix.

Output: 1 UAT results file + (on GREEN) 2 status updates (STATE.md, ROADMAP.md).
No source code changes. This plan is gate-only; the build artefacts are produced by
20.06-01/02/03 commits already on the branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-ai-cleanup-demotion-uat-visibility/20-UAT-FINDINGS.md
@.planning/phases/20-ai-cleanup-demotion-uat-visibility/20-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<read_first>
- .planning/phases/20-ai-cleanup-demotion-uat-visibility/20-UAT-FINDINGS.md (the WHOLE file — it contains the test sentence, the per-Configuration baselines, and the failure descriptions to re-validate against).
- .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-01-SUMMARY.md (will exist by the time this plan runs — confirms HELVETISMS rework actually shipped).
- .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-02-SUMMARY.md (will exist; confirms currency fixes actually shipped).
- .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-03-SUMMARY.md (will exist; confirms iOS UX affordances actually shipped).

Branch invariant: stay on `feature/phase-19-ai-cleanup-ios`. Do NOT create or switch branches.

UAT test sentence (German, mixed-currency, mixed-locale — VERBATIM from 20-UAT-FINDINGS.md):
> Also dann versuchen wir es doch mal auf Deutsch. Und zwar war ich am vergangenen Freitag ausgeflogen. Und zwar nach Basel, war da dann auch auf beiden Seiten der Grenze unterwegs, das heisst also auch auf der deutschen und schweizerischen Seite. Und nebst vielen leckeren Dingen, die ich gegessen hatte, war ich natürlich auch einkaufen, ebenfalls auf beiden Seiten. Auf der deutschen Seite habe ich ca. 110.57 € ausgegeben, während ich dann später noch auf der Schweizerischen Seite in der Tankstelle etwas Kleines als Erfrischung gekauft habe. Das hat mich dann ca. 4.50 Franken gekostet.
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build both platforms from current HEAD</name>
  <files>(no source files modified — build verification only)</files>
  <action>
    Confirm the three preceding plans are committed on `feature/phase-19-ai-cleanup-ios` and produce passing builds before requesting human UAT.

    1. Branch + commit verification:
       ```
       cd /Users/mowehr/code/dicticus
       git branch --show-current  # MUST print: feature/phase-19-ai-cleanup-ios
       git log --oneline -10 | grep -E "20\.06-0[123]"  # MUST show commits from all three preceding plans
       ```

    2. macOS build:
       ```
       xcodebuild build -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5
       ```
       Expected: `** BUILD SUCCEEDED **`.

    3. macOS unit tests (regression sweep):
       ```
       xcodebuild test -project macOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=macOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO -only-testing:DicticusTests/CleanupPromptTests -only-testing:DicticusTests/SwissNumberFormatterTests -only-testing:DicticusTests/CurrencyAntiFlipTests -only-testing:DicticusTests/RulesCleanupServiceTests 2>&1 | tail -15
       ```
       Expected: all four classes PASS.

    4. iOS build (best-effort — if pre-existing FluidAudio break still applies, document and proceed):
       ```
       xcodebuild build -project iOS/Dicticus.xcodeproj -scheme Dicticus -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.4' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
       ```
       Expected: `** BUILD SUCCEEDED **`. If the pre-existing FluidAudio API mismatch surfaces, the iOS build for UAT must use the locally-fixed pbxproj per Phase 20-02 SUMMARY guidance — record the workaround in 20.06-04-UAT-RESULTS.md.

    Do NOT commit anything in this task. Build/test only.
  </action>
  <verify>
    <automated>cd /Users/mowehr/code/dicticus && [ "$(git branch --show-current)" = "feature/phase-19-ai-cleanup-ios" ] && git log --oneline -20 | grep -E "20\.06-0[123]" | wc -l | awk '$1 >= 3 { exit 0 } { exit 1 }' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Branch is `feature/phase-19-ai-cleanup-ios`.
    - At least three `20.06-0[123]` commits visible in `git log`.
    - macOS build SUCCEEDED.
    - macOS unit tests for the four affected classes all PASS.
    - iOS build status recorded (SUCCEEDED, or pre-existing FluidAudio break documented).
  </acceptance_criteria>
  <done>
    All three preceding plans confirmed shipped on the correct branch; macOS build + tests green; iOS build status known. Ready to install both apps for human UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — install both apps and dictate the test sentence</name>
  <files>.planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md</files>
  <action>
    **THIS IS A CHECKPOINT TASK** — it requires a human to perform dictation on real devices and capture verbatim outputs. Claude prepares everything, the human runs the test, then Claude resumes at Task 3.

    **What was built (recap of Phase 20.06 deliverables on the branch):**
    - HELVETISMS prompt block (Shared/Models/CleanupPrompt.swift) — preservation-first wording + NEGATIVE list of HG→CH-G traps
    - SwissNumberFormatter.foldCurrencyUnits — idempotency guard
    - CurrencyAntiFlip.speakerExplicitCurrencies — speaker-word anchor against wrong-direction flip
    - CleanupPrompt STRICT block — explicit-currency-wins anchor sentence
    - iOS HistoryRow — .contextMenu Copy + trailing chevron

    **Setup (Claude can run these before yielding to human):**
    1. From repo root, build + install macOS Release into `/Applications/`:
       ```
       cd /Users/mowehr/code/dicticus
       xcodebuild -project macOS/Dicticus.xcodeproj -scheme Dicticus -configuration Release -destination 'platform=macOS' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO build
       # Then copy the built .app into /Applications/ (script: scripts/install-local.sh if it exists, else manual cp from DerivedData)
       ```
    2. Install iOS build to a real device or iOS 26.4 Simulator with iPhone 17 (or any available iOS 26.x sim). If the device is unavailable, Simulator is acceptable for UAT items 1, 2, 5; long-press / chevron items (3, 4) prefer a real device but Simulator is acceptable.

    **Human-only steps — Claude PAUSES here and waits for resume signal:**

    The human dictates this exact sentence on each platform/configuration:
    > Also dann versuchen wir es doch mal auf Deutsch. Und zwar war ich am vergangenen Freitag ausgeflogen. Und zwar nach Basel, war da dann auch auf beiden Seiten der Grenze unterwegs, das heisst also auch auf der deutschen und schweizerischen Seite. Und nebst vielen leckeren Dingen, die ich gegessen hatte, war ich natürlich auch einkaufen, ebenfalls auf beiden Seiten. Auf der deutschen Seite habe ich ca. 110.57 € ausgegeben, während ich dann später noch auf der Schweizerischen Seite in der Tankstelle etwas Kleines als Erfrischung gekauft habe. Das hat mich dann ca. 4.50 Franken gekostet.

    For each Configuration: copy the resulting transcription verbatim into 20.06-04-UAT-RESULTS.md.

    1. **iOS Configuration A** — AI cleanup OFF, Swiss toggle ON.
       - Expected: matches the "good baseline" from 20-UAT-FINDINGS.md (rules-only output: '110.57 €' and '4.50 Franken' both preserved, High German preserved).

    2. **iOS Configuration B** — AI cleanup ON, Swiss toggle ON. **(THE CRITICAL CASE)**
       - Expected (post-fix):
         - 'auf' stays 'auf' (NOT 'uf').
         - 'ausgeflogen' stays 'ausgeflogen' (NOT 'usgfloge').
         - 'gekostet' stays 'gekostet' (NOT 'choschtet').
         - '110.57' stays '110.57' (NOT '100.57') — F-20-UAT-05 gate.
         - '4.50 Franken' stays 'Franken' (or canonical 'CHF 4.50') — NOT 'Euro'.
         - '110.57 €' has at most ONE currency token (no 'Euro Euro').
         - Light editing (filler removal, punctuation tidy) is still allowed.

    3. **macOS Configuration C** — AI cleanup ON, Swiss toggle ON. **(THE OTHER CRITICAL CASE)**
       - Expected (post-fix): same as Configuration B above.

    4. **iOS HistoryRow — long-press behaviour:**
       - Open the History tab in the iOS app. Long-press one of the rows.
       - Expected: a SwiftUI .contextMenu appears with a "Copy" item.
       - Expected: NO system path/link preview popover.

    5. **iOS HistoryRow — chevron + tap behaviour:**
       - Visual: each row shows a trailing chevron (Image system 'chevron.right').
       - Tap a row: HistoryDetailView pushes onto the navigation stack (the same view shipped in Phase 20.05 with Raw/Polished picker).

    **Capture rule:** Verbatim outputs only. If a Configuration's output diverges from the expected, do NOT edit/sanitize — paste it raw with a ❌ marker so a follow-up phase has the truth to work from.

    Create the results file at `.planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md` using this template:

    ```markdown
    ---
    phase: 20.06-ai-cleanup-behavioural-hotfix
    type: uat-results
    date: <YYYY-MM-DD>
    tester: <name/email>
    branch: feature/phase-19-ai-cleanup-ios
    builds:
      macos: <commit sha + install path>
      ios:   <commit sha + sim device or real device id>
    ---

    # Phase 20.06 UAT Re-run

    Original UAT findings: 20-UAT-FINDINGS.md (2026-04-27)

    ## Test sentence
    [verbatim sentence]

    ## Configuration A — iOS, AI cleanup OFF, Swiss ON
    Output: <verbatim>
    Verdict: ✅ / ❌

    ## Configuration B — iOS, AI cleanup ON, Swiss ON
    Output: <verbatim>
    Verdict: ✅ / ❌
    F-20-UAT-01 (dialect preserved): ✅ / ❌  details: <words observed>
    F-20-UAT-02 (currency preserved): ✅ / ❌  details: <Franken vs Euro at 4.50, Euro Euro at 110.57>
    F-20-UAT-05 (number drift): ✅ / ❌  details: <110.57 vs 100.57>

    ## Configuration C — macOS, AI cleanup ON, Swiss ON
    Output: <verbatim>
    Verdict: ✅ / ❌
    F-20-UAT-01 (dialect preserved): ✅ / ❌  details: <words observed>
    F-20-UAT-02 (currency preserved): ✅ / ❌  details: <Franken vs Euro, Euro Euro>

    ## iOS history-row long-press
    Behaviour observed: <description>
    F-20-UAT-03 (.contextMenu Copy not link preview): ✅ / ❌

    ## iOS history-row chevron + tap
    Chevron visible: ✅ / ❌
    Tap pushes HistoryDetailView: ✅ / ❌
    F-20-UAT-04: ✅ / ❌

    ## Summary
    All 5 findings: <count> ✅ / <count> ❌

    Phase 20 closure unblocked: ✅ / ❌
    ```

    **Resume signal expected from human:**
    - Type "approved" if all 5 findings pass (Claude proceeds to Task 3 status updates).
    - Type "regression" with details if any finding still fails (Phase 20.06 gets a follow-on plan).
    - Type "partial" with details if some pass, some fail (escalate to user for triage).
  </action>
  <verify>
    <automated>[ -f .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md ] && grep -q "Configuration A" .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md && grep -q "Configuration B" .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md && grep -q "Configuration C" .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md && echo OK</automated>
  </verify>
  <done>
    Human UAT performed on both platforms with the verbatim test sentence; outputs captured per-Configuration in 20.06-04-UAT-RESULTS.md; long-press and chevron behaviour validated on iOS; per-finding ✅/❌ verdicts recorded; resume signal received from user (approved / regression / partial).
  </done>
</task>


<task type="auto" tdd="false">
  <name>Task 3: On GREEN UAT — flip Phase 20 + 20.06 to Shipped</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Only proceed if Task 2 returned "approved" with all 5 findings ✅. If any finding ❌, STOP — return to user for follow-on planning.

    1. Update `.planning/ROADMAP.md`:
       - Find the Phase 20.06 row.
       - Mark it `✅ Shipped` with date.
       - Find the Phase 20 row.
       - If it currently reads `Code Complete (UAT-failed: behavioural)` or similar, flip to `✅ Shipped` (the artifact-level UAT was already 12/12; the behavioural gate now passes via Phase 20.06).
       - Reference the UAT-RESULTS.md path in the Phase 20.06 row.

    2. Update `.planning/STATE.md`:
       - `milestone:` unchanged (still v1.0 / v2.1 — Phase 20.06 is a hotfix, not a new milestone).
       - `status:` flip from `executing` to `idle` (or whatever the project's convention is for between-phase rest state — check the existing `status:` usage at the top of the file).
       - `stopped_at:` rewrite to `Phase 20 + Phase 20.06 SHIPPED <date>; UAT GREEN; Phase 20 closed.`
       - `last_activity:` `<date> -- Phase 20.06 UAT GREEN; Phase 20 closed`
       - Update progress block: `completed_phases` increments by 1 (20.06 is a new phase row), `total_plans` += 4, `completed_plans` += 4, `percent` recompute.
       - Append a new entry under `### Technical` (or whatever the Decisions section header is) ONLY IF a load-bearing decision emerged from UAT (e.g., a tweak to the HELVETISMS NEGATIVE list). Otherwise no decision entry.
       - Add a "Session Continuity" line: `Stopped at: Phase 20.06 SHIPPED — Phase 20 closed.`

    3. Commit both edits in one go:
       ```
       cd /Users/mowehr/code/dicticus
       git add .planning/STATE.md .planning/ROADMAP.md .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md
       git commit -m "docs(20.06): UAT GREEN — Phase 20 + Phase 20.06 SHIPPED"
       ```

    Do NOT push. The user opens the PR (project convention: maksim-101 owns push timing).
  </action>
  <verify>
    <automated>cd /Users/mowehr/code/dicticus && grep -q "20.06" .planning/ROADMAP.md && grep -q "Shipped" .planning/ROADMAP.md && grep -q "20.06" .planning/STATE.md && [ -f .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md ] && git log --oneline -1 | grep -q "20.06" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` reflects Phase 20.06 SHIPPED + Phase 20 closed.
    - `.planning/ROADMAP.md` Phase 20.06 row marked ✅ Shipped with reference to UAT-RESULTS.md.
    - `.planning/ROADMAP.md` Phase 20 row marked ✅ Shipped (closure of the parent UAT-failed state).
    - `.planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md` exists and is committed.
    - One commit `docs(20.06): UAT GREEN — Phase 20 + Phase 20.06 SHIPPED` present in `git log`.
    - Branch unchanged: still `feature/phase-19-ai-cleanup-ios`.
  </acceptance_criteria>
  <done>
    Project STATE + ROADMAP reflect Phase 20.06 SHIPPED and Phase 20 CLOSED; UAT results file committed; ready for the user to open a PR or move to the next phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a verification/documentation plan; no code surface change, no runtime data flow change beyond what 20.06-01/02/03 already shipped and threat-modelled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.06.04-01 | Repudiation | UAT result accuracy | mitigate | Verbatim capture rule (paste raw, do NOT sanitize). Tester identity recorded in frontmatter. UAT-RESULTS.md committed to git for tamper evidence. |
| T-20.06.04-02 | Tampering | False ✅ marker on a regressing case | mitigate | Per-finding granular ✅/❌ with required `details:` line forces explicit content; not just a single rolled-up Verdict. |
</threat_model>

<verification>
**Branch invariant:**
```
cd /Users/mowehr/code/dicticus && git branch --show-current
# Expected: feature/phase-19-ai-cleanup-ios
```

**Plan dependency invariant (commits exist before this plan runs):**
```
git log --oneline -30 | grep -E "20\.06-0[123]"
# Expected: at least 3 distinct commits from 20.06-01, 20.06-02, 20.06-03
```

**Artifact existence (after Task 2):**
```
[ -f .planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-UAT-RESULTS.md ] && echo "UAT results captured"
```

**Status flip (after Task 3, only on GREEN):**
```
grep -E "20\.06.*Shipped|Shipped.*20\.06" .planning/ROADMAP.md
grep "20.06" .planning/STATE.md
```
</verification>

<success_criteria>
- [ ] All three plan dependencies (20.06-01, 20.06-02, 20.06-03) shipped on the branch with commits in `git log`.
- [ ] macOS build + 4 affected test classes GREEN.
- [ ] iOS build status recorded.
- [ ] User-driven UAT executed against the verbatim test sentence on iOS Configurations A + B and macOS Configuration C.
- [ ] iOS HistoryRow long-press + chevron behaviour validated.
- [ ] 20.06-04-UAT-RESULTS.md captures verbatim outputs per Configuration with per-finding ✅/❌ markers.
- [ ] If UAT GREEN: STATE.md + ROADMAP.md updated; one closure commit recorded.
- [ ] If UAT RED on any finding: STOP and escalate to user — do NOT mark Shipped.
- [ ] No file outside `files_modified` changed (excluding the UAT results file, which is in `files_modified`).
</success_criteria>

<output>
After completion, create `.planning/phases/20.06-ai-cleanup-behavioural-hotfix/20.06-04-SUMMARY.md` documenting:
- Build status on both platforms (with the SHA tested).
- Per-Configuration verdict summary (link to UAT-RESULTS.md for full content).
- Per-finding pass/fail (5 findings).
- Phase 20 closure status (Shipped vs still-open).
- If any finding still ❌: a one-paragraph "next phase" recommendation (what to plan in 20.07).
- If all GREEN: a one-line "Phase 20 closed; ready for next milestone" entry.
</output>
