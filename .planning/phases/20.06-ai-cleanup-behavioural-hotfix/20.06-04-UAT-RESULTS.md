---
phase: 20.06-ai-cleanup-behavioural-hotfix
type: uat-results
date: 2026-04-27
tester: <FILL IN — name/email>
branch: feature/phase-19-ai-cleanup-ios
builds:
  macos: d631eb5 + Debug build (no signing) — see DerivedData/Dicticus-gnqxctluqpdanwaazwzhnpvdhncd
  ios:   d631eb5 + iPhone 17 sim (OS 26.4.1, id 5CD3EE34-B1F0-4075-A817-0935D353A7C1) — see DerivedData/Dicticus-fldeuyfiaepnizhgochqghzihwnn
preflight:
  macos_build: SUCCEEDED
  macos_tests: "CleanupPromptTests 19/19 GREEN (includes 5 Wave-1 HELVETISMS preservation + 2 Wave-2 STRICT-block tests)"
  ios_build: "SUCCEEDED on iPhone 17 sim (OS 26.4.1)"
---

# Phase 20.06 UAT Re-run

Original UAT findings: [`../20-ai-cleanup-demotion-uat-visibility/20-UAT-FINDINGS.md`](../20-ai-cleanup-demotion-uat-visibility/20-UAT-FINDINGS.md) (2026-04-27)

## Test sentence

> Also dann versuchen wir es doch mal auf Deutsch. Und zwar war ich am vergangenen Freitag ausgeflogen. Und zwar nach Basel, war da dann auch auf beiden Seiten der Grenze unterwegs, das heisst also auch auf der deutschen und schweizerischen Seite. Und nebst vielen leckeren Dingen, die ich gegessen hatte, war ich natürlich auch einkaufen, ebenfalls auf beiden Seiten. Auf der deutschen Seite habe ich ca. 110.57 € ausgegeben, während ich dann später noch auf der Schweizerischen Seite in der Tankstelle etwas Kleines als Erfrischung gekauft habe. Das hat mich dann ca. 4.50 Franken gekostet.

---

## Configuration A — iOS, AI cleanup OFF, Swiss ON

**Output (verbatim, 2026-04-27 re-run after Settings UX fix):**

```
Das hat mich dann ca. 4, Franken50 Euro gekostet.
```

**Verdict:** ⚠️ DEFERRED to Phase 20.07

| Item | Status | Details |
|------|--------|---------|
| Dialect-style and `heisst` preserved | ✅ | unchanged from pre-20.06 baseline |
| `110.57 €` preserved | ✅ | exact, no `Euro Euro` |
| `4.50 Franken` preserved | ⚠️ ASR-drift, deferred | ASR drifted on this run to `4, Franken50 Euro` — a shape `SwissNumberFormatter.bridgeCrossTokenDecimal` cannot rescue (regex requires whitespace-separated tokens; `Franken50` has letters glued to digits). Verified by simulating the formatter against 13 plausible ASR transcriptions: only `4 Franken 50 Euro` style inputs match Bridge 2; the formatter is not the corruption source. Logged as Phase 20.07 (Rules-only ASR-Mishearing Recovery) — needs a pre-tokenization split-rule with false-positive guards. |
| Homescreen "Copied to clipboard" preview truncated at `…` (Image #1) | ⚠️ pre-existing UX | Separate from 20.06 — forward-looking finding for a future UX phase. |

---

## Configuration B — iOS, AI cleanup ON, Swiss ON  *(CRITICAL CASE)*

**Output (verbatim, 2026-04-27 re-run after iOS warmup fix):**

```
Das hat mich dann ca. 4.50 Franken gekostet.
```

(Full sentence dialect/currency preserved; user reported "almost immediate" transcription — no hang.)

**Verdict:** ✅ GREEN

| Finding | Status | Details |
|--------|--------|---------|
| Pipeline completes within reasonable time (< 10s) | ✅ | "almost immediately transcribed" per user. Earlier hang was caused by `CleanupService.loadModel` (synchronous ~30 s `llama_model_load_from_file` C call) running on `MainActor` inside a `MainActor.run` wrapper; the fix marks `init` + `loadModel` + `initializeBackend` as `nonisolated` so they execute on the detached `Task.detached(priority: .userInitiated)` warmup task. |
| F-20-UAT-01 (dialect preserved: 'auf', 'ausgeflogen', 'gekostet') | ✅ | dialect kept verbatim |
| F-20-UAT-02 (currency preserved: 'Franken' at 4.50, no 'Euro Euro' at 110.57) | ✅ | `4.50 Franken` exact; no `Euro Euro` doubling |
| F-20-UAT-05 (number drift: '110.57' stays '110.57', not '100.57') | ✅ | preserved |

---

## Configuration C — macOS, AI cleanup ON, Swiss ON  *(OTHER CRITICAL CASE)*

**Output (verbatim):**

```
<paste raw transcription here>
```

**Verdict:** ⏳ pending

| Finding | Status | Details |
|--------|--------|---------|
| F-20-UAT-01 (dialect preserved) | ⏳ | <words observed> |
| F-20-UAT-02 (currency preserved, no Euro Euro) | ⏳ | <Franken vs Euro; Euro-Euro check> |

---

## iOS HistoryRow — long-press behaviour

**Behaviour observed:** <description — context menu? link preview? nothing?>

| Finding | Status |
|--------|--------|
| F-20-UAT-03 (.contextMenu Copy appears on long-press, NOT system path/link preview) | ⏳ |

---

## iOS HistoryRow — chevron + tap behaviour

| Item | Status |
|------|--------|
| Trailing `chevron.right` visible on each row | ⏳ |
| Tapping a row pushes HistoryDetailView (Raw/Polished picker view from Phase 20.05) | ⏳ |
| F-20-UAT-04 overall | ⏳ |

---

## Summary

| Finding | AI cleanup ON path | AI cleanup OFF (rules-only) path |
|---------|---------------------|----------------------------------|
| F-20-UAT-01 (dialect) | ✅ | ✅ |
| F-20-UAT-02 (currency) | ✅ | ⚠️ ASR-drift, → 20.07 |
| F-20-UAT-05 (number drift) | ✅ | ✅ |

**Result:** Phase 20.06's behavioural goal (demote LLM hallucinations on the AI-cleanup path) is met. The rules-only path's ASR-mishearing recovery surfaces as a new, narrower problem and is logged as Phase 20.07.

**In-phase fixes added beyond the original four plans (logged as 20.06-05-FIX in ROADMAP):**
- iOS warmup hang (`CleanupService` MainActor block on Step 4 LLM load) → `nonisolated init` + `loadModel` + `initializeBackend`.
- iOS onboarding "Download Now" non-reactive → `warmupService.warmup(force: true)`.
- iOS onboarding fake 1%/sec progress timer → real `FluidAudio.DownloadUtils.ProgressHandler` with phase + fraction.
- iOS Settings AI-Cleanup toggle did not immediately reveal "Download Required" panel → `@AppStorage` refactor in `AiCleanupSection` (UserDefaults reads were not observable; `@AppStorage` registers KVO).

**Phase 20 closure:** ✅ unblocked.
