---
phase: 20.08
artefact: variant-g-rationale
status: locked — all decisions answered 2026-04-29
purpose: Single source of truth for why variant (g) exists and what it must do, so future sessions stop re-deriving it from chat.
created: 2026-04-29
locked: 2026-04-29
---

# Variant (g) — Reasoning Chain

This document reconstructs the reasoning that produced variant (g)
after variant (e) was selected in 20.08-03 but then rejected post-UAT.
It is the canonical brief for any future spike or implementation work
on the Swiss / de cleanup prompt.

## 1. Cleanup Goal (User-Stated, 2026-04-29)

The AI cleanup is **slight speech correction**, not a heavy rewrite.
Same target output for everyone (Standard High German with Swiss
orthography); different cleanup intensity depending on speaker.

Concrete duties of the LLM cleanup pass:

1. Fix grammar errors.
2. Capitalize and punctuate correctly.
3. Fill in obvious gaps in broken or incomplete speech (non-native
   speakers — relevant for iOS/Windows rollout to a Swiss employer with
   many non-native employees who speak better than they write).
   #note: this is especially relevant for those two OS but should be consider for all OS including MacOS
4. **Recognise and repair contextual misrecognitions** — if a word does
   not fit the surrounding sentence (because the ASR misheard or the
   speaker mispronounced), replace it with the word that does fit.
5. Preserve the speaker's intent and content.
6. Do not rewrite what is already clean.
7. Do not translate High German into Swiss German dialect (the original
   20.08 anti-pattern).

Disfluencies — stutters, repetitions, filler words, swearing — are
**RulesCleanupService territory** (the deterministic Swift pre-pass,
Phase 20.02-03). They are NOT in the LLM's brief.

> **Decisive correction from the user:** "this is not contradictory
> but complementary. … the goal is exactly the same." The native vs.
> non-native split is intensity, not target. I had earlier framed them
> as contradictory; that framing was wrong.

## 1.5. Empirical Evidence from Variant (e) UAT (2026-04-29)

Recorded in the unmerged worktree at
`.claude/worktrees/agent-a3d0b5eb9cc61fc94/.planning/phases/20.08-llm-swiss-ification-suppression/20.08-04-UAT-RESULTS.md`.
Three concrete failure modes observed when running variant (e) on real
multi-clause input:

- **Noun capitalization regression** — output: "autos auf der strasse",
  "fahrräder", "mufers". Common German nouns left lowercase. Caused by
  the model over-respecting the identity-preserving clauses ("Output
  Standard High German words and grammar exactly as dictated", "Do not
  substitute any word with a different word"). Spike fixtures 2-5 were
  already correctly cased so this gap was invisible at design time.
- **Clause-seam duplication** — output: "fahrräder und mufers und Und auf
  die Spritpreise". The model emits "und Und" at clause boundaries.
- **Meaning-inference scar** — anchor utterance "würde ich denn" is
  transcribed by ASR as something like "Würdigten" and variant (e)
  passes it through unchanged because substitution is forbidden. The
  contextual-mishear-repair duty in variant (g) would catch this.

What variant (e) DID succeed at:

- Zero Swiss dialect particles on the anchor utterance (the original
  20.08 primary goal).
- Phase 20.06 currency anti-flip preserved ("2.20 Franken").
- Swiss orthography rule applied ("heisst" not "heißt").

This empirical record is the strongest signal yet that the
identity-preserving contract is the wrong contract for production. It
also confirms the spike's fixture-coverage gap: fixtures testing
already-clean input cannot surface a "fails on dirty input" regression.

## 2. Why Variant (e) Is Wrong

Variant (e), winner of the 20.08-03 spike:

```text
INSTRUCTION: Edit the following German dictation for grammar, punctuation, and capitalization. Do not paraphrase. Do not substitute any word with a different word.
LANGUAGE: de
RULE 1: Output Standard High German words and grammar exactly as dictated.
RULE 2: Apply Swiss orthography only — write ss instead of ß; write thousands as 1'250 not 1.250.
```

**Why it passed the spike:** the 5 fixtures were already-clean Standard
High German sentences. Identity preservation = correct behaviour on
identity-shaped inputs.

**Why it fails post-UAT real usage:**

- "Do not paraphrase. Do not substitute any word with a different
  word." **explicitly forbids** the cleanup the user actually wants
  (gap-filling, contextual mishear repair, grammatical reconstruction).
- "Output Standard High German words and grammar **exactly as
  dictated**" doubles down on the same prohibition.
- Observed UAT failures with variant (e):
  - lowercase nouns left uncapitalized (e.g. "Fahrrad")
  - "und Und" duplication left in
  - "Würdigten" left as a meaning-inference scar
  - residual Swiss dialect leakage on some inputs (Plan 02 gate didn't
    catch everything)

The model is doing exactly what variant (e) tells it to do. The
contract is the bug.

## 3. Variant (g) Evolution

### Draft 1 (rejected — by user)

```text
INSTRUCTION: Clean up the following German dictation into Standard High German. Remove stutters, repetitions, and filler words. Fix grammar and capitalize correctly. Fill in obvious gaps in broken speech. Preserve the speaker's intent and content — do not rewrite or rephrase what is already clean.
LANGUAGE: de
RULE 1: Output Standard High German. Do not output Swiss German dialect (no "uf", "hie", "alli", "mini", "wahrschiinli", "choschtet", "gsi", etc.).
RULE 2: Apply Swiss orthography — write ss instead of ß; write thousands as 1'250 not 1.250.
```

User rejected RULE 1's negative-particle list: "isn't that
highlighting something I think that might not even be an issue or is a
very rare issue even." Same priming trap that bit us with HELVETISMS —
the model treats the negative-example list as "things on the table."

User added a new requirement: contextual misrecognition repair.

### Draft 2 (rejected — by me, on architecture grounds)

```text
INSTRUCTION: Clean up the following German dictation. Remove stutters, repetitions, and filler words. Fix grammar errors. Capitalize and punctuate correctly. Fill in obvious gaps in broken or incomplete speech. If a word does not fit the surrounding context — typically because the speech recognizer misheard it or the speaker mispronounced it — replace it with the word that does fit. Preserve the speaker's intent. Do not rewrite or rephrase what is already clean.
LANGUAGE: de
RULE 1: Output Standard High German.
RULE 2: Apply Swiss orthography — write ss instead of ß; write thousands as 1'250 not 1.250.
```

I rejected my own draft after re-reading `CleanupPrompt.swift`. The
Phase 20 D-01 architecture comment makes it explicit:

> "verb changed Rewrite → 'Lightly edit' and the LLM is no longer
> asked to remove fillers or convert spelled numbers — those tasks
> moved to the deterministic Swift rules pass (RulesCleanupService,
> plan 20-03)."

Asking the LLM to "remove stutters, repetitions, and filler words"
re-duplicates work into the LLM that already lives in deterministic
Swift. Bad layering.

### Draft 3 (current — pending user confirmation)

```text
INSTRUCTION: Clean up the following German dictation. Fix grammar errors. Capitalize and punctuate correctly. Fill in obvious gaps in broken or incomplete speech. If a word does not fit the surrounding context — typically because the speech recognizer misheard it or the speaker mispronounced it — replace it with the word that does fit. Preserve the speaker's intent. Do not rewrite or rephrase what is already clean.
LANGUAGE: de
RULE 1: Output Standard High German.
RULE 2: Apply Swiss orthography — write ss instead of ß; write thousands as 1'250 not 1.250.
```

What changed vs. Draft 2:

- **Dropped:** "Remove stutters, repetitions, and filler words" —
  belongs in RulesCleanupService.
- RULE 1 has no negative-particle list — Plan 02's helvetism-delta
  gate is the runtime backstop.
- **Kept:** gap-filling, contextual-mishear repair, intent
  preservation, no-heavy-rewrite — these are LLM-only duties.

Defense-in-depth: Plan 20.08-02's helvetism-delta gate
(`CleanupService.gateLLMDialect`) remains the structural backstop if
the model slips back into dialect.

## 4. Decisions (Answered 2026-04-29)

### A1 — Non-German languages: KEEP base `defaultInstruction`

English and other-language inputs continue to flow through the
existing `defaultInstruction` ("Lightly edit the following transcribed
text…"). Variant (g) duties (gap-filling, contextual-mishear repair)
are NOT promoted to the base instruction in this round. Re-evaluate in
a follow-on phase after empirical data from production.

### A2 — German + Swiss toggle: TOGGLE GATES ORTHOGRAPHY ONLY

The Swiss toggle controls *only* whether RULE 2 (Swiss orthography:
ss-for-ß, 1'250 thousands) is appended. The variant (g) INSTRUCTION
body and RULE 1 ("Output Standard High German") apply to **all German
input regardless of toggle state.** Otherwise the toggle would
silently downgrade cleanup quality, which contradicts its purpose.

This is an architectural change to `Shared/Models/CleanupPrompt.swift`:

- **Before** (current): `swissEnabled && language == "de"` gated the
  entire HELVETISMS block.
- **After:** two conditional layers:
  1. `language == "de"` → use the variant (g) INSTRUCTION body + RULE 1
     in place of `defaultInstruction`.
  2. `swissEnabled && language == "de"` → append RULE 2 (Swiss
     orthography).

Two German prompt shapes will exist post-implementation:
- "de-only" (variant g INSTRUCTION + RULE 1)
- "de + Swiss orthography" (variant g INSTRUCTION + RULE 1 + RULE 2)

### A3 — Profanity backlog: YES, open it

Open `.planning/backlog/rulescleanup-profanity-audit.md` capturing the
need for a profanity word-list addition to `RulesCleanupService` (or
its successor). Out of 20.08 scope.

### A4 — Cross-platform applicability

Duty #3 (gap-filling for non-native speakers) applies to **all
platforms** — macOS, iOS, Windows. The iOS/Windows rollout to a Swiss
employer makes it especially salient there, but the macOS app benefits
equally. No per-platform prompt branching.

## 5. Spike Fixture Set (Draft 3)

Original 5 fixtures from 20.08-03 + 1 new fixture for non-native
broken grammar:

1. UAT-anchor (2026-04-27 failing utterance) — regression check
2. Original everyday-German fixture from 20.08-03 — regression
3. Original everyday-German fixture from 20.08-03 — regression
4. "vier Franken fünfzig" — confirms we don't auto-convert currency
   (still ITN territory per backlog item)
5. Original everyday-German fixture from 20.08-03 — regression
6. **NEW:** Non-native broken grammar — e.g. "Ich gestern gehen Markt
   und kaufen viele Apfel weil ich brauchen für Kuchen." Tests
   grammatical reconstruction + gap filling.

**Dropped (per user):**
- The earlier "fixture 8" ASR-mishear-in-context example ("Auf der
  anderen Seite war ich wahrscheinlich, alle meine, würde ich denn,
  hat, hier ausfiltern.") — user: "doesn't make sense. We can take a
  bad grammar example, but this, I don't know if anybody would say
  that just like that." Replaced by fixture 6 above.
- Earlier proposed disfluency / repetition / profanity fixtures —
  those test RulesCleanupService, not the LLM prompt.

**Grid shape:** 6 fixtures × 1 variant (no comparison column —
variants e/f already taught us few-shot is unsafe at temp 0.1 on
Gemma 4 E2B; (g) goes solo).

## 6. Pass Criteria for Variant (g)

For each fixture, output must:

- [P1] Be Standard High German (no Swiss dialect particles)
- [P2] Apply Swiss orthography only (ss, 1'250) — no other vocabulary
  substitution toward dialect
- [P3] Capitalize German nouns correctly
- [P4] Fix grammar errors when present (fixture 6)
- [P5] Preserve speaker intent — no paraphrase, no rewrite of
  already-clean text
- [P6] Repair words that don't fit context, when context is
  unambiguous
- [P7] Not auto-convert spelled-out currency to numeric (still
  ITN/backlog territory — fixture 4)

Pass = all applicable criteria met. Fail on any P1-P7 violation.

## 7. Current Production State (As of 2026-04-29)

- `feature/phase-19-ai-cleanup-ios` tip: `68e86ce`. Carries 20.08-01
  (SwissDialectForms), 20.08-02 (helvetism-delta gate), 20.08-03 (spike
  harness with variants e+f, variant (e) recorded as winner).
- `Shared/Models/CleanupPrompt.swift` on this branch: Phase 20.06
  preservation-first HELVETISMS block with negative trap list. **NOT
  variant (e).**
- 20.08-04 commits (`61070d1` swap to variant (e), `8613dba` R6 tests)
  exist on `worktree-agent-a3d0b5eb9cc61fc94` but have NOT merged into
  the feature branch.
- This means variant (g) can replace variant (e) **before** anything
  ships to the feature branch — no undo needed on production prompt.

## 8. Decisions Locked (2026-04-29)

| ID | Question | Answer |
|----|----------|--------|
| D1 | Confirm Draft 3 prompt body | ✅ Locked. Try it. |
| D2 | Non-German: promote variant (g) duties? | ❌ Keep base `defaultInstruction`. Revisit later. |
| D3 | German + Swiss toggle OFF | Toggle gates ONLY RULE 2 (Swiss orthography). INSTRUCTION + RULE 1 fire on all German. |
| D4 | Profanity backlog item | ✅ Open `.planning/backlog/rulescleanup-profanity-audit.md`. |
| D5 | Wave structure | ✅ Stay in 20.08. Wave shape: implementer's call. |

## 8.1. Wave Plan (Implementer's Choice — to be confirmed)

Three-wave shape proposed for 20.08:

- **Wave A — Spike (g):** add `buildVariantG` to the spike harness;
  add fixture 6 (non-native broken grammar); run 6×1 grid; record
  results in `20.08-SPIKE-RESULTS.md` (append section, do not overwrite
  the (e) decision — historical record).
- **Wave B — Production wiring + supersede 20.08-04:** restructure
  `Shared/Models/CleanupPrompt.swift` per A2 (two-layer German
  conditional); update R6 contract tests to lock variant (g) shape;
  document that the unmerged worktree commits `61070d1` + `8613dba`
  are now obsolete.
- **Wave C — UAT replay:** re-run the 2026-04-27 anchor utterance +
  multi-clause UAT on macOS Debug; verify variant (g) catches
  lowercase nouns, "und Und" duplication, and the "Würdigten"
  meaning-inference scar. iOS UAT remains deferred per the prior plan
  pending FluidAudio drift fix.

Open backlog files (D4 + year-thousands) to be created before Wave A
so they are not lost.

## 9. Out of Scope (Explicit)

- Currency spell-out → numeric conversion (deterministic ITN; backlog)
- Profanity word-list (RulesCleanupService; separate phase)
- iOS/Windows-specific UX work
- Acronym spacing collapse (separate finding, separate phase)
- **Year-as-thousands bug** — observed 2026-04-29: "Jahr 2'026" gets a
  Swiss thousands separator. The LLM prompt RULE 2 ("thousands as
  1'250") is correct in isolation; the bug is in deterministic Swift
  (likely `SwissNumberFormatter` / `ITNUtility` foldThousandsSeparator):
  years (1900-2099) should be exempt. Sibling to Phase 20.06's
  currency-direction fix. Belongs in a separate ITN-polish phase. New
  backlog item required.
- ASR Swiss-dialect rendering on raw German speech ("de rote Punkt i de
  Mübar") — Parakeet/FluidAudio language-detection issue, separate
  Phase 2.x ASR concern.
- Menu-bar red recording dot missing — UI regression, separate phase.

---

## 10. Plan 04 Prep — Post-Plan-03 Ad-Hoc Work (2026-04-30)

Between locking this rationale (2026-04-29) and being ready to write
Plan 04, several ad-hoc commits landed on `feature/phase-19-ai-cleanup-ios`
that change the Plan 04 surface area. Recorded here so Plan 04 picks up
the *current* state, not the state at the time variant (g) was first
drafted.

### 10.1 Commit Trail

| Commit  | Subject | Why it matters for Plan 04 |
|---------|---------|----------------------------|
| `435004b` | feat(20.08): apostrophe-strike — drop thousands grouping + widen Bridge 2 cents regex | Removes the `1'250` thousands-separator clause from the LLM contract; deterministic Swift now emits ungrouped integers. Plan 04's variant (g15) prompt does NOT mention thousands at all (vs. the (e) winner's `RULE 2`). |
| `78738a4` | fix(20.08): align sampler chain to llama-server convention (top_k → top_p → temp → dist) | Production macOS sampler chain reordered to match `/tmp/spike-harness` and llama-server. Variant (g15)'s 7/7 sweep was measured under this order — Plan 04's R6 tests must run under the same chain. |
| `17005f3` | feat(20.08): SwissNumberFormatter Bridge 1.5 — word-cardinal cents prep | Moves the cardinals-to-digits responsibility for `vier Franken 50` etc. OUT of the LLM prompt and INTO deterministic Swift. Variant (g15) drops what was variant (g)'s `RULE 3` (cardinals ≥13 → digits). Plan 04's prompt body has no number-formatting rules at all. |
| `8daf91a` | chore(20.08-spike): mirror production pipeline + anglicism differentiation | Spike harness now runs ITN before the LLM and `SwissNumberFormatter.format` after — so Wave-B numbers measured 7/7 reflect the actual production pipeline, not a synthetic LLM-only path. Plan 04's UAT replay can rely on the spike numbers as predictive. Also: anglicism *differentiation* (established Fachbegriffe stay English, untypical adjectives get translated) is now baked into the variant (g15) examples. |
| `fb72da3` | chore: harden .gitignore + drop tracked Finder/log junk | Hygiene only — no Plan 04 impact. |

### 10.2 Variant (g15) Final Form

The Wave-B exploration in this rationale settled on Draft 3 (§3) as the
*conceptual* winner. The empirical winner — the prompt that produces
the 7/7 fixture pass at production sampler params — is **variant
(g15)**, a few-shot ORIGINAL/KORRIGIERT German-language frame, NOT the
instruction-only frame Draft 3 sketched. The pivot:

- Draft 3 was English-instruction, no examples.
- Variant (g) (in-app harness) added an English instruction + RULE 3
  cardinals clause.
- Variant (g12) (`/tmp/spike-harness`) flipped to a German-language
  4-shot frame (anglicism preservation, broken-grammar reconstruction,
  currency-cents preservation, dialect-anti-drift) — stabilised
  Gemma 4 E2B at temp 0.1 in a way variants (e) and (f) never managed.
- Variant (g14) added a verbose 4-pair umlaut anchor → disqualified
  (F2 prompt-leak, F3 Deadline→Frist).
- **Variant (g15)** = g12 + a six-token umlaut clause
  (`Umlaute ä/ö/ü bleiben`) that fixes F3 muessen drift while keeping
  the prompt-shape identical to g12.

The verbatim block is in `20.08-SPIKE-RESULTS.md` "Wave B Update"
section and the reference Swift builder is
`SpikeFixtures.buildVariantG15` in
`macOS/Dicticus/Views/CleanupSpikeView.swift`.

This contradicts the §3 conclusion against few-shot. The empirical
finding from Wave B: few-shot is unsafe at temp 0.1 *when examples
share content with the input* (variant f's failure) but stabilising
*when examples each demonstrate a distinct rule on diverse content*
(variant g15's success). The earlier conclusion stands as a constraint,
not a prohibition.

### 10.3 Spike Harness Pipeline Mirror

The Wave-B in-app spike (variant g harness) was bypassing pre/post LLM
steps. Commit `8daf91a` brought it to parity with production:

- **Step 2 (ITN):** `ITNUtility.applyITN` runs BEFORE the LLM, so
  cardinals ≥10 (`zwanzig`, `hundertfünfzig`) arrive at Gemma in digit
  form.
- **Step 3b (Swiss number formatter):** `SwissNumberFormatter.format`
  runs AFTER the LLM on de fixtures only.
- **Per-fixture seed reset (`0xDEADBEEF`)** so cells are independently
  reproducible — without this, sampler state cascaded across fixtures
  and a prompt change on N shifted outputs for N+1, N+2.
- Spike timeout extended to 30 s (`timeoutSeconds:` parameter on
  `cleanupWithExplicitPrompt`) for long few-shot prompts on
  multi-clause fixtures.
- `cleanSpikeOutput` hardened against BPE-fragmented chat-template
  leaks (`_turn>`, `▁turn>`, `<end_of_turn>` tails the upstream marker
  scan misses when SentencePiece splits the template token).

### 10.4 Anglicism Differentiation (New Cleanup Duty)

The variant (g15) INSTRUCTION encodes a two-tier rule for English words
in German sentences:

- **Etablierte Fachbegriffe stay English:** `Deadline`, `Meeting`,
  `Workaround`, `E-Mail`, `Team`, `Product Owner`, `Release` — these
  are how Swiss/German tech speakers actually use the words.
- **Untypical English adjectives/verbs get translated:** `realistic` →
  `realistisch`, `awesome` → `toll`, `appreciate` → `schätzen` — these
  are calques the speaker probably reached for under code-switch
  pressure, not native vocabulary.

This duty is NOT in any earlier draft (§3) and was not in the original
20.08-RESEARCH.md. It emerged from Wave-B fixtures and is now part of
variant (g15)'s contract. Plan 04 ships it as part of the verbatim
prompt body.

### 10.5 Apostrophe-Strike Implication for Plan 04

The original Phase 19.5 D-C1 decision rendered Swiss thousands as
`2'026`. Commit `435004b` strikes this rule from `SwissNumberFormatter`
(years getting an apostrophe is wrong; the simpler fix is no thousands
separator at all). Period-decimal (D-C2) still applies.

Implications for the Plan 04 prompt:
- Variant (g15) has NO thousands-separator clause and NO apostrophe in
  the third example.
- The R6 negative test in Plan 04 should NOT assert presence of the
  `1'250` string.
- Bridge 2's cents regex was widened from `\d{1,3}` to `\d+` to fold
  4-digit amounts like `1250 Franken 20` → `1250.20 Franken` —
  surfaced during variant (g15) UAT prep.

### 10.6 R6 Test Shape Update

Plan 04's original R6 contract tests (designed for variants a/b/c/d)
asserted on the substring `Standard High German`. Variant (g15) is
written in German, so the assertions change:

| Old (variant e/letter-variants) | New (variant g15) |
|---------------------------------|-------------------|
| `prompt.contains("Standard High German")` | `prompt.contains("Standard-Hochdeutsch")` |
| Negative: no `SwissHelvetisms.words.joined(", ")` | Negative: no negative-particle list (already absent in g15) |
| Conditional skip on variant b | (No conditional needed — g15 is unconditional winner) |
| (Not present) | Two-layer gating test: `ss statt ß` clause appears IFF `swissEnabled == true && language == "de"` |
| (Not present) | Few-shot frame markers: `prompt.contains("ORIGINAL:")` and `prompt.contains("KORRIGIERT:")` — locks the 4-shot shape |

### 10.7 Year-Thousands Bug + Profanity Audit (Backlog)

Captured at:
- `.planning/backlog/year-thousands-separator-bug.md` — years
  (1900-2099) should never get an apostrophe; deterministic Swift
  bug, not LLM territory.
- `.planning/backlog/rulescleanup-profanity-audit.md` — RulesCleanupService
  has no profanity word-list.

Both are explicitly OUT of Plan 04 scope (the apostrophe-strike commit
addresses thousands at the formatter level; profanity is unrelated to
Swiss-ification suppression).

### 10.8 Plan 04 Net Delta

Compared to the originally-drafted 20.08-04-PLAN.md (which targeted
variant a/b/c/d), Plan 04 must now:

1. Source the verbatim prompt from **20.08-SPIKE-RESULTS.md "Wave B
   Update" section** (variant g15) AND this rationale doc, NOT from
   the older "Verbatim Prompt Block to Ship in Plan 04" section above
   it (which is variant e).
2. Restructure `Shared/Models/CleanupPrompt.swift` per §4 D3 — two-layer
   German conditional, NOT a single `swissEnabled && language == "de"`
   gate.
3. Update R6 test shape per §10.6.
4. Reference VARIANT-G-RATIONALE.md as the canonical brief in the
   in-code comment header.
5. Acknowledge that the 20.08-04 worktree commits `61070d1` (variant
   (e) swap) + `8613dba` (variant (e) R6 tests) are obsolete and must
   not be merged — see §7.
