---
phase: 20.08
slug: llm-swiss-ification-suppression
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 20.08 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: derived from `20.08-RESEARCH.md` §6 "Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | XCTest (Apple-native, both iOS + macOS targets) |
| **Config file** | macOS `.xcodeproj` (committed); iOS xcodegen `iOS/project.yml` (project regenerated) |
| **Quick run command** | `xcodebuild test -only-testing:DicticusTests/SwissDialectFormsTests -scheme Dicticus -destination 'platform=macOS'` |
| **Full suite command (macOS)** | `xcodebuild test -scheme Dicticus -destination 'platform=macOS'` |
| **Full suite command (iOS)** | `cd iOS && xcodegen generate && xcodebuild test -scheme Dicticus -destination 'platform=iOS Simulator,name=iPhone 17'` |
| **Estimated runtime** | ~30s (targeted unit) · ~3 min (full macOS) · ~5 min (full iOS) |

---

## Sampling Rate

- **After every task commit:** Run targeted unit test for the file just touched (e.g. `…SwissDialectFormsTests` after editing `SwissDialectForms.swift`).
- **After every plan wave:** Run full macOS suite. Run iOS suite at the wave boundary IF the FluidAudio drift is resolved (see Manual-Only).
- **Before `/gsd-verify-work`:** Full macOS suite green; iOS suite green OR explicit deferral note documented.
- **Max feedback latency:** ≤ 60s for targeted unit; ≤ 300s for full suite.

---

## Per-Task Verification Map

> Plan IDs and task IDs are placeholders — the planner finalizes wave assignment in Step 8. The map below mirrors `RESEARCH.md` Requirements Table (R1–R9) and binds each requirement to a verification mechanism.

| Req | Behavior | Wave | Test Type | Automated Command | File Exists | Status |
|-----|----------|------|-----------|-------------------|-------------|--------|
| R1 | Gate demotes when LLM injects dialect token absent from raw ASR | 0 | unit (deterministic) | `xcodebuild test -only-testing:DicticusTests/CleanupServiceTests/testGateLLMDialectDemotesOnUnsolicitedToken` | ❌ W0 | ⬜ pending |
| R2 | Gate passes when LLM output has zero dialect delta | 0 | unit (deterministic) | `xcodebuild test -only-testing:DicticusTests/CleanupServiceTests/testGateLLMDialectPassesOnCleanLLMOutput` | ❌ W0 | ⬜ pending |
| R3 | Gate accepts dialect that was already in the raw ASR (speaker-said exception) | 0 | unit (deterministic) | `xcodebuild test -only-testing:DicticusTests/CleanupServiceTests/testGateLLMDialectAcceptsSwissAlreadyInRaw` | ❌ W0 | ⬜ pending |
| R4 | `SwissDialectForms.tokens` shape contract (count 30..50, lowercase, unique, no overlap with `SwissHelvetisms.words`) | 0 | unit (data) | `xcodebuild test -only-testing:DicticusTests/SwissDialectFormsTests` | ❌ W0 | ⬜ pending |
| R5 | License attribution constant present and well-formed | 0 | unit (data) | `…/SwissDialectFormsTests/testLicenseAttribution` | ❌ W0 | ⬜ pending |
| R6 | New HELVETISMS prompt block contract (spike-winner directive present; if variant a/c/d wins, no `SwissHelvetisms.words.joined(", ")` in output) | 1 | unit (string contract, macOS only — file precedent) | `xcodebuild test -only-testing:DicticusTests/CleanupPromptTests` | ⚠️ extends existing | ⬜ pending |
| R7 | Pipeline integration: dialect-gate runs BEFORE Levenshtein-gate; both demote to identical `rulesCleanedText` | 1 | integration | `xcodebuild test -only-testing:DicticusTests/TextProcessingServiceTests/testDialectGateRunsBeforeLevenshteinAndDemotes` | ❌ W0 | ⬜ pending |
| R8 | Spike: human picks variant that preserves High German on ALL 5 fixtures | 1 | manual / qualitative (debug-only harness) | `CleanupSpikeView` on macOS Debug build | ❌ W0 (harness IS the deliverable) | ⬜ pending |
| R9 | UAT replay binary pass/fail of the 2026-04-27 utterance on macOS + iOS Release | 2 | manual UAT | Live mic + clipboard inspection | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Shared/Models/SwissDialectForms.swift` — provides the data file the unit tests depend on (R4, R5)
- [ ] `iOS/DicticusTests/SwissDialectFormsTests.swift` — covers R4, R5
- [ ] `macOS/DicticusTests/SwissDialectFormsTests.swift` — covers R4, R5 (parity per `feedback_cleanup_cross_platform_parity.md`)
- [ ] `Shared/Services/CleanupService.swift::gateLLMDialect` + `tokenizeForDialectGate` helper — required for R1, R2, R3, R7
- [ ] `iOS/DicticusTests/CleanupServiceTests.swift` AND/OR `macOS/DicticusTests/CleanupServiceTests.swift` — gate-function tests for R1, R2, R3
- [ ] `macOS/Dicticus/Views/CleanupSpikeView.swift` (or equivalent debug-menu integration) — covers R8
- [ ] Confirm iOS xcodeproj generation works AND `IOSTranscriptionService.swift` FluidAudio drift status before counting on iOS test execution. If blocked: document and proceed macOS-only for unit tests; iOS UAT (R9) still required at phase gate.

---

## Manual-Only Verifications

| Behavior | Req | Why Manual | Test Instructions |
|----------|-----|------------|-------------------|
| Spike variant selection (best preservation of High German across 5 fixtures) | R8 | Qualitative judgment of natural-language preservation; no automatable rubric without re-introducing the very LLM behaviour we're trying to gate. | 1) Build macOS Debug. 2) Open the spike harness (debug menu). 3) Run the 5 fixtures × 4 variants (~20 inferences, seed-pinned). 4) Pick the variant that produces clean High German on ALL 5 fixtures. 5) Record decision in 20.08-SPIKE-RESULTS.md. |
| 2026-04-27 UAT replay on macOS | R9 | Final acceptance is end-to-end on a real mic; integration tests cannot prove the prompt-+-gate combo holds against the actual model under real audio. | 1) Build macOS Release with spike-winner prompt + dialect gate enabled. 2) Dictate the 2026-04-27 utterance with AI cleanup ON + Swiss toggle ON. 3) Confirm output is High German words/grammar with Swiss orthography (`ss` only). 4) Confirm no Swiss particles (`uf, hie, het, alli, mini, wahrschiinli, …`). |
| 2026-04-27 UAT replay on iOS | R9 | Same reason as macOS; cross-platform parity requires both. | Same procedure on iOS Release (TestFlight or device build). Skip if iOS FluidAudio drift remains unresolved AND document the deferral. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 300s for full suite
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
