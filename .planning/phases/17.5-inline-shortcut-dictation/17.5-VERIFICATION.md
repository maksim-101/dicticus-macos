---
phase: 17.5-inline-shortcut-dictation
verified: 2026-04-23T20:15:00Z
status: human_needed
score: 17/18 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Tap mic button on Dicticus keyboard in a third-party app (e.g., Messages), speak, and confirm transcription appears at cursor"
    expected: "After mic tap, keyboard shows hourglass, then stop button (recording), then speech bubble (transcribing), then text appears at cursor position in the text field"
    why_human: "Full end-to-end IPC flow across two processes (keyboard extension + main app) requires device testing with real audio input and real textDocumentProxy insertion"
  - test: "Tap mic button when main app is not running (cold start), confirm app launches and recording starts"
    expected: "Keyboard shows hourglass, app opens via URL scheme, recording begins, transcription delivered back to keyboard and inserted at cursor"
    why_human: "Cold-start app launch via responder-chain URL scheme cannot be verified programmatically"
  - test: "Long-press mic button during recording to cancel"
    expected: "Dictation cancels, keyboard returns to idle state, mic button shows microphone emoji"
    why_human: "Gesture recognition and IPC cancel flow requires device interaction"
  - test: "Verify smart text insertion: dictate mid-sentence and confirm lowercase first char and leading space"
    expected: "After 'Hello' at cursor, dictating 'World' should insert ' world' (space + lowercase W)"
    why_human: "textDocumentProxy.documentContextBeforeInput behavior varies by host app and cannot be tested without a real text field"
---

# Phase 17.5: Darwin IPC Keyboard Dictation Verification Report

**Phase Goal:** Enable the Dicticus keyboard extension's mic button to trigger dictation in the main app via Darwin notification IPC, with transcription inserted directly at the cursor via textDocumentProxy -- user never leaves their current app.
**Verified:** 2026-04-23T20:15:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

**Plan 01 -- Intent Flag Wiring**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | DictateIntent.perform() writes isShortcutLaunch=true alongside pendingDictation=true to App Group UserDefaults | VERIFIED | `iOS/Dicticus/Intents/DictateIntent.swift:17-18` -- `DicticusIPCBridge.defaults?.set(true, forKey: "pendingDictation")` and `set(true, forKey: "isShortcutLaunch")` |
| 2 | DictationViewModel exposes a published isShortcutLaunch property | VERIFIED | `iOS/Dicticus/DictationViewModel.swift:18` -- `@Published var isShortcutLaunch: Bool = false` |
| 3 | checkPendingIntent() reads and consumes isShortcutLaunch before starting dictation | VERIFIED | `iOS/Dicticus/DictationViewModel.swift:217-218` -- reads `shared?.bool(forKey: "isShortcutLaunch")`, clears it, passes to `startDictation(fromShortcut:)` |
| 4 | stopDictation() resets isShortcutLaunch to false so manual dictation is unaffected | FAILED | Commit `33afb01` intentionally removed `isShortcutLaunch = false` from `stopDictation()` to keep swipe hint visible. Reset now occurs at `startDictation(fromShortcut: false)` line 61. Manual dictation is unaffected because `startDictation` defaults `fromShortcut` to `false`. See override suggestion below. |
| 5 | DicticusShortcuts includes a Quick dictation phrase | VERIFIED | `iOS/Dicticus/Intents/DicticusShortcuts.swift:11` -- `"Quick dictation with \(.applicationName)"` |

**Plan 02 -- IPC Bridge + Host Bridge**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 6 | DicticusIPCBridge defines all Darwin notification names and UserDefaults keys shared between app and keyboard | VERIFIED | `iOS/Dicticus/IPC/DicticusIPCBridge.swift` -- 131 lines, 7 Darwin notification names (lines 35-43), 4 UserDefaults keys (lines 22-28), RecordingState enum, heartbeat helpers, post helper |
| 7 | DicticusHostBridge listens for keyboard commands via Darwin notifications and publishes state updates back | VERIFIED | `iOS/Dicticus/IPC/DicticusHostBridge.swift` -- `registerDarwinObserver` for start/stop/cancel (lines 26-28), `handleNotification` dispatches to callbacks (lines 100-111), 4 publish methods (lines 42-62) |
| 8 | DicticusHostBridge runs a 1Hz heartbeat so keyboard can detect warm sessions | VERIFIED | `iOS/Dicticus/IPC/DicticusHostBridge.swift:66-73` -- `startHeartbeatTimer` with 1-second interval calling `DicticusIPCBridge.touchHeartbeat()`. Rate-limited via `OSAllocatedUnfairLock` in bridge (line 95) |
| 9 | DictationViewModel owns the DicticusHostBridge and calls publish methods at each lifecycle point | VERIFIED | `iOS/Dicticus/DictationViewModel.swift:22` -- `var hostBridge: DicticusHostBridge?`. Calls: `publishRecordingStarted()` (line 89), `publishTranscribing()` (line 100), `publishTranscriptionReady()` (line 127), `publishNoSpeech()` (lines 130, 149, 152) |
| 10 | DicticusIPCBridge.swift is compiled into both Dicticus and DicticusKeyboard targets | VERIFIED | `iOS/project.yml:59` -- `path: Dicticus/IPC/DicticusIPCBridge.swift` under DicticusKeyboard sources. File imports only Foundation (line 1) -- no UIKit/SwiftUI |

**Plan 03 -- Keyboard IPC + Dictation Controller**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 11 | Keyboard mic button sends startRecording command to main app via Darwin notification | VERIFIED | `DicticusKeyboardDictationController.swift:94` -- `ipcManager.sendStartCommand()` which calls `DicticusIPCBridge.postDarwinNotification(named: .startRecording)` |
| 12 | Keyboard receives recordingStarted/transcribingStarted/transcriptionReady/noSpeech state updates from main app | VERIFIED | `DicticusKeyboardIPCManager.swift:87-91` -- registers observers for all 4 notifications. Callbacks dispatch to controller (lines 125-147) |
| 13 | Keyboard state machine transitions: idle -> waitingForApp -> recording -> transcribing -> idle | VERIFIED | `DicticusKeyboardDictationController.swift:19-24` -- State enum with all 4 cases. Transitions in `handleMicTap` (idle->waitingForApp, recording->transcribing), IPC callbacks (waitingForApp->recording, recording->transcribing, *->idle) |
| 14 | Keyboard detects warm session (heartbeat fresh) and sends Darwin notification directly | VERIFIED | `DicticusKeyboardDictationController.swift:92-95` -- `if ipcManager.isSessionWarm()` then `sendStartCommand()` + grace period |
| 15 | Keyboard detects cold session (no heartbeat) and opens main app via URL scheme | VERIFIED | `DicticusKeyboardDictationController.swift:97-99` -- else branch calls `openURL?(startRecordingURL)` where `startRecordingURL = dicticus://record/start` |
| 16 | Transcription text is inserted at cursor via textDocumentProxy when transcriptionReady arrives | VERIFIED | `KeyboardViewController.swift:35-39` -- `onTranscriptionReady` callback calls `self.textDocumentProxy.insertText(processedText)`. Smart text processing on lines 92-135 |
| 17 | Mic button shows visual state (idle/waiting/recording/transcribing) | VERIFIED | `KeyboardExtensionView.swift:55-75` -- `micButtonLabel` (4 emojis), `micButtonColor` (4 colors), `micButtonForeground` (idle vs active). Button on lines 124-150 |
| 18 | Old polling timer is removed from KeyboardViewController | VERIFIED | `KeyboardViewController.swift` -- zero matches for `pollingTimer`, `checkForResult`, `kbResultReady`. File is 136 lines with only Darwin IPC wiring |

**Score:** 17/18 truths verified (1 intentional deviation)

### Truth #4 Deviation Analysis

**This looks intentional.** Commit `33afb01` ("keep isShortcutLaunch alive through stopDictation for swipe hint") deliberately removed the reset from `stopDictation()`. The functional requirement "manual dictation is unaffected" is still met because `startDictation(fromShortcut: false)` (the default) resets the flag at line 61 before any manual dictation begins. To accept this deviation, add to VERIFICATION.md frontmatter:

```yaml
overrides:
  - must_have: "stopDictation() resets isShortcutLaunch to false so manual dictation is unaffected"
    reason: "Reset moved to startDictation(fromShortcut:) to keep swipe hint visible post-dictation. Manual dictation defaults fromShortcut=false which resets the flag. Commit 33afb01."
    accepted_by: "{your name}"
    accepted_at: "2026-04-23T20:15:00Z"
```

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `iOS/Dicticus/IPC/DicticusIPCBridge.swift` | Shared IPC constants, notification names, UserDefaults keys, heartbeat helpers | VERIFIED | 131 lines, 7 notification names, 4 UD keys, RecordingState enum, heartbeat with OSAllocatedUnfairLock, post helper. Foundation-only import. |
| `iOS/Dicticus/IPC/DicticusHostBridge.swift` | Main app Darwin notification bridge with heartbeat | VERIFIED | 116 lines, @MainActor, 3 command callbacks, 4 publish methods, 1Hz heartbeat timer, Darwin observer registration with Unmanaged pointer bridging |
| `iOS/DicticusKeyboard/IPC/DicticusKeyboardIPCManager.swift` | Keyboard-side Darwin notification sender/receiver | VERIFIED | 152 lines, @MainActor, 3 send commands, 4 receive callbacks, stale state reconciliation, Darwin observer registration |
| `iOS/DicticusKeyboard/Dictation/DicticusKeyboardDictationController.swift` | Keyboard dictation state machine | VERIFIED | 169 lines, @MainActor ObservableObject, 4-state machine, warm/cold start, 0.5s grace period, 5s waiting timeout, handleMicTap/handleCancelTap |
| `iOS/DicticusKeyboard/KeyboardExtensionView.swift` | Updated keyboard UI with stateful mic button | VERIFIED | 166 lines, @ObservedObject dictationController, Button (not Link), micButtonLabel/Color/Foreground computed properties, long-press cancel gesture |
| `iOS/DicticusKeyboard/KeyboardViewController.swift` | Darwin IPC controller, no polling | VERIFIED | 136 lines, dictationController wiring, responder-chain URL opener, smart text insertion, zero polling code |
| `iOS/Dicticus/DictationViewModel.swift` | hostBridge property with publish calls at lifecycle points | VERIFIED | hostBridge property (line 22), publishRecordingStarted (89), publishTranscribing (100), publishTranscriptionReady (127), publishNoSpeech (130, 149, 152) |
| `iOS/Dicticus/DicticusApp.swift` | hostBridge @StateObject with callback wiring | VERIFIED | @StateObject hostBridge (line 10), callbacks wired (lines 79-97), registerObservers (line 98), dicticus://record/start URL handler (lines 37-41) |
| `iOS/project.yml` | DicticusIPCBridge in DicticusKeyboard target sources | VERIFIED | Line 59: `path: Dicticus/IPC/DicticusIPCBridge.swift` under DicticusKeyboard sources |
| `iOS/Dicticus/Intents/DictateIntent.swift` | isShortcutLaunch flag write in perform() | VERIFIED | Lines 17-18: writes both `pendingDictation` and `isShortcutLaunch` via `DicticusIPCBridge.defaults` |
| `iOS/Dicticus/Intents/DicticusShortcuts.swift` | Quick dictation Siri phrase | VERIFIED | Line 11: `"Quick dictation with \(.applicationName)"` |
| `iOS/DicticusTests/DictationViewModelTests.swift` | Unit tests for isShortcutLaunch lifecycle | VERIFIED | `testIsShortcutLaunchInitiallyFalse` (line 40), `testStopDictationFromIdleDoesNotAffectShortcutFlag` (line 45) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| DicticusHostBridge | DictationViewModel | callback closures wired in DicticusApp | WIRED | `DicticusApp.swift:79-97` -- `hostBridge.onStartRecordingCommand`, `onStopRecordingCommand`, `onCancelRecordingCommand` all wired to viewModel methods |
| DicticusHostBridge | DicticusIPCBridge | shared constants and helpers | WIRED | Every publish method in HostBridge calls `DicticusIPCBridge.setRecordingState`, `postDarwinNotification`, `setTranscription` |
| DicticusKeyboardDictationController | DicticusKeyboardIPCManager | ipcManager property | WIRED | `DicticusKeyboardDictationController.swift:28` -- `let ipcManager = DicticusKeyboardIPCManager()`. Used: `ipcManager.sendStartCommand()` (line 94), `sendStopCommand()` (line 105), `sendCancelCommand()` (line 117), `registerObservers()` (line 72), callbacks wired (lines 47-71) |
| DicticusKeyboardIPCManager | DicticusIPCBridge | shared constants | WIRED | Every send/receive method references `DicticusIPCBridge.Notification.*`, `DicticusIPCBridge.setRecordingState`, `latestTranscription`, etc. |
| KeyboardViewController | DicticusKeyboardDictationController | dictationController property | WIRED | `KeyboardViewController.swift:8` -- `private let dictationController = DicticusKeyboardDictationController()`. Setup (line 18), teardown (line 23), passed to KeyboardExtensionView (line 49) |
| DictateIntent | DictationViewModel | App Group UserDefaults isShortcutLaunch key | WIRED | `DictateIntent.swift:18` writes `isShortcutLaunch=true`. `DictationViewModel.swift:217` reads it in `checkPendingIntent()` |
| KeyboardExtensionView | DicticusKeyboardDictationController | @ObservedObject | WIRED | `KeyboardExtensionView.swift:41` -- `@ObservedObject var dictationController`, mic button calls `dictationController.handleMicTap()` (line 125), state-dependent computed properties reference `dictationController.state` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| KeyboardExtensionView | dictationController.state | DicticusKeyboardDictationController @Published state | State machine transitions driven by Darwin IPC callbacks from real transcription events | FLOWING |
| DicticusHostBridge | publishTranscriptionReady text | DictationViewModel.stopDictation -> transcriptionService?.stopRecordingAndTranscribe() | Real ASR result from FluidAudio/Parakeet model | FLOWING |
| KeyboardViewController | onTranscriptionReady text | DicticusKeyboardIPCManager.onTranscriptionReady -> reads latestTranscription from UserDefaults | Written by DicticusHostBridge.publishTranscriptionReady with real transcription text | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED (requires running iOS Simulator with keyboard extension enabled and real audio input -- cannot test without device/simulator interaction and microphone access)

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-----------|-------------|--------|----------|
| KEYB-02 | 17.5-01, 17.5-02, 17.5-03 | Transcribed text appears at cursor in any text field (no paste required) | SATISFIED | `KeyboardViewController.swift:38` -- `self.textDocumentProxy.insertText(processedText)` inserts directly at cursor. Smart text processing (capitalization, spacing) on lines 92-135. Full IPC pipeline: mic tap -> Darwin notification -> main app records -> transcription -> Darwin notification -> keyboard reads text -> insertText |

Note: REQUIREMENTS.md maps KEYB-02 to Phase 17 (completed) and does not explicitly add Phase 17.5. Phase 17.5 is an enhancement to the KEYB-02 implementation -- replacing the URL-bounce approach with Darwin IPC so the user never leaves their current app. No orphaned requirements detected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| KeyboardExtensionView.swift | 119 | `KeyboardKey(label: "123", action: { }, ...)` -- empty action for 123 key | INFO | Placeholder for numeric layer -- pre-existing from Phase 17, not introduced by 17.5 |

No TODO/FIXME/PLACEHOLDER markers found in any Phase 17.5 files. No empty implementations. No hardcoded empty data flowing to rendering.

### Code Review Fixes Verification

All critical and warning findings from `17.5-REVIEW.md` addressed in commit `8091ef3`:

| Finding | Severity | Fix Status | Evidence |
|---------|----------|------------|----------|
| CR-01: Cancel handler doesn't stop audio | Critical | FIXED | `DicticusApp.swift:91-96` -- now calls `cancelRecording()`, sets `state = .idle`, and `publishNoSpeech()` |
| CR-02: DicticusKeyboardIPCManager lacks @MainActor | Critical | FIXED | `DicticusKeyboardIPCManager.swift:12` -- `@MainActor final class` |
| WR-01: Heartbeat uses nonisolated(unsafe) without sync | Warning | FIXED | `DicticusIPCBridge.swift:95` -- `OSAllocatedUnfairLock` with `withLock` |
| WR-02: Dual IPC systems | Warning | DEFERRED | Intentionally kept for backward compatibility during migration |
| WR-03: publishNoSpeech double-clears state | Warning | FIXED | `DicticusHostBridge.swift:59-61` -- no longer calls `clearTransientOperationState()`, just sets state to idle |
| WR-04: Cold-start URL legacy cleanup | Warning | DEFERRED | Intentionally kept for backward compatibility |
| WR-05: Misleading timer closure pattern | Warning | FIXED | `DicticusHostBridge.swift:68` -- no `_ = self` or misleading comment |
| IN-01: Hardcoded app group IDs | Info | FIXED | `DictationViewModel.swift` uses `DicticusIPCBridge.defaults` (lines 29, 102, 214). `DicticusApp.swift` uses `DicticusIPCBridge.defaults` (lines 29, 38, 50) |

### Human Verification Required

These items require device testing with the keyboard extension enabled:

### 1. End-to-End Dictation via Keyboard (Warm Start)

**Test:** With Dicticus app running in background, open Messages or Notes, switch to Dicticus keyboard, tap mic button, speak a sentence, wait for transcription
**Expected:** Mic button transitions through all 4 states (microphone -> hourglass -> stop -> speech bubble -> microphone), transcription text appears at cursor in the text field
**Why human:** Full cross-process IPC flow with real audio requires device testing

### 2. Cold Start Dictation via Keyboard

**Test:** Force-quit Dicticus app, open a text field, switch to Dicticus keyboard, tap mic button
**Expected:** Keyboard shows hourglass, app launches via URL scheme, model warms up, recording starts automatically, transcription delivered back to keyboard
**Why human:** App launch and model warmup timing cannot be tested programmatically

### 3. Cancel Dictation via Long-Press

**Test:** Start dictation from keyboard, then long-press the mic button for 0.5 seconds
**Expected:** Recording cancels, mic button returns to idle (microphone emoji), no text inserted
**Why human:** Long-press gesture interaction and IPC cancel flow require device testing

### 4. Smart Text Insertion

**Test:** Type "Hello" in a text field, then dictate "World" via keyboard mic button
**Expected:** Text inserted as " world" (lowercase w, leading space) since cursor is mid-sentence
**Why human:** textDocumentProxy context varies by host app and requires real text field interaction

### Gaps Summary

One truth (#4) is technically FAILED: "stopDictation() resets isShortcutLaunch to false" -- the reset was intentionally moved to `startDictation(fromShortcut:)` in commit `33afb01` to keep the swipe hint visible after dictation. The functional intent (manual dictation unaffected) is still met. This is a candidate for an override.

All 12 artifacts exist, are substantive (no stubs), and are properly wired. All 7 key links verified. All code review critical/warning findings are addressed. The KEYB-02 requirement is satisfied -- transcription text inserts at cursor via textDocumentProxy without paste.

The remaining verification gap is end-to-end device testing of the Darwin IPC flow, which requires a physical device or simulator with the keyboard extension enabled and real audio input.

---

_Verified: 2026-04-23T20:15:00Z_
_Verifier: Claude (gsd-verifier)_
